# 断言

在测试函数中，我们会做一些简单的事情：获取期望值，然后检查函数返回值是否等于该值。还有其他有用的断言方法，例如`assertTrue`、`assertFalse`和`assertContains`。你可以在 PHPUnit API（[`https://phpunit.de/manual/current/en/appendixes.assertions.html`](https://phpunit.de/manual/current/en/appendixes.assertions.html)）中查看所有断言方法。

