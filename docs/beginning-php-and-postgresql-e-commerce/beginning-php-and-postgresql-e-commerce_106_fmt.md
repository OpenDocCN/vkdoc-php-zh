# 数据库集成实现

## 连接 PostgreSQL（82–87）

`PDO`（24–26）提供了一个数据库抽象层（24, 26）。

在 Unix/Linux 上安装（580–582）——处理运行时错误（84）。

在 Windows 上安装（574–575）。

使用 `mcrypt` 库实现数据库集成（361, 24–26）。

将 PHP 逻辑与 HTML 混合（21）。

使用 PHP `PDO` 进行数据库操作——可用的 `RDBMS`（23, 83–84）。

将表现逻辑从 HTML 中分离——`PDO` 类（83），`exec` 方法（87）。

服务器端脚本语言（21），`prepare` 方法（86, 89）。

使用 `PDO` 进行数据库操作（83–84）。

`PDO` 库——在 Windows 上安装 PHP 5（574）。

`PDOStatement` 类——`execute` 方法（86, 90），`fetch` 方法（86, 91），`fetchAll` 方法（86, 90）。

PHP 社区（21）。

`PHP Designer 2006` 编辑器（29）。

PHP 错误（84）。

`PECL`（24）。

PHP 文件——`Smarty` 组件（36）。

`PHP PDO`——参见 PDO（PHP 数据对象）扩展。

PHP SOAP 扩展（556）。

`phpEclipse` 编辑器（29）。

`phpinfo` 函数（31）。

`phpLib` 模板引擎（23）。

## 性能

数据库函数（78），`HowManyPages` 方法（138, 139）。

索引（70）。

链接到安全/非安全页面（103–106）。

大型结果集的分页（125）。

预处理语句（87）。

主键 `ID` 列（66）。

使用带 `LIKE` 的 `WHERE` 进行搜索（171）。

会话变量（136）。

`$persistent` 变量——`PDO` 类构造函数（84）。

持久连接（82）。

`pgAdmin III` 工具——创建分类表（114），创建部门表（71），创建 HatShop 数据库（52–55）。

PostgreSQL——从 PHP 代码访问（24），与它通信（26）。

连接至（82–87）。

创建 HatShop 数据库（52–55）。

创建 PostgreSQL 函数和类型（79–81）。

数据类型（66–68）。

在 Unix/Linux 上安装（578–580）。

在 Windows 上安装（576–577）。

其他接口（24）。

`pgAdmin III` 接口（24）。

`psql` 基于文本的接口（24）。

三层架构与（26）。

使用 PHP `PDO` 进行数据库操作（83–84）。

使用 PostgreSQL 存储网站数据（23–26）。

`PostgreSQL` 服务器——打开和关闭与其的连接（82–83）。

`PRIOR_AUTH_CAPTURE` 交易类型——`MakeXmlPre` 方法（517），与 `Authorize.net` 配合使用（524, 525, 531）。

预认证请求——`DataCash`（508–509）。

预认证响应——`DataCash`（509–510）。

私有类属性名称（27）。

私有构造函数（93）。

私钥（360）。

私有方法（139）。

`Prepare` 方法（86, 87, 89）。

预处理语句（86）——防止 SQL 注入攻击（87）。

`prepare_link` `Smarty` 修饰符——创建 `categories_list` 模板（152），链接到安全/非安全页面（103, 498），更新订单管理页面。

表现逻辑——将模板引擎从 HTML 中分离。

表现层——添加客户账户功能（388–412），添加“下订单”按钮（312）。

## 待处理订单

显示（316）。

实现业务层（319–320）。

实现数据层（317–319）。

实现表现层（320–324）。

性能——`HowManyPages` 方法（138, 139）。

管道——参见订单处理管道。

“下订单”按钮——添加到表现层（312），功能（304），实现功能（313, 314），实现表现层（312）。

`PL/pgSQL`——数据库函数（78）。

插件——参见 `Smarty` 插件文件。

可移植性——使用 `PDO`（24）。

端口——链接到安全/非安全页面（105）。

`pgAdmin III` 工具——创建分类表（114），创建部门表（71），创建 HatShop 数据库（52–55）。

PostgreSQL——从 PHP 代码访问（24），与它通信（26）。

连接至（82–87）。

创建 HatShop 数据库（52–55）。

创建 PostgreSQL 函数和类型（79–81）。

数据类型（66–68）。

在 Unix/Linux 上安装（578–580）。

在 Windows 上安装（576–577）。

其他接口（24）。

`pgAdmin III` 接口（24）。

`psql` 基于文本的接口（24）。

三层架构与（26）。

使用 PHP `PDO` 进行数据库操作（83–84）。

使用 PostgreSQL 存储网站数据（23–26）。

## PostgreSQL 服务器

打开和关闭与其的连接（82–83）。

## 预认证

请求——`DataCash`（508–509）。

响应——`DataCash`（509–510）。

## 私有

类属性名称（27）。

构造函数（93）。

密钥（360）。

方法（139）。

## `Process` 方法 – `OrderProcessor` 类

实现订单管道（465–467）。

实现 `PsCheckFunds` 类（479）。

实现 `PsInitialNotification` 类（103, 498）。

实现 `PsStockOk` 类（482）。

## 产品管理（226–244）

### 产品目录

外观（58）。

目录管理页面——分类管理（226–244），部门管理（215–226），产品管理（226–244）。

## 将 ECS 与 HatShop 集成（564–568）

为订单管理进行修改（435–439, 441）。

分页（125）。

产品评论（541–545）。

搜索产品目录（181–187）。

购物车（284–295）。

购物车管理页面（299）。

存储订单（312）。

更新订单管理页面（500–503）。

## `PGP（Pretty Good Privacy）`（361）

`price` 字段——产品表（118）。

主键（64–66）——自动编号列（68），多对多关系如何工作，`ID` 列（65），`NULL` 值（65, 68）。

[www.it-ebooks.info](http://www.it-ebooks.info/)

648XIndex.qxd 11/22/06 2:46 PM Page 609

■索引

**609**

`添加购物车推荐`，`产品详情管理`，244–262

347–350

`设置`，202–215

`管理部门`，216–222

`catalog_count_products_in_category`

`管理产品详情`，245–253

函数，129

`description`，15

`catalog_count_products_on_catalog`

`显示部门和类别`

函数，133

`详情`，143–148

`catalog_count_products_on_department`

`显示首页内容`，162–163

函数，131

`显示类别列表`，149–152

`catalog_get_categories_list`函数，127

`显示部门列表`，95–103

`catalog_get_category_details`函数，128

`显示待处理订单`，320–324

`catalog_get_departments_list`函数，

`显示产品详情`，163–166

79–80

`显示产品列表`，153–161

`catalog_get_department_details`函数，

`电子商务项目周期`，590

`实现`，142–166

`catalog_get_products_in_category`

`实现订单提交`

函数，129

`功能`，313–314

`catalog_get_products_on_catalog`

`实现税费和运费`，

函数，133

448–452

[www.it-ebooks.info](http://www.it-ebooks.info/)

648XIndex.qxd 11/22/06 2:46 PM Page 610

**610** ■索引

`catalog_get_products_on_department`

`类别`，`部门`，和`产品`，58

函数，131

`构建电子商务网站时的`

`catalog_get_product_details`函数，134

`注意事项`，5

`类别`，58

`从购物车中删除产品`，296

`创建类别表`，113–114

`显示产品详情`，163–166

`创建部门表`，70–74

`显示产品列表`，153–161

`创建部门列表模板`，

`GetProductDetails`方法，142

95–103

`GetProductsInCategory`方法，139

`创建部门列表类型`，79

`GetProductsOnCatalog`方法，141

`创建产品表`，118–119

`GetProductsOnDepartment`方法，141

`创建产品-类别表`，120–121

`获取简短描述`，122, 129

`GetProductsOnCatalog`方法，141

`与类别之间的多对多关系`，

`实现数据层函数`，126–134

111

`实现部门列表`，60

`PRODUCTS_PER_PAGE`常量，135

`实现表示层`，142–166

`SHORT_PRODUCT_DESCRIPTION_LENGTH`常量，135

`预览`，58–60

`搜索产品目录`，169–187

`分页显示查询结果`，

*另请参阅* `搜索产品目录`

125–126, 129

`存储数据`，62

`ProductsList`类

`三层架构`，61

`创建 products_list 模板`，160

`为业务层编写代码`，135–142

`创建 search_results 模板`，184

`Product`类

`products_list 模板`，153–161

`创建产品模板`，166

`将产品添加到自定义购物车`，

`产品详情管理`，244–262

`业务层`，254–257

`创建`，154, 160

`数据层`，257–262

`创建 search_results 模板`，186

`表示层`，245–253

`定义产品列表常量`，135

`产品推荐`，335

`显示搜索结果`，170

`实现业务层`，346, 347

`集成 PayPal 购物车与`

`实现数据层`

`结账`，192, 195

`添加产品推荐`，

`PRODUCTS_PER_PAGE`常量

342–344

`创建 products_list 模板`，161

`添加购物车`

`定义产品列表常量`，135, 136

`推荐`，344–346

`HowManyPages`方法，138

`理解其逻辑`，337–341

`product_category 表`，120–121

`实现表示层`，347–350

`连接数据表`，123

`利用动态推荐增加销量`，

`product_id`字段

335

`product 表`，118

`产品评论`，537–545

`product_category 表`，120

`catalog_create_product_review`函数，

`shopping_cart 表`，271

`catalog_get_product_reviews`函数，

`程序设计`阶段

`实现业务层`，540

`瀑布`方法，`项目管理`，

`实现数据层`，538–540

`项目管理`，583–591

`实现表示层`，541–545

`开发软件解决方案`，583

`reviews 模板`，541

`电子商务项目周期`，589–591

`product 表`，118–119

`极限编程 (XP)`方法，588

`为其添加 Gist 索引`，172

`维护客户关系`，591

`product 模板`，163–166

`快速应用开发 (RAD)`方法，588

`将产品添加到自定义购物车`，

`选择使用的方法`，589

`软件开发生命周期模型`，

`创建`，163, 166

`更新`，347–350