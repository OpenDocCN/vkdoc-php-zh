# 17. 使用数据库进行用户认证

第 9 章向你展示了用户认证和使用会话保护网站部分区域的原则，但所有登录脚本都依赖于存储在 CSV 文件中的用户名和密码。将用户详细信息保存在数据库中的方式既更安全也更高效。数据库不仅可以存储用户名和密码列表，还可以存储其他详细信息，例如名字、姓氏、电子邮件地址等。数据库还为你提供了使用单向或双向加密的选项。在本章的第一节中，我们将探讨两者的区别。然后，你将针对这两种加密类型创建注册和登录脚本。

本章内容涵盖：

*   决定如何加密密码

*   使用单向加密进行用户注册和登录

*   使用双向加密进行用户注册和登录

*   解密密码