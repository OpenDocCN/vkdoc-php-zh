# NuSOAP

- 返回数组，498
- 维护键/值关联，120
- 输出，105–106

## 未维护键/值关联

- 将元素传递给用户定义函数，114
- `pg_fetch_array` 函数，678
- `PGSQL_BOTH` 值，679
- `PGSQL_ASSOC` 值，678

## 多维数组

- 维持自然数顺序，119
- 打印，105
- `register_long_arrays` 指令，34
- 移除重复值，118
- 按值从低到高对元素排序，118

## 返回

- 键的数组，111
- 值的数组，112
- 数组的第一个元素，110
- 当前指针处的键元素，113
- 当前指针处的键/值对，113
- 数组的最后一个元素，110
- 数组的最后一个元素（指针移至末尾），114
- 当前指针之后的下一个数组值，114
- 随机值，129
- 当前指针处的值，113
- 当前指针之前的值，114

## 反转键/值角色

- `arsort` 数组函数，122
- 反转元素顺序，116

## 搜索

- `asort` 数组函数，120
- 所有元素，201
- 搜索特定键，返回真/假，112
- 搜索特定值，返回键，112
- 搜索特定值，返回真/假，111
- 将数组指针设置为数组末尾，114
- 将数组指针设置为数组起始，113
- `sqlite_array_query` 函数，543
- `sqlite_fetch_array` 函数，541
- 起始位置为零，104
- 测试变量是否为数组，108–109
- 遍历，112–116
- 唯一性，118
- 处理多值表单组件，307
- 发送至客户端，498

## `CREATE RULE` 命令

- `AS ON event_type`，709

## `asp_tags` 参数

- `ASP` 风格标签（`<% ... %>`）界定 PHP 代码，45
- `asp_tags` 参数，22

## assign 参数

- `insert` 标签，Smarty，463

##### 赋值运算符

- `assoc_case` 指令，sqlite，538

