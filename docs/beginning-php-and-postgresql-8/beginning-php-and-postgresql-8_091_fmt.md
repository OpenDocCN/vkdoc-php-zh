# `create_dropdown` 函数

## 守护进程帐户参数

- 自动选择表单数据，310
- 安装 PostgreSQL，586
- 使用 PHP 生成表单，308

## 数据

- `createdb` 命令行工具，626
- 将数据从表复制到文本文件，782
- 将凭据数据复制到/从表中，778–782
- `ldap_bind` 函数，402、403
- 隐藏敏感数据，522–523
- 跨站脚本攻击，524
- 导入和导出数据，777–785

## `cryptType` 元素

- `phpPgAdmin`，783–785
- `Auth_HTTP` 类，PEAR，337
- 检索和显示数据

## CSS（层叠样式表）

- PostgreSQL，678–681
- 文字标签，Smarty，464
- 已选行数与已修改行数，681
- Smarty 配置文件与，465
- 清理用户数据，524–528
- 与 Smarty 模板引擎一起使用，467–468

## 数据加密，528–532

- `Auth_HTTP` 类，PEAR，337

## CSV（逗号分隔值）文件，246

- 将数据从表复制到文本文件，782
- 将数据复制到/从表中，781

## 花括号语法

- PHP 5 中的变更，192

## 货币

- 本地化格式，280

## `current` 数组函数，113

## `current_date` 函数，PostgreSQL，724

## `current_time` 函数，PostgreSQL，724

## `current_timestamp` 函数，PostgreSQL，724

## `currval` 序列函数，634

## `cursor_offset` 参数

- `fetch` 方法，PDO，568

## `cursor_orientation` 参数

- `fetch` 方法，PDO，568

## 自定义错误处理器

- 导航提示，321–323

## `CYCLE` 选项

- 创建序列，633

## Cygwin，430

[www.it-ebooks.info](http://www.it-ebooks.info/)

**799**

## 数据源名称

- *参见* DSN

## 数据库服务器选项类别

- 安装 PostgreSQL，585

## 数据类型，PHP，50–57

- 数组，52
- 布尔型，50
- 复合数据类型，52
- 浮点数，51
- 整型，51
- 空值，54
- 对象，53
- 资源，53
- 字符串，51
- 类型转换，54
- 类型标识函数，57（`is_name` 函数，57）
- 类型转换，55
- 类型相关函数，56（`gettype` 函数，57；`settype` 函数，56）

## 数据唯一性

- 索引，PostgreSQL，749

## 数据库抽象层

- 描述，555
- 列表，556
- PHP 数据对象，556–572

## 基于数据库的身份验证，PHP，331–332

- 根据 PostgreSQL 表验证用户，332
- 使用登录对和 IP 地址进行身份验证，333

## 数据库类

- *参见* PostgreSQL 数据库类

## 数据库集群选项

- 安装 PostgreSQL，587

## 数据库驱动选项类别

- 安装 PostgreSQL，586

## `DATABASE` 字段，`pg_hba.conf` 文件，654

## 数据库操作

- 事务，765–775

## `datadir` 选项

- 从源代码安装 PostgreSQL，583

## 数据类型，PostgreSQL，635–640

- 属性，635、640–644
  - `CHECK`，640
  - `DEFAULT`，641
  - `NOT NULL`，642
  - `NULL`，642
  - `PRIMARY KEY`，642
  - `REFERENCES`，643
  - `UNIQUE`，644
- `BOOLEAN`，640
- `bytea`，635
- 复合类型，635、644–645
- 日期和时间数据类型，636–637（`DATE`，636；`INTERVAL`，637；`TIME`，636；`TIMESTAMP`，637）
- 域，635
- `inet` 类型，635
- 数值数据类型，637–639（`BIGINT`，637；`BIGSERIAL`，639；`DECIMAL`，638；`DOUBLE PRECISION`，638；`FLOAT`，638；`INTEGER`，637；`NUMERIC`，638；`REAL`，638；`SERIAL`，639；`SMALLINT`，637）
- 字符串数据类型，639–640（`CHAR`，639）

## `getYear`，293

## `isLeap`，293

## `isValid`，294

## `setDay`，291

## `setDMY`，290

## `setFirstDow`，300

## `setJulian`，292

## `setLastDow`，301

## `setMonth`，292

## `setToLastMonthDay`，300

## `setToWeekday`，298

## `setYear`，293

## `subDays`，294

## `subMonths`，295

## `subWeeks`，296

## `subYears`，297

[www.it-ebooks.info](http://www.it-ebooks.info/)

**800**

## 索引

### A

`mutators`（设置器），290

### C

`Calendar` 包，PEAR，286

`checkdate` 函数，272

### D

`date`（日期和时间库），PHP 5.1，288–301

使用注意事项，289

`Date` 构造器，289

日期处理能力，294

方法

`addDays`，294

`addMonths`，295

`addWeeks`，296

`addYears`，297

`date` 属性，消息，380，383

日期类，`Calendar` 包，PEAR，286

`DATE` 数据类型，PostgreSQL，636

`date` 函数，PHP，272–275

确定当前月份的天数，283

格式参数，273

`date` 方法，290

`date_format` 函数，Smarty，455

`date_part` 函数，PostgreSQL，724

日期，`Calendar` 包，PEAR，285–288

本地化格式，280

Unix 纪元前的日期，272

标准化日期格式，271

`DB` 数据库抽象层，556

`DB` 包，PEAR，260

`$dblogin` 数组，`Auth_HTTP` 类，PEAR，336

`dbname` 参数，`pg_connect` 函数，668

Debian 操作系统

下载 PostgreSQL，580

启动和停止 PostgreSQL 服务器，596

`debug_flag` 属性，NuSOAP，501

`debug_str` 属性，NuSOAP，502

调试

`getLastRequest` 方法，SOAP，505

`getLastResponse` 方法，SOAP，505

NuSOAP，501

`DECIMAL` 数据类型，PostgreSQL，638

`declare` 语句，PHP，78

声明变量，PHP，58–60

解码，*参见* 数据加密

`decorator` 类，`Calendar` 包，PEAR，286

递减（`--`）运算符，72

解密，*参见* 数据加密

默认参数值，94

`DEFAULT` 属性，PostgreSQL 数据类型，641

默认异常构造器，基础异常类，185

`default` 函数，Smarty，456

`default_charset` 参数，36

`default_mimetype` 参数，35

`default_socket_timeout` 参数，39

`define` 函数，68

`define_syslog_variables` 函数，181

`define_syslog_variables` 参数，39

删除规则，PostgreSQL，711

`DELETE` 语句，使视图可交互，711

`deleted` 属性，消息，380，383

删除数据，PostgreSQL，685

删除条目/值，LDAP，418

`delim` 参数，683，684

`DELIMITER` 子句，`COPY` 命令，更改默认分隔符，780

分隔符，模板引擎，448

界定 PHP 代码，43–46

`ASP` 风格标签（`<% ... %>`），45

默认语法（`<?php ... ?>`），44

嵌入多个代码块，45

脚本标签，45

短标签，44

依赖关系，PEAR 包

自动安装依赖项，267

失败的依赖项，266

`deref` 参数，`ldap_search()`，405

设计，网站，导航提示，313–323

析构函数，面向对象编程，151–152

开发选项类别，安装 PostgreSQL，586

字典，使用 `CrackLib` 扩展，341

`die` 语句，PostgreSQL，691，692

`dir` 文件类型，232

指令，*参见* PHP 配置指令，*另见* PHP 配置指令列表

指令，SQLite，537–538

目录，*另见* LDAP

关闭目录流，251

LDAP 与目录操作，421

`ldap_dn2ufn` 函数，421

`脏读`，事务隔离，766

`disable_classes` 参数，27，519

`disable_functions` 参数，26，518

磁盘页

`max_fsm_pages` 设置，PostgreSQL，597

`random_page_cost` 设置，PostgreSQL，598

`disk_free_space` 函数，236

`disk_total_space` 函数，236

磁盘，识别分区的空闲/总空间，236

`display` 方法，Smarty，453

`:digit:` 字符类，195

`display_errors` 参数，30，179

`display_startup_errors` 参数，30，179

显示数据，PostgreSQL，678–681

`专有名称`，*参见* `DN`

除法（`/`）运算符，71

`DN`（专有名称），别名，LDAP，419

LDAP 与 DN 操作，421

`ldap_dn2ufn` 函数，421

### I

`INTERVAL`，637

### L

`LDAP`

`别名`，419

`ldap_dn2ufn` 函数，421

### M

`mutators`（设置器），290

### P

`PHP`

日期和时间函数，272–278

计算日期，284

确定当前月份的天数，283

显示本地化的日期和时间，279–282

显示网页修改日期，283

函数

`checkdate`，272

`date`，272–275

`getdate`，275

`getlastmod`，283

`gettimeofday`，276

`mktime`，277

`setlocale`，279

`strftime`，281–282

`strtotime`，284

`time`，278

`PostgreSQL`

日期和时间数据类型，636–637

`DATE`，636

`INTERVAL`，637

`TIME`，636

`TIMESTAMP`，637

日期和时间函数，723，724

### S

`session_decode` 函数，436

`setlocale` 函数，279

`sqlite_udf_decode_binary` 函数，551

`strftime` 函数，281–282

`strtotime` 函数，284

### T

`TEXT`，640

与早期版本配合使用，289

`TIME`，636

`TIMESTAMP`，637

`time` 函数，278

### V

`VARCHAR`，640

与早期版本配合使用，289

验证器，293

`accessors`（获取器），290