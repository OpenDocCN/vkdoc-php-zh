# 索引条目排版

`CreateOrder` 方法, `ShoppingCart` 类, 312, 428, 448

`创建搜索框`, 181–183

`支持搜索的数据结构`, 172–173

`数据层`, 171–178

`设计搜索功能`, 170

`设计购物车`, 270

`显示自定义购物车摘要`, 287

`显示搜索结果`, 170, 184–187

`显示购物车摘要`, 269

`更快查找`

`生成购物车 ID`, 283

`HatShop 购物车`, 268

`HTTP/HTTPS 协议`, 203

`实现自定义购物车`, 267–301

`实现 PsShipGoods 类`, 483

`实现 PsShipOk 类`, 484

`集成 PayPal 购物车与结账`, 192–197

`load_admin_cart.php`, 299

`load_admin_categories.php`, 229

`load_admin_departments.php`, 218

`load_admin_login.php`, 212

`load_admin_order_details.php`, 457, 463, 489, 495, 502

`load_admin_product.php`, 247

`load_admin_products.php`, 234

`load_amazon_products_list.php`, 565

`load_cart_details.php`, 290

`load_cart_summary.php`, 288

`load_categories_list.php`, 150

`load_checkout_info.php`, 457, 495, 496

`load_department.php`, 144, 147

`load_departments_list.php`, 96, 99, 102, 118

`表示层`, 181–187

`search_box 模板`, 181–183

`search_results 模板`, 184–187

`搜索数据库`, 173–174

`秘密访问密钥`, Amazon, 550

`SecureCard 类`, 实现, 366–376

`secure_card.php`, 357

`安全连接`

`链接到安全/非安全页面`, 103–106

`安全性`

`信用卡交易协议`, 505

`数据库函数`, 78

`网关提供商`, 505

`HTTP/HTTPS 协议`, 203

`Web 安全组织`, 203

`设置目录管理页面`, 296–301

`使用安全连接`, 203–205

`安全证书`, 203

`安全类`, 实现, 356

`SELECT 语句`, SQL

`* 通配符`, 74–76

`DISTINCT 子句`, 132

`FROM 子句`, 75

`LIMIT 子句`, 318

`ORDER BY 子句`, 76

`SetCartId 方法`, 282, 283

`SetDateShipped 方法`, 472

`SetHandler 方法`, 50

`SetImage 方法`, 254

`SETOF 关键字`, 80

`SetOrderAuthCodeAndReference 方法`, 508

`SetProductDisplayOption 方法`, 254

`SetRequest 方法`, 529, 530

`SetThumbnail 方法`, 254

`setweight 函数`, 172

`set_error_handler 函数`, 44, 50

`sha1 函数`, 212

`发货邮件`, 491

`配送`

`设计订单处理管道`, 455

`实现 PsShipGoods 类`, 483

`实现 PsShipOk 类`, 484

`设置订单发货日期`, 472

`配送相关问题`

`业务层修改`, 447–448

`数据库修改`, 443–447

`订单管理`, 442

`表示层修改`, 448–452

`shipping_region 表`, 355–356

`购物车`

`向自定义购物车添加产品`, 285–286

`管理购物车`, 296–301

`管理页面`, 296–301

`业务层`, 277–283

`分阶段开发考虑因素`, 8

`数据层`, 271–277

`从购物车删除产品`, 296

`设计购物车`, 270

`显示自定义购物车摘要`, 287

`显示购物车摘要`, 269

`生成购物车 ID`, 283

`HatShop 购物车`, 268

`实现自定义购物车`, 267–301

`集成 PayPal 购物车与结账`, 192–197

`表示层`, 284–295

`存储购物车信息`, 270–295

`购物车模板`, 284

`ShoppingCart 类`

`CreateOrder 方法`, 312, 428, 448

`GetCartId 方法`, 283

`GetCartProducts 方法`, 283

`SetCartId 方法`, 282, 283

`shopping_cart 表`

`创建`, 270–271

`从中删除旧记录`, 296

`存储购物车信息`, 270–271

`shopping_cart.php 文件`, 277

`shopping_cart_add_product 函数`, 271–273

`shopping_cart_count_old_carts 函数`, 296

`shopping_cart_create_order 函数`

`添加到 hatshop 数据库`, 308–311

`由 CreateOrder 方法调用`, 312, 313

`修改`, 426, 427

`shopping_cart_delete_old_carts 函数`, 296

`shopping_cart_empty 函数`, 308–311

`shopping_cart_get_products 函数`, 271, 274, 275

`shopping_cart_get_recommendations 函数`, 344, 345, 346

`shopping_cart_get_saved_products 函数`, 271, 275, 276

`shopping_cart_get_total_amount 函数`, 271, 276

`site.conf 文件`

`构建第一个 HatShop 页面`, 37

`Smarty 模板引擎工作原理`, 43

`SITE_ROOT 常量`, 39

`site_title 变量`, 43

`小组响应`, 554

`Smarty 组件化模板`

*另请参阅* `Smarty 模板`

`描述`, 36

`图示`, 22

`实现 HatShop 站点骨架`, 34–43

`HatShop 中网页的结构`, 35

`Smarty 设计模板`

`描述`, 36

`图示`, 22

`存储其背后的逻辑`, 96

`Smarty 修饰符`

`链接到安全/非安全页面`, 105

`Smarty 插件`

`描述`, 36

`图示`, 22

`load_admin_cart.php`, 299

`load_admin_categories.php`, 229

`load_admin_departments.php`, 218

`load_admin_login.php`, 212

`load_admin_order_details.php`, 457, 463, 489, 495, 502

`load_admin_product.php`, 247

`load_admin_products.php`, 234

`load_amazon_products_list.php`, 565

`load_cart_details.php`, 290

`load_cart_summary.php`, 288

`load_categories_list.php`, 150

`load_checkout_info.php`, 457, 495, 496

`load_department.php`, 144, 147

`load_departments_list.php`, 96, 99, 102, 118

`按相关性对结果排序`, 174–178

`SSL（安全套接层）连接`, 203

`存储购物车信息`, 270–295

使用 `tsearch2` 模块, 172–178

将 `WHERE` 与 `LIKE` 结合使用, 171

编写代码, 175–178