# 实现自定义购物车，268

## 实现 `PsCheckStock` 类，481

### 稍后购买功能，268

实现 `PsFinalNotification` 类，  
`shopping_cart_add_product` 函数，  
271–273

实现 `PsInitialNotification` 类，  
`shopping_cart_get_products` 函数，  
271, 274–275

实现 `PsShipGoods` 类，484  
`shopping_cart_get_saved_products`  
`GetOne` 方法，87, 91  
函数，271, 275–276

`GetOrderAsString` 方法，473  
`shopping_cart_get_total_amount`  
`GetOrderShortDetails` 方法，434  
函数，271, 276

`GetProductDetails` 方法，142  
`shopping_cart_move_product_to_cart`  
`GetProductInfo` 方法，254  
函数，272, 277

`GetProductReviews` 方法，540  
`shopping_cart_remove_product` 函数，  
`GetProducts` 方法，559, 562, 568  
271, 273–274

`GetProductsInCategory` 方法，139  
`shopping_cart_save_product_for_later`  
`GetProductsOnCatalog` 方法，141  
函数，272, 276–277

`GetProductsOnDepartment` 方法，141  
`shopping_cart_update` 函数，271, 273

`GetRequest` 方法，513, 518

[www.it-ebooks.info](http://www.it-ebooks.info/)  
648XIndex.qxd 11/22/06 2:46 PM Page 603

■索引  
**603**

## HatShop 网站

### 显示自定义购物车详情，  
376–412

### 添加客户账户功能，  
实现业务层，383–388  
显示自定义购物车，  
实现数据层，379–383  
摘要，289

实现表示层开始，  
集成 PayPal 购物车与  
388–412  
结账，195

### 管理员身份验证，207–215  
更新结账页面，497

### 构建第一个页面，37–43  
`hatshopadmin` 角色，52, 54

### 类别、部门和产品，58  
`header` 模板

### 代码下载，55  
构建第一个 HatShop 页面，38

### 为 SSL 配置 HatShop，205  
Smarty 模板引擎工作原理，43

### 创建 PostgreSQL 数据库，52–55  
### 托管解决方案

### 面向增长的设计，14–20  
电子商务项目周期，591

### 设计订单管道，454  
`HowManyPages` 方法，137

### 在 HatShop 中显示亚马逊产品，  
HTML  
564–568  
将 PHP 逻辑与 HTML 混合，21

### 强制启用 SSL 连接，206  
将模板引擎与  
### 获取代码编辑器，29  
表示逻辑分离，22

### 处理并报告错误，44–51  
HTTP 协议  
### 实现网站骨架，34–43  
身份验证，207

### 集成亚马逊超级帽子  
链接到安全/非安全页面，  
部门，548  
103–106

### 将 Authorize.net 与 HatShop 集成，532–535  
安全性，203

### 将 DataCash 与 HatShop 集成，520–523  
`httpd.conf` 文件，29, 31

### 将 ECS 与 HatShop 集成，558–568  
HTTPS 协议  
业务层，559–563  
强制启用 SSL 连接，206  
表示层，564–568  
链接到安全/非安全页面，  
### 登录页面，200  
103–106

### 命名约定，27  
安全性，203  
### 分阶段开发，10  
`HTTP_SERVER_PORT` 常量，105, 106

### 准备 hatshop 虚拟文件夹，29–32  
HatShop 的 PHP 版本要求，571

### 预览产品目录，59  
**I**

### 产品目录，57–167  
`ID` 列，65

### 产品评论，537–545  
`image` 字段，产品表，118

### 所需软件，28  
`images` 文件夹，HatShop，37

### 搜索产品目录，169–187  
`Images` 响应组，554

*另请参阅* 搜索产品目录  
`inCategoryID` 参数，129

### 开发的第二阶段，9  
代码缩进，28

### 启动 HatShop 项目，28–43  
`index` 模板

### HatShop 中网页的结构，35  
构建第一个 HatShop 页面，37, 40

### 来自亚马逊的超级帽子，555  
创建 `departments_list` 模板，100

### 使用的技术，26  
创建 `search_box` 模板，183  
### 三层架构，17, 19  
显示自定义购物车  
### 更新结账页面，497  
摘要，287

## `hatshop.css` 文件  
Smarty 模板引擎工作原理，42–43  
构建第一个 HatShop 页面，38  
集成 PayPal 购物车与  
创建 `categories_list` 模板，149  
结账，192, 194

### 创建部门模板，143  
`index.php` 文件  
创建 `departments_list` 模板，96  
构建第一个 HatShop 页面，40

### 创建 `products_list` 模板，154  
创建 `categories_list` 模板，151

### 创建评论模板，544  
创建部门模板，145, 146

### 创建 `search_box` 模板，183  
创建 `departments_list` 模板，100

### 创建 `search_results` 模板，186  
创建 `first_page_contents` 模板，163  
在 HatShop 中显示亚马逊产品



创建产品模板，163，166

HatShop，565

创建产品列表模板，159

创建搜索结果模板，186

[www.it-ebooks.info](http://www.it-ebooks.info/)

648XIndex.qxd 11/22/06 2:46 PM 第 604 页

**604** ■ 索引

显示自定义购物车详情，

■ **K**

关键词
- `AS`，122
- `BEGIN`，81
- Smarty 模板引擎工作原理，41
- `CASCADE`，115
- `END`，81
- `LANGUAGE`，78，80
- `LIMIT`，125，318
- `NO ACTION`，115
- `OFFSET`，125
- `PRIMARY KEY` 列，66
- `RESTRICT`，115
- `SET DEFAULT`，115
- `SET NULL`，115
- `SETOF`，80

`init` 方法
- `AdminOrders` 类，438
- `CheckoutInfo` 类，449
- `ProductsList` 类，161，185，195

注入攻击 *参见* SQL 注入攻击

`inProductsPerPage` 参数，129

`INSERT` 语句，SQL，76–77

`inShortProductDescriptionLength` 参数，129

安装
- Smarty 模板引擎，32–34
- 在 Unix/Linux 上安装 PHP 5，581

在 Unix/Linux 上安装，577–582
- Apache，577–578
- 在 Linux 上安装 pgAdmin III，579–580
- PHP，580–582
- PostgreSQL，578–580

在 Windows 上安装，571–577
- Apache，573–574
- 启用 SSL，571–573
- PHP，574–575
- PostgreSQL，576–577

`inStartItem` 参数，129

整数数据类型，67

接口，467

互联网支付服务提供商网站，

`IPipelineSection` 接口
- 设计订单管道，455
- 实现订单管道，462，467
- 实现 `PsInitialNotification` 类，468
- 接口和方法签名，467

`IS_WARNING_FATAL` 常量，51

■ **J**

JavaScript
- 在 Smarty 模板中使用，194

`JOIN` 子句，123

连接数据表，123
- `catalog_get_products_in_category` 函数，129
- `catalog_get_products_on_department` 函数，131

连接表
- 多对多关系，111，120

[www.it-ebooks.info](http://www.it-ebooks.info/)

648XIndex.qxd 11/22/06 2:46 PM 第 605 页

■ 索引 **605**

`load_admin_cart.php`，299

`load_admin_categories.php`，229

`load_admin_departments.php`，218

`load_admin_login.php`，212

`load_admin_order_details.php`
- 订单管道，457，463，489
- 更新结账页面，495
- 更新订单管理页面，502

`load_admin_product.php`，247

`load_admin_products.php`，234

`load_amazon_products_list.php`，565

`load_cart_details.php`，290

`load_cart_summary.php`，288

`load_categories_list.php`，150

`load_checkout_info.php`，457，495，496

`load_department.php`，144，147

`load_departments_list.php`
- 创建部门列表模板，99，154
- 在 HatShop 中显示 Amazon.com 产品，567
- 使用 Smarty 插件，96

`load_product.php`，164，286

`load_products_list.php`
- 将产品添加到自定义购物车，195
- 创建产品列表模板，157，160
- 创建搜索结果模板，184
- 集成 PayPal 购物车和结账，195

`load_reviews.php`，543

`load_search_box.php`，182，183

本地主机邮件账户
- 错误处理，51

逻辑
- 存储数据层逻辑，26
- 三层架构，19
- 使用 Smarty 插件，96

登录
- HatShop 登录页面，200

登出

长期需求，14

■ **M**

“魔法”信用卡详细信息

`LoadEncryptedDataAndDecrypt` 方法，
- `LoadPlainDataAndEncrypt` 方法，372

方法
- `AdminMail`，468
- `AssignProductToCategory`，254
- `Close`，89
- `CreateAudit`，459，465，468，469，478，480，496
- `CreateOrder`，312，313，428，448
- `CreateProductReview`，540
- `CreateXml`，373
- `DataFormat`，561–562
- `Decrypt`，366
- `DecryptData`，372，374
- `DeleteDepartment`，222
- `DeleteProduct`，254
- `Encrypt`，366
- `EncryptData`，372，374
- `error_log`，PHP，50
- `exec`，87
- `execute`，86，87，90
- `ExtractXml`，373
- `fetch`，86，91
- `fetchAll`，86，90
- `FlagStopWords`，179，181
- `GetAll`，87，90，95
- `GetAmazonDataWithRest`，563
- `GetAmazonDataWithSoap`，563
- `GetAuditTrail`，500
- `GetBacktrace`，50
- `GetByCustomerId`，434
- `GetCartId`，283

显示自定义购物车详情，292–301
- 摘要，287

Smarty 模板引擎工作原理，41
- 更新结账页面，498
- 使用 `DatabaseHandler` 类，93

索引，70
- 缺点，70
- `PRIMARY KEY` 列，66
- `UNIQUE` 列，66

`LANGUAGE` 关键字，78，80

布局
- 将类别添加到类别表，116
- 使用 Smarty 将布局与代码分离，21–23

libcurl/libxml2 库

软件开发生命周期模型，584–589

`LIKE` 运算符，`WHERE` 子句，171

`LIMIT` 关键字，SQL，125，318

链接表
- 多对多关系，111

链接
- 链接到安全/非安全页面，103–106

Linux
- 在其上安装 pgAdmin III，579–580

字面量元素
- 在 Smarty 模板中使用 JavaScript，194

`inProductsPerPage` 参数，129

`INSERT` 语句，SQL，76–77

`inShortProductDescriptionLength` 参数，129

`inStartItem` 参数，129

`IS_WARNING_FATAL` 常量，51

`JOIN` 子句，123

`LoadEncryptedDataAndDecrypt` 方法，372
- `LoadPlainDataAndEncrypt` 方法，372

`load_amazon_products_list.php`，565

`load_cart_details.php`，290

`load_cart_summary.php`，288

`load_categories_list.php`，150

`load_checkout_info.php`，457，495，496

`load_department.php`，144，147

`load_departments_list.php`，96，99，154，567

`load_product.php`，164，286

`load_products_list.php`，157，160，184，195

`load_reviews.php`，543

`load_search_box.php`，182，183

`localhost` 邮件账户，51

`GetCartId`，283



`GetCartProducts`, 283

测试`Authorize.net`，530

`GetCategories`, 254

`x.apr`

测试`DataCash`与以下内容的集成：

`GetCategoriesForProduct`, 254

`HatShop`, 523

`GetCategoriesInDepartment`, 137

`ess.com/`

`MailAdmin`方法，467, 469

`GetCategoryDetails`, 137, 148

`MailCustomer`方法，473

`GetCurrentPipelineSection`, 487, 493

`MailSupplier`方法，473, 481

`GetCurrentPipelineSection`, 466

可维护性

`GetCustomerAddressAsString`, 473

注释代码，27

`GetCustomersList`, 435

数据库函数，78

`GetDataWithRest`, 560, 562

命名和编码标准，27

`GetDataWithSoap`, 560, 562

`MakeXmlFulfill`方法，513, 518

`GetDepartmentDetails`, 136, 148

`MakeXmlPre`方法，512, 517

`GetDepartments`, 87, 95

多对多关系，110, 111, 112

`GetDepartmentsWithDescriptions`, 222

工作原理，120

`GetHandler`, 88

`mcrypt`库，361

`GetMailBody`, 479, 481, 484, 486

在 Unix/Linux 上安装 PHP 5，580

`GetOne`, 87, 91

在 Windows 上安装 PHP 5，574

`GetOrderAsString`, 473

`md5`函数，283

`GetOrderShortDetails`, 434

方法签名

`GetProductDetails`, 142

接口和方法签名，467

`GetProductInfo`, 254

方法

`GetProductReviews`, 540

命名约定，27

`GetProducts`, 559, 562, 568

私有方法，139

`GetProductsInCategory`, 139

静态方法，93

`GetProductsOnCatalog`, 141

方法列表

`GetProductsOnDepartment`, 141
*另请参阅* 函数列表
`AddDepartment`, 222
`GetRequest`, 513, 518
`GetResponse`, 514, 518, 529–531
[www.it-ebooks.info](http://www.it-ebooks.info/)
648XIndex.qxd 11/22/06 2:46 PM Page 606
**606**
■索引
`GetRow`, 87, 91, 94
NO ACTION 关键字
`GetShippingInfo`, 448
一对多关系的工作原理，115
`Handler`, 44, 45, 50
噪声词 *请参阅* 停用词
`Hash`, 357
NOT NULL 列，68
`HowManyPages`, 137
NOW 函数，PostgreSQL，273
`init`, AdminOrders, 438
NULL 值
`init`, CheckoutInfo, 449
NOT NULL 列，68
`init`, ProductsList, 161, 185, 195
主键，65
`LoadEncryptedDataAndDecrypt`, 372
SET NULL 关键字，115
`LoadPlainDataAndEncrypt`, 372
NULLIF 函数，PostgreSQL，275
`MailAdmin`, 467, 469
数值数据类型，67
`MailCustomer`, 473
`MailSupplier`, 473, 481
■**O**
`MakeXmlFulfill`, 513, 518
`ob_start`函数，210
`MakeXmlPre`, 512, 517
OfferSummary 响应组，554
`MoveProductToCategory`, 254
OFFSET 关键字，SQL，125
`OpenPayPalWindow`, 194–195
一对多关系，109, 114
`Prepare`, 86, 87, 89
一对一关系，109
`Process`, 465, 467, 478, 479, 482, 498
打开到 PostgreSQL 服务器的连接，
`RemoveProductFromCategory`, 254
82, 83
`Search`, 179, 181, 187
`OpenPayPalWindow`函数，JavaScript，
`SetCartId`, 282, 283
194–195
`SetDateShipped`, 472
OpenSSL
`SetHandler`, 50
在 Unix/Linux 上安装启用了 SSL 的 Apache，
`SetImage`, 254
577
`SetOrderAuthCodeAndReference`, 471
Opera
`SetProductDisplayOption`, 254
在 Opera 中打开安全网页，204
`SetRequest`, 529, 530
OR (|) 布尔运算符
`SetThumbnail`, 254
使用搜索向量进行搜索，173
`UpdateDepartment`, 222
订单管理，314–315, 429
`UpdateOrder`, 434–435
业务层修改，434–435
`UpdateOrderStatus`, 470
数据库修改，429–434
`mhash`库
显示待处理订单详情，325–334
在 Unix/Linux 上安装 PHP 5，580
显示待处理订单，316–324
在 Windows 上安装 PHP 5，574
实现税费和运费，443–452
中间层 *请参阅* 业务层
表示层修改，435–439,
建模工具
设置订单管理页面，
使用数据库图，121
“module access_module ...”错误，578
运输问题，442
`MoveProductToCategory`方法，254
税务问题，441, 442
`move_uploaded_file`函数，PHP，253
ORDER BY 子句，SQL，76
■**N**
COUNT 聚合函数，340
n 层架构
rank 函数，175
为何不使用 n 层架构，19
显示查询结果分页，125
名称字段
订单完成错误邮件
类别表，113
测试订单管道，495
产品表，118
订单详情
命名约定
显示，325
HatShop 网站，27
实现业务层，327–334
Smarty 插件文件，96
实现数据层，325–327
命名标准，27–28
订单管道
导航逻辑
实现管道段，477–486
目录管理，202
`PsCheckFunds`类，479–480
网络

```markdown
# 索引

## 数字和符号

`orders_get_orders_by_customer_id` 函数

`orders_get_orders_by_status` 函数，431

`orders_get_order_details` 函数，325

`orders_get_order_info` 函数，325–326，431，446

`orders_get_order_short_details` 函数，432

`orders_get_shipping_info` 函数，447

`orders_set_auth_code` 函数，471

`orders_set_date_shipped` 函数，472

`orders_update_order` 函数，325，327
- 创建，326
- 创建新函数，433
- 删除，429
- 更新 `Orders Admin` 页面，498

`orders_update_status` 函数，470

`order_detail` 表，305，306，308

`order_done` 模板，497

`order_error` 模板，497

`order_info` 类型，446

`order_processor.php`，460

`ORDER_PROCESSOR_EMAIL` 值，463

输出缓冲
- 实现 `admin.php` 的骨架，210

重载
- 数据库函数，79
- 事务与重载，453

## A

## B

## C

## D

## E

## F

## G

网关提供商，505

## H

## I

## J

## K

## L

## M

## N

## O

订单放置系统
- 实现，303–314
  - 业务层，312
  - 数据层，308–311
  - 表示层，312–314
- 将订单存储在数据库中，305–308

订单处理管道
- 描述，453
- 设计，454–458
- 实现，458–469
- 订单管道功能，453
- 管道任务，454

`OrderProcessor` 类
- 设计订单管道，455，457
- 实现订单管道，458，460

订单
- 构建电子商务网站时的考虑，6
- 电子商务网站降低成本，5
- 订单处理注意事项，8

`ORDERS ADMIN` 链接，314，316

订单管理页面，314
- 设置，316
- 更新 `Orders Admin` 页面，498–503
  - 实现业务层，500
  - 实现数据层，498–499
  - 实现表示层，500–503

`Orders` 类
- `GetByCustomerId` 方法，434
- `GetOrderShortDetails` 方法，434
- `GetShippingInfo` 方法，448
- `UpdateOrder` 方法，434，435

`orders` 表
- 创建，305，306，308
- 设计订单管道，455

`orders_create_audit` 函数，458，459

`orders_get_audit_trail` 函数，498，499

`orders_get_orders_between_dates` 函数，431

## P

`Page` 类
- 构建第一个 `HatShop` 页面，40
- `Smarty` 模板引擎工作原理，42

`page.php` 文件
- 构建第一个 `HatShop` 页面，39

页面
- 显示首页内容，162–163
- `GetProductsInCategory` 方法，140
- `HowManyPages` 方法，137
- 链接到安全/非安全页面，103–106

`page_link` 会话变量
- 创建 `products_list` 模板，159，161

分页，125–126，129
- `products_list` 模板，153
- `PRODUCTS_PER_PAGE` 常量，135

参数，按引用传递，140

帕斯卡命名法，27

密码哈希，207，356

`PasswordHasher` 类，357，359

支付网关
- 信用卡支付网关，506–507

支付服务提供商
- 互联网支付服务提供商网站，190

支付
- 设计订单管道，455

PayPal
- 按钮生成器，192
- 创建 PayPal 账户，191
- 电子商务网站注意事项，8
- 入门指南，190–191
- 链接和资源，191
- 单件商品购买功能，197–198
- 创建购物车，8

`PayPal` 开发者网络，191

PayPal 购物车
- 分阶段开发的注意事项，8
- 实现自定义购物车，267
- 集成购物车和结账，192–197

`PayPalDev` 论坛，191

`PDO`（PHP 数据对象）扩展

`PsCheckStock` 类，480–481

`PsFinalNotification` 类，485

`PsInitialNotification` 类，478–479

`PsShipGoods` 类，483–484

`PsShipOk` 类，484

`PsStockOk` 类，482

`PsTakePayment` 类，482

## Q

## R

检索客户地址，473
检索订单详情，473

## S

向客户和供应商发送电子邮件，473
设置信用卡认证详情，471–472
设置订单发货日期，472
测试管道，487–495
更新结账页面，495–498
更新订单状态，470

## T

## U

## V

## W

## X

## Y

## Z
```


