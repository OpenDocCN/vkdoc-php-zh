# content/[node:title]

190



`content_migrate_batch_process_create_fields()` 方法，902

`content_migrate_batch_process_migrate_data()` 方法，902

`content-migrate-field-structure`，902

`context` 项目，621

Drush 的上下文，608–609

内容类型的上下文相关新增链接，767–771
- 寻找更好的方式，770–771
- 模型，767–770

`contextual` 命令，603

上下文过滤器，62，82–83

上下文链接模块，91

可访问的对比度，944

`contrib` 目录，94

贡献（Contributed）模块术语，501

贡献模块，890–891
- 尾声，792
- 依赖关系，592–593
- 存储位置，94
- 更新，145–147
  - 自动化模块安装程序，146
  - 使用 Drush 工具，146–147

贡献项目，处理其中的问题，135

为社区做贡献，866–881
- 回答问题，873
- 通过犯错来贡献，871
- 处理问题队列，875–877
- 主办会议，879–880
- 其重要性，867–868
- 让 Drupal.org 变得更好，878–879
- 指导他人，871–872
- 资金支持，880–881
- 非技术性支持，869
- 补丁，874–875
- 审核补丁，878
- 编写文档，873–874

贡献冲刺活动，738

贡献者，847–851
- 成为贡献者的好处，847–848
  - 良好的社区声望，848
  - 提升自身工作，847–848
  - 知名度，848
  - 参与对话，848
- 商业模式，849–851
  - 说服客户认识贡献的价值，849
  - 开发安装配置文件，850
  - “开发+”模式，850
  - 直接资助，850–851
- 对其的期望，851
- 可持续性，848–849

控制面板链接，988

控制语句，405

控制结构，402–403
- `if`、`elseif` 和 `else` 语句，402–403
- 循环，403
- `switch` 和 `case` 语句，403

对话的重要性，218

Cooperperkins.com，934

复制代码，540–541

文案写作
- 无益文案的原因，744–745
- 概述，743–744

核心（core）

- Drupal
  - 处理其中的问题，135
  - 加载其代码，953–957
- 文件
  - CSS，342
  - 在 Mac OSX 安装中下载，979–984
  - 其中的 jQuery UI，366–379
    - 特效，378–379
    - 元素，367–378
- 核心模块，91–94
- 模板
  - 常用模板，295
  - 其中的渲染数组，326
- 主题目录，269–274
  - Bartik 主题，270
  - Garland 主题，271
  - Seven 主题，272
  - Stark 主题，272–273
  - 主题引擎，273–274

`core` 属性，807

`core` `debug()` 方法，752

`core` 指令，389

核心 Drupal Commerce 实体，584

核心 Drupal Commerce 字段，584

核心方法，Drupal，943

核心模块术语，501

`core` 属性，279

`core-cli` 命令，602

`core-rsync` 命令，612，614

`core-status` 命令，612

`count()` 方法，420–421

`countQuery()` 方法，445

`<create a new book>` 标签，175

创建内容菜单，492

Navicat 中的“创建新数据库”选项，980

登录凭证，237

米哈里·契克森米哈赖（Mihaly Csikszentmihalyi），264

CSS（级联样式表）文件，341–348
- 添加、移除和替换，343–348
- 聚合与压缩，341
- 利用默认类，353–354
- 模式与命名约定，342–343
  - 双向文本支持，343
  - 核心与模块 CSS 文件，342
- 渲染后的 CSS，231–232
- 使用 CSS 简化表单元素，766
- 有条件地包含管理页面的样式表，788–790
- 使用 CSS 进行样式设计，440–442

CSS 类，63

CTools 工具集，257–258

`ctools` 文件夹，51

自定义显示设置，749

自定义模块术语，501

自定义模块，501–502

客户模块，577

客户/客户界面，568

`cvs.drupal.org`，389

Cyberduck 软件，235



### D

仪表盘，自定义，225

仪表盘链接，Drupal 站点，224

仪表盘主区域，287

仪表盘模块，91

仪表盘侧边栏区域，287

#### 数据

去中心化数据空间，654

用于添加的钩子，708–709

遗留数据，908–909

全球范围内的关联，655

数据迁移，907–911

- 审计，910
- 初始分析，909–910
- 迭代，910
- 启动，910–911
- 遗留数据，908–909
- 管理，907–908
- 显示，910
- 难点，909
- 时机，910

在用户界面中存储和编辑，548–549

#### 数据模型

定义，547–555

概述，452

`data.agaric.com/edit-term-love` 编辑术语模块，869

数据库抽象层，初始化，引导阶段，689

`Database` API，442–456

`.install` 文件，451–452

数据模型，452

在两个位置显示相同数据，447–449

动态查询，450–451

获取数据
- 使用 `SELECT` 查询，443–445
- 使用连接两个表的静态查询，445–447

插入和更新数据，455–456

表，453–454

使用 `variable_get()` 方法和另一个静态选择计数与分组查询，450

数据库备份工具，45

数据库日志模块，92

数据库覆盖代码，56

#### 数据库

639–645

`CAP` 在 `ACID` 与 `BASE` 之间，643–645

创建
- 命令行步骤，980–981
- 用于 Drupal 站点，974
- Mac OSX 操作系统安装，979–980

导出，244

在数据库中查找系统名称，428–430

索引，640–642

MongoDB，645–650

- MongoDB 中的空值，649–650
- 看门狗子系统、会话子系统和消息队列，648–649

SQL 中的 `NULL` 值，642–643

设置
- 使用 `phpMyAdmin` 工具，239–240
- 使用向导和手动工具，240

慢查询，920–921

`date_validate()` 方法，543

`datepicker` 库，368



`db_delete()` 方法，536，898，900–901

`db_drop_table()` 方法，899–900

`db_insert()` 方法，455，536

`db_merge()` 方法，455

`db_or()` 方法，901

`db_query()` 方法，443–446，450–451，457，689

`db_query_range()` 方法，797–798

`db_select()` 方法，445，450–451

`db_update()` 方法，455，536

`dblog_overview()` 方法，456

`--db-su` 选项，613

`--db-su-pw` 选项，613

截止日期，212

Debian 包管理器，250

`debug()` 方法，311，410，416，456，481，504，543，748，752，796

`--debug` 标志，615

`'debug'` 日志级别，625

`--debug` 选项，625，632

`debug($items)` 方法，761

`debug($node)` 方法，796

`debug(debug_backtrace())` 方法，475

`debug(field_info_bundles())` 方法，460

`debug(list_themes())` 方法，416

去中心化数据空间，654

默认缓存表，使用缓存数据，765–766

默认键，246

默认订单状态与状态值，585

默认区域，284–286

默认状态，813

默认视图模式，165

`default_message` 数组，927

`.default_views.inc` 文件，812

`default.settings.php` 文件，141

`definitivedrupal.mayfirst.org` 域名，248

DefinitiveDrupal.org，747，771–772，776

`definitivedrupal.org/patch`，875

`definitivedrupal.org/suggestions`，869

DefinitiveGuide.org 账户，155

降级处理，JavaScript 语言与 jQuery 库，365–366

交付回调函数，928–929

演示模块，254

依赖关系

关于贡献模块，592–593

设为可选，538–539

`dependencies` 属性，807

依赖项项目，360

`dependencies[]` 指令，389–391

部署模块，251，259

部署与上线阶段，9

网站部署，251–261

方法，252

在代码中捕获所有变更，256–260



代码变更，[254][1]–[255][1]

内容，[253][2]–[254][2]

开发工作流，[260][3]–[261][3]

工作流，[252][4]–[253][4]

部署/发布，Drupal 项目阶段，[208][5]

描述，项目，[742][6]–[743][6]

描述属性，[807][7]

描述属性，[279][8]

设计，Drupal 项目阶段，[208][5]

设计方案，[723][9]。*另请参阅* 视觉设计

Drupal Commerce 项目，[587][10]–[588][10]

在资源有限的情况下，[722][9]

用于详细设计的线框图，[733][9]

流程

做出明智决策，[721][9]

概述，[720][9]–[721][9]

项目规划，[206][5]–[207][5]

压力测试，[730][9]–[733][9]

空屏状态，[731][9]

错误屏状态，[733][9]

满屏状态，[732][9]

正常屏状态，[731][9]

开发者日，Drupal，[880][11]

Dev Desktop 应用程序，Acquia。*参见* Acquia Dev Desktop 应用程序

Dev Desktop 控制面板，[987][12]

`devel` 命令文件，[607][13]

`Devel dpm()` 方法，[752][14]

Devel 模块，使用其追踪错误，[474][15]–[476][15]

《为 Drupal 开发》手册，[134][16]

开发者简介，[804][17]–[805][17]

开发者，[835][18]–[843][18]

作为开发者的成功之道，[841][18]–[843][18]

预算时间，[842][18]

创建流程，[841][18]–[842][18]

使用发行版，[843][18]

聘请经验丰富的开发者，[843][18]

使用安装配置文件，[843][18]

常见陷阱，[835][18]–[836][18]

咨询专家，[838][18]

内容需要管理，[837][18]

与社区贡献，[840][18]

使用 Drupal 的长期收益，[838][18]

长期支持，[841][18]

做出可持续的选择，[840][18]

赞助，[881][11]

从目标出发，[838][18]–[839][18]

支持不同的参与者，[839][18]–[840][18]

理解 Drupal 站点的概念，[836][18]–[837][18]

开发

在此期间禁用缓存，[637][19]–[638][19]

从人的思维模式出发，[263][20]–[265][20]

备份，[263][20]–[264][20]

贡献，[265][20]

试验，[264][20]–[265][20]

版本控制，[263][20]

会议，[215][5]

项目规划，[207][5]

开发，Drupal 项目阶段，[208][5]

开发环境，[227][21]–[241][21]

适用于初学者，[233][21]–[241][21]

选择托管服务，[234][21]–[238][21]


```markdown
服务端任务和工具，239–241

解压归档文件，238–239

增强现有功能，229–233

访问命令行，230–231

浏览器和设备兼容性测试，232–233

本地托管站点，230

PHP 文件，233

渲染后的 HTML，231–232

Microsoft Windows。*参见* Microsoft Windows 开发环境

从 Quickstart 设备开始，228–229

Visual Studio，948

开发加模式，贡献者的商业模式，850

开发配置文件，804–805

开发标准，Drupal Commerce 项目，588

开发团队文档，225–226

开发工具，将安装配置文件和功能作为开发工具使用，815

开发工作流程（网站），260–261

设备与浏览器兼容性测试，232–233

DGB（Drush Git 备份），249

DGD7 站点代码，43

`dgd7_field_formatter_view()` 方法，761

`dgd7_tip` 目录，775

`dgd7_tip_add_rm_formset()` 方法，778–779，790

`dgd7_tip_filter_info()` 方法，776

`dgd7_tip_settings()` 方法，776

`dgd7_tips_settings()` 方法，782

`dgd7.aliases.drushrc.php` 文件，610

`dgd9781430231356.css` 文件，794

`dgd7glue` 文件夹，749

`dgd7glue_drupal_page_title()` 方法，763

`dgd7glue_field_formatter_settings_form()` 方法，761

`dgd7glue_field_formatter_settings_summary()` 方法，761

`dgd7glue_field_formatter_view()` 方法，761

`dgd7glue_nextprev_suggestion()` 方法，797–798，800

`dgd7glue_node_view()` 方法，795

`dgd7glue_preprocess_field()` 方法，754

`dgd7glue.info` 文件，749

`dgd7glue.module`，798

对话，参与社区，848

对话框库，369–370

`diff` 命令，140，498

`diff` 方法，通过其更新，143–144

`diff` 工具，141

通过 `diff`，138

直接反馈，264

直接资助，贡献者的商业模式，850–851

指令（Directives）

配置，390

`dependencies[]`，389–390

概述，389

包，391

目录，设置权限，245

`<Directory>` 标签，956

禁用视图，57

`disabled = status`，444

发现阶段

会议，214

项目规划的阶段，205–206
```



发现，Drupal 项目阶段，208

显示状态，62

`displays`。*另请参阅* Views 内容

- 微调，164–167
- 相关模块，103–104
- 创建额外的，74
- 定义内容元素，72
- 定义内容顺序，72
- 定义内容类型，71
- 字段的，使用章节内容类型设置，173–175
- 使用多个来突出显示第一个结果，77–78
- 名称，60
- 资源内容类型，管理，181
- 设置
  - 覆盖，63
  - Views 的设置，61
- 用于唯一显示的标签页，78–79
- 摘要，修改，166–167
- 类型，58–59

可处理属性，724

不同手势属性，724

`distributions`，803–818

- 配置功能，810–815
- `drune_track.*.inc` 文件，812
- `drune_track.module` 文件，812–815
- 使用安装配置文件与功能作为开发工具，815
- Drune 示例，805
- 未来展望，817–818
- 打包代码，816–817
  - 托管在 `drupal.org`，817
  - 生成文件，816–817
  - 打包，817
- 站点模板，803–805
  - 开发配置文件，804–805
  - 示例发行版，805
  - 全功能服务，804
- 结构，806–809
  - `drune.info` 文件，807
  - `drune.profile` 文件，807–809
- 以及作为开发者的成功，843

`<div class="content">` 包装器，328

`<div class="inner">` 标签，352–353

`<div class="user-picture">` 标签，293

`<div id="header">` 标签，292

`<div id="page-wrapper">` 标签，292

div 标签，75

`<div>` 包装器，315

DNS（域名系统）设置，247

`docs/drush.api.php` 文件，613

`documentation`，221–226，741–743

- 面向社区，226
- 面向开发团队，225–226
- 面向最终用户（发布后），222–223
- 模块内的站点文档，482–483
- 优秀文档的标准，221–225
- 编写文档，873–874

文档贡献，265

为代码编写文档，513

DocumentRoot，956

文档，面向客户，209

Dojo Barn Raisings，872


道场，Drupal，880

`DOM` 遍历技术，657

域名，247

域名系统 (DNS) 设置，247

向 Drupal 协会捐赠，881

`Downloads` 部分，89

`dpm()` 方法，308，311，317，505，795

可拖拽库，370

`Dreditor`，使用它审查补丁，878

`Dropbox`，978

可放置库，371

`Drubuntu` 脚本，使用它自定义用于开发的 Ubuntu 操作系统，972

`drubuntu-site-add`，894，973

`drune_track` 特性，809

`drune_track.*.inc` 文件，812

`drune_track.info` 文件，812

`drune_track.module` 文件，812–815

异常，814–815

覆盖，813

更新，813

`drune.info` 文件，806–807

`drune.install` 文件，814

`drune.profile` 文件，806–809

批处理任务，809

表单任务，808–809

Drupal

7 代码库，893–896

编码标准，404–406

核心，处理其中的问题，135

与 HTML5，940

在 Ubuntu 操作系统上安装，973–975

自动安装程序，974–975

数据库，974

聚会，824

发展历程，821–833

起源，822–824

Drupal 基金会，828

服务器被黑事件，825–827

从 6 升级到 7。*参见* 升级

Drupal 7 平台，589–593 中的改进

贡献模块的依赖关系，592–593

核心实体和字段，589–591

表单 API，591–592

Drupal 管理菜单，984

Drupal 协会，向其捐赠，881

Drupal 咖啡馆，880

Drupal 营地，879

Drupal Commerce 项目，565–593

开发历史，586–588

设计理念，587–588

开发标准，588

实现，585–586

Drupal 7 中的改进，589–593

贡献模块的依赖关系，592–593

核心实体和字段，589–591

表单 API，591–592

关键特性，565–566

概述，565

商店配置，566–585


购物车模块，583

结账系统，581–583

商务模块，569

客户模块，577

动态定价，570

启用支付方式，579–581

订单项，573–575

订单系统，577–578

支付模块，578

价格模块，569–570

产品展示节点类型，575–577

产品模块，571–573

产品引用模块，575

主要组件概要，584–585

Drupal Commons，804

Drupal 上下文，608

Drupal 核心代码加载，953–957

Drupal 开发者日，880

Drupal 道场，880

Drupal 活动，832

Drupal 安装界面，981

Drupal 编码马拉松，880

Drupal 学习项目，872

Drupal 聚会，880

Drupal 开放课程项目，872

Drupal 派对，880

Drupal Planet 聚合器，196

Drupal 项目页面，10

Drupal 项目，832，853–854

Drupal 安全报告，134

Drupal 峰会，879

`drupal_add_css()` 方法，343–344，361，788–789

`drupal_add_js()` 方法，356–359，361–362

`drupal_add_library()` 方法，360–362，366–367

`drupal_add_library('system', 'ui.dialog');`，369–371

`drupal_add_library('system', 'ui.progressbar');`，372

`drupal_add_library('system', 'ui.resizable');`，373

`drupal_add_library('system', 'ui.selectable');`，374

`drupal_add_library('system', 'ui.slider');`，375

`drupal_add_library('system', 'ui.sortable');`，376

`drupal_add_library('system', 'ui.tabs');`，377

`drupal_alter()` 方法，538

`drupal_bootstrap()` 方法，407，685–686

`drupal_deliver_html_page()` 方法，676，692，697，928–929

`drupal_deliver_page()` 方法，692，697

`drupal_flush_all_caches()` 方法，407，436

`drupal_form_submit()` 方法，258–259

`drupal_get_form()` 方法，464–465

`drupal_get_normal_path()` 方法，692

`drupal_get_path()` 方法，356，770

`Drupal_http_request()` 方法，710



`drupal_is_front_page()` 方法，296

`drupal_load()` 方法，918

`drupal_parse_info_file()` 方法，919

`drupal_placeholder()` 方法，476

`drupal_process_attached()` 方法，932

`drupal_render()` 方法，692、697、923、930、932

`drupal_render_cache_by_query()` 方法，931

`drupal_render_children()` 方法，334、782

`drupal_render_children($form)`，337

`drupal_render_page()` 方法，929

`drupal_required_modules()` 方法，919

`drupal_set_message()` 方法，451、495

`drupal_static()` 方法，907、909

`drupal_system_listing()` 方法，919

`drupal_uninstall_schema()` 方法，899

`drupal_uninstall_modules()` 方法，902

`drupal_write_record()` 方法，536

`Drupal.attachBehaviors()` 方法，364–365

`Drupal.behavior` 方法，364、368、371–377

`Drupal.behaviors` 对象，364

`DrupalCacheInterface`，688

`DrupalCamp` 活动，198

`DrupalCon` 会议，198

`drupalcontrib.org`，394

`drupalCreateUser()` 方法，530

`DrupalDefaultEntityController` 类，553、694

`DrupalDojo.com`，869、872

`drupalGet('path')` 方法，530

`drupalLogin($user)`，530

`DrupalOpenLearning.org`，872

`Drupal.org` 账户，设置以参与贡献，854–855

`Drupal.org` 上的分支和标签，861

`Drupal.org` 向其中贡献补丁文件，498–499

`Drupal.org` 改进，878–879

`Drupal.org` 使用字段格式化器链接到，757–766

`Drupal.org` 使用默认缓存表缓存数据，765–766

`Drupal.org` 获取用户名，763–764

`Drupal.org` 上的沙箱，在其中共享代码，541

`Drupal.org` 在上面分享模块，791–792

`Drupal.org` 向其中提交补丁，531–532

`drupal.org`，托管于，234、817

`drupal.org` 文档，869

`Drupal.org` 论坛，197、873

`drupal.org` 项目页面，228

`Drupal.org` 用户 ID，154

`drupal.org` 用户名，870

`drupal.org` 网络论坛，196

`drupal.org/coding-standards`，478

`drupal.org/community-spotlight`，869

`drupal.org/contribute/documentation`，874

`drupal.org/contribute/documentation/join`，874

`drupal.org/home`，140

`drupal.org` 托管的模块，488

`drupal.org/irc/guidelines`，881

`drupal.org/localize`，11



- `drupal.org/node/add/book`，874
- `drupal.org/node/add/project-project`，541、856
- `drupal.org/patch`，91
- `drupal.org/patch/apply`，875
- `drupal.org/patch/create`，875
- `drupal.org/patch/novice`，874
- `drupal.org/planet`，871
- `drupal.org/project/advanced_help`，183
- `drupal.org/project/api`，394
- `drupal.org/project/backupmigrate` 备份与迁移模块，45
- `drupal.org/project/bbcode`，772
- `drupal.org/project/browscap`，232
- `drupal.org/project/captcha_pack`，99
- `drupal.org/project/coder`，406、478、862
- `drupal.org/project/content_type_overview`，100、179
- `drupal.org/project/corolla`，13
- `drupal.org/project/custom_formatters`，154
- `drupal.org/project/customfilter`，772
- `drupal.org/project/devel`，474
- `drupal.org/project/dreditor`，878
- `drupal.org/project/drubuntu`，972
- `drupal.org/project/drupalforfirebug`，232
- `drupal.org/project/drupalorg_testing`，879
- `drupal.org/project/enabled_modules`，898
- `drupal.org/project/entity`，550
- `drupal.org/project/examples`，773
- `drupal.org/project/field_group`，96
- `drupal.org/project/field_permissions`，187
- `drupal.org/project/flexifilter`，772
- `drupal.org/project/grammar_parser`，406
- `drupal.org/project/image_resize_filter`，781
- `drupal.org/project/imagefield_crop`，159
- `drupal.org/project/insert`，186
- `drupal.org/project/issues`，868
- `drupal.org/project/issues/drupal`，877
- `drupal.org/project/issues/gci`，872
- `drupal.org/project/issues/projectapplications`，863
- `drupal.org/project/issues/remarkup`，794
- `drupal.org/project/link`，96、151
- `drupal.org/project/markdown`，772
- `drupal.org/project/media`，97
- `drupal.org/project/modules`，88、90、94
- `drupal.org/project/multicolumncheckboxesradios`，766
- `drupal.org/project/openid_provider`，15
- `drupal.org/project/oracle`，442
- `drupal.org/project/pathauto`，190、801
- `drupal.org/project/pathologic`，187
- `drupal.org/project/project_shortname`，94
- `drupal.org/project/quickstart`，228、972
- `drupal.org/project/references`，96
- `drupal.org/project/relation`，96、179、182
- `drupal.org/project/remarkup`，792
- `drupal.org/project/reptag`，772
- `drupal.org/project/simplehtmldom`，772
- `drupal.org/project/sitedoc`，889
- `drupal.org/project/skinr`，939
- `drupal.org/project/sky`，939
- `drupal.org/project/smartcrop`，159



`drupal.org/project/sqlsrv`, 442

`drupal.org/project/strongarm`, 905

`drupal.org/project/textile`, 772

`drupal.org/project/themes`, 13

`drupal.org/project/token`, 190, 853

`drupal.org/project/typogrify`, 772

`drupal.org/projectvctools`, 95

`drupal.org/project/views`, 89

`drupal.org/project/votingapi`, 537

`drupal.org/project/wysiwyg_linebreaks`, 97

`drupal.org/project/xray`, 769

`drupal.org/requirements`, 234

`drupal.org`的版本控制, 492

`drupal.org/user/register`, 874

`drupal.orgvnode/1002658`, 146

`drupal.orgvproject/hacked`, 143

`drupal.orgvproject/migrate`, 889

`drupal.orgvproject/xray`, 902

`drupal.orgvsecurity-team`, 137

`drupal.orgvuser/26979`, 876

`drupal.org/writing-secure-code`, 863

`drupalPost()` 方法, 527–528

Drupal 疗法, 226

Drush, 31–46, 595–633

`alias` 文件 (`aliases.drushrc.php`), 600–601

升级中涉及所有站点的别名, 892–893

使用 Drush 应用代码更新, 603–606

与手动升级方法结合使用, 893–896

命令特定选项, 609

上下文, 608–609

数据库备份工具, 45

Drush Shell（Drush 外壳）, 601–603

安装 Drush, 32–38

创建别名, 35–37

下载 Drush 工具, 34

Drush 扩展, 606–607

使 Drush 工具可执行, 35

测试 Drush, 37–38

使用 Drush 编写脚本, 617–626

脚本参数, 618

`drush_dispatch` 命令, 619

`drush_html_to_text` 命令, 623

`drush_invoke` 命令, 619

`drush_invoke_process` 命令, 620

`drush_invoke_process_args` 命令, 620

`drush_invoke_sitealias` 命令, 620

`drush_invoke_sitealias_args` 命令, 620

`drush_log` 命令, 625–626

`drush_op_system` 命令, 619

`drush_print` 命令, 622

`drush_print_pipe` 命令, 622

`drush_print_table` 命令, 622–623

`drush_set_error` 命令, 626

`drush_shell_exec` 命令, 619

`dt` 命令, 622

脚本选项, 618

处理命令结果, 620–621

提示用户, 623–624

站点列表, 609–610



站点选址，参考 597–600

使用以下方式更新：

贡献的模块，146–147

概述，142

使用远程命令进行操作，610–617

创建远程 Drupal 站点的本地副本，612–614

管理转储文件，614–615

设置 SSH 密钥对，611–612

使用 Drush 站点上下文控制 `sql-sync` 选项，616–617

在远程系统上不安装 Drush 的情况下使用 `sql-sync`，615–616

为其编写扩展，626–633

使用扩展更改 Drush 命令行为，631–633

为其添加命令钩子，627–628

为其添加帮助钩子，630–631

实现命令方法，628

通过回调手动指定命令，629

将命令实现放在单独的文件中，629

返回数组以将数据传递给其他 Drush 脚本，629

`drush @dev core-status` 命令，609

`drush cache-clear all` 命令，598，613

`drush cc all` 命令，46，407，798

`drush cc` 命令，494

`drush` 命令，35

`drush commandname` 命令，33

`drush convert-makefile` 命令，817

`drush core-cli` 命令，601

`drush core-rsync` 命令，612

`drush dl menu_block; drush en -y menu_block`，176

`drush dl MODULE_NAME`，31

`drush dl modulename`，38

`drush docs-commands` 命令，628

`drush en featurename`，906

`drush en views_ui`，904

`drush features-export` 命令，905

`drush features-revert` 命令，813

`drush features-update` 命令，813

`.drush` 文件夹，607

Drush Git 备份 (DGB)，249

`drush help --filter` 命令，630

Drush 帮助屏幕，968

`drush make` 命令，816–817

`drush mmas` 命令，628

Drush 程序，965–969

安装，965–968

运行，968–969

Drush 项目页面，34

`drush site-install` 命令，540，806

`drush site-upgrade` 命令，888，893，895，897，901，904

`drush sql-dump > /path/to/filename.sql` 命令，45

`drush sql-query 'show tables;' | grep cache` 命令，613

`drush sql-sync @dev @live` 命令，617

`drush sql-sync` 命令，612，894

`drush topic docs-api` 命令，633

`drush up` 命令，31–32，38，147

`drush upc ctools views`，147

`drush verify-makefile` 命令，817

`drush_bootstrap_max` 方法，631

`drush_choice` 方法，624

`drush_command_invoke_all` 方法，632

`drush_command_invoke_all_ref` 方法，632

`drush_COMMANDFILE_HOOK` 方法，631

`drush_COMMANDFILE_HOOK_validate` 方法，631

`drush_COMMANDFILE_post_HOOK` 方法，631

`drush_COMMANDFILE_pre_HOOK` 方法，631

`drush_confirm` 方法，624

`drush_devel_download` 方法，632

`drush_dispatch` 命令，619

`drush_dispatch` 方法，629

`drush_docs_readme` 方法，629

`drush_extras` 模块，611

`drush_get_merged_options` 方法，621

`drush_HOOK_init` 方法，631

`drush_html_to_text` 命令，623

`drush_invoke` 命令，619

`drush_invoke_process` 命令，620

`drush_invoke_process_args` 命令，620

`drush_invoke_sitealias` 命令，620

`drush_invoke_sitealias_args` 命令，620

`drush_key_value_to_array_table` 方法，623

`drush_log` 命令，625–626

`drush_log` 方法，624–626

`drush_op_system` 命令，619

`drush_print` 命令，622

`drush_print` 方法，624

`drush_print_file` 方法，629

`drush_print_pipe` 命令，622

`drush_print_pipe` 方法，629

`drush_print_table` 命令，622–623

`drush_prompt` 方法，624

`drush_redispatch_get_options` 方法，625

`drush_set_error` 命令，626

`drush_set_error` 方法，624–625，631–632

`drush_shell_exec` 命令，619

已接入 drush 的模块，386

`drush/examples/helloworld.script` 文件，618，622

`drush/examples/sandwich.drush.inc` 文件，626

`drush.php` 文件，608

`drushrc.php` 文件，600，606–607，613，617

`dsm()` 方法，311

`dt` 命令，622

都柏林核心词汇表，662

转储文件，管理，614–615

`dur` 选项，648

动态表单，通过 `#ajax` 属性，591–592

动态页面，使用 Views 模块构建。*参见* Views 模块

动态定价，570

动态查询，450–451



### E

`EcoAlign` 站点，937

`Edit` 视图，57

有效沟通，218

jQuery UI 的效果，378–379

元素字段名，26

`element_validate` 属性，544

元素

接受多个元素的表单，776–779

jQuery UI

手风琴库，367

日期选择器库，368

对话框库，369–370

拖拽库，370

放置库，371

进度条库，372–373

可调整大小库，373

可选择库，374

滑块库，375

可排序库，376

标签页库，377–378

渲染

操作其输出，322–326

概述，322

`else` 语句，402–403

`elseif` 语句，402–403

`<em>` 标签，413

空屏幕状态，731

启用并设置默认链接，281

已启用的过滤器，707

最终用户文档，222–223

`engine` 属性，279

引擎，主题，273–274

实体，460–461

与字段，589–591

订单实体，590–591

产品实体，589–590

为实体提供管理界面，555–559

提供新实体类型，549–555

定义实体访问回调方法，554

定义新权限，554–555

创建实体的原因，550

实体类，553

实体键，553

`ENTITY_build_content()` 方法，694

`entity_load()` 方法，559

`entity_metadata_comment_access()` 方法，554

`entity_prepare_view()` 方法，694

`ENTITY_view_multiple()` 方法，694

`EntityAPIController` 类，553

`entityFieldQuery()` 方法，549

环境指示器模块，99，252

环境变量对话框，967

错误代码，621

错误日志级别，625

错误消息，472–476

致命错误

运行时错误，473

语法错误，472

查找错误原因，473–474

搜索解决方案，472

使用 Devel 模块追踪，474–476

错误屏幕状态，733

`error_log` 项，621

`ERROR_REPORTING_DISPLAY_ALL`，408

`error_status` 项，621

错误

用于处理错误搜索和 404 模块，106–107

显示错误，408

`etc/backup.d` 文件，250

活动赞助，881

`every_page` 标志，358

一切在代码中的方法，251–252

一切通过 Web 的理念，252

示例组件，813

示例模态对话框，60

`example_something()` 方法，469

`example_update_7001()` 方法，451

`example.aliases.drushrc.php` 文件，600

`example.com/blog/blog-post-title`，96

`example.drushrc.php` 文件，608，613

`exampleform_unsubscribe()` 表单，331

`example.js` 文件，357

示例目录，608

示例文件夹，618

`examples/examples.drushrc.php` 文件，622

`example/web` 目录，140

异常，814–815

排除，707–708

挂钩到现有模块，504

对贡献者的期望，851

专家

咨询专家，838

与开发者成功的关系，843

探索性可用性测试，736–737

环境，737

方法，736

场景，736

设置模块、录制和日志记录，738

任务，736–737

导出视图，58

可导出的，811

将视图导出为代码，85–86

暴露的过滤器，使用其克隆并创建管理表，80–81

区块中的暴露表单，62

暴露表单样式，62

暴露表单，62

Drush 扩展，626–633

使用扩展更改 Drush 命令行为，631–633

扩展的命令钩子，627–628

扩展的帮助钩子，630–631

实现命令方法，628

安装扩展，606–607

使用回调手动指定命令，629

将命令实现放在单独文件中，629

返回数组以将数据传递给其他 Drush 脚本，629

`extract()` 方法，471



### F

-   刻面块, 703
-   致命错误
    -   运行时, 473
    -   语法, 472
-   伪可导出项, 811
-   功能，定义, 214
-   `features` 目录, 906
-   功能模块, 905–907
    -   添加到自动升级, 906–907
    -   基础, 905
-   功能项目, 256–258
-   `features` 属性, 279
-   功能套件, 252
-   `features_revert()` 方法, 258
-   `.features.content.inc` 文件, 812
-   反馈
    -   直接且即时, 264
    -   专家，关于网站可访问性, 946
    -   来自问题队列, 741
    -   关于线框图, 729–730
-   Feeds 模块, 259, 805
-   字段显示, 754–757
-   字段格式化程序，链接到 Drupal.org/Twitter 账户, 757–766
    -   使用默认缓存表缓存数据, 765–766
    -   获取用户名, 763–764
-   字段 SQL 存储，字段 UI 模块, 92
-   `field_add_more_js()` 方法, 777
-   `field_attach_form()` 方法, 557
-   `field_attach_load()` 方法, 694
-   `field_create_field()` 方法, 560
-   `field_get_items()` 方法, 509
-   `field_info_bundles()` 方法, 460–461
-   `field_language()` 方法, 509
-   `field_status`, 187
-   字段
    -   使用元数据向 Chapter 内容类型添加, 172–173
    -   向字段模块添加字段类型, 96–97
    -   编辑, 74–75
    -   实体与字段, 589–591
    -   Order 实体, 590–591
    -   Product 实体, 589–590
    -   以编程方式创建和附加, 559–560
    -   使用 Chapter 内容类型设置显示, 173–175
    -   视图的字段, 61
-   字段 API, 509–510
-   字段模块，向其中添加字段类型, 96–97
-   字段升级，自动化, 902–903
-   `field.tpl.php` 文件, 293, 295
-   `'file' => filename.extension`, 424
-   文件目录，设置权限, 245
-   文件放置, 9–11
-   文件传输协议客户端。*参见* FTP 客户端
-   `file xray.admin.inc`, 465
-   `file_get_contents()` 方法, 710
-   `file_scan_directory()` 方法, 919
-   `file.js`, 365
-   文件
    -   添加到仓库, 859–861
    -   自动包含, 592
    -   通用文件，允许用户附加到内容, 180
    -   Photoshop 文件, 206
    -   上传，通过 FTP 客户端, 237



`files` 属性，807

`files[]` = `mymodule.test`，519

FILO（先进后出）顺序，308

`--filter` 标志，607

`filter` 模块，PHP，128

### 过滤器设置

#### 表单
- 接受额外的两行，777–779
- 提供相关说明，781–782
- 验证，780–781

`filter_example.module`，773，775–776

`filter_xss()` 方法，478

`--filter=devel` 标志，607

`filtered_html_plus`，185

### 过滤器
- 上下文相关的，62，82–83
- 条件组，65–66
- 视图的过滤器，61

#### 自定义文本，771–795
- 为输出添加自定义标记，795
- 具有指定类的容器表单元素，790–791
- 自定义钩子，783
- 编辑文本格式，794
- 示例，773
- 过滤内容，783–788
- 模块，771–773，776–788，791–794
- 管理页面的样式表，788–790
- 标签和替换标记表单，776–782

- 已启用的，707
- 已暴露的，使用其克隆并制作管理表格，80–81
- 未更改的，163

`Filters` 部分，64–65

`find` 命令，646

`查找重复问题`，875

`查找维护者方向`，875

`查找相关问题`，876

### 微调内容显示，164–167
- 修改摘要显示并设置截断长度，166–167
- 使用视图模式以不同方式显示相同内容，166

`Firebug 网络摘要`，914

`FireFTP`，236

先进后出（FILO）顺序，308

路径的适用性，679–680

`屏幕淹没`状态，732

流状态，264

FOAF（朋友的朋友）词汇表，662

字体，为视觉设计选择，939

`foo_load()` 方法，677

页脚，61，357

`foreach()` 语句，763

`foreach` 循环，419，421

`表单 API`，134

`表单组件`类型，504

#### 表单元素
- 容器，具有指定类的，790–791
- 调查，543–544

表单任务，808–809

`form` 变量，547

`form_alter()` 方法，412

`form_error()` 方法，544

`form_get_error()` 方法，547



`form_id` 表单，332

`form_process_password_confirm()`，542

`form_set_error()` 方法，544

`form_set_value()` 方法，779

`form_state` 变量，547

`<form>` ID 属性，333

## 格式设置

配置选项，67–68

关于视图，72

`format_plural()` 方法，结合 `t()` 方法的本地化，412–414

格式，文本，794

格式化器，字段，757–766

## 格式化

允许图片，添加到文本，184–187

以表格形式显示数据，433–435

视图，60–61

`formmsgs` 目录，540

`formmsgs` 实体，553

`formmsgs_form()` 方法，557

`formmsgs_form_submit()` 方法，557

`formmsgs_load()` 方法，557

`formmsgs_load_by_name()` 方法，557

`formmsgs_load_multiple()` 方法，557

`formmsgs.admin.inc` 文件，556

## 表单

接受多个元素，776–779

修改，409–412

API 元素和默认属性，331

动态表单，通过 `#ajax` 属性，591–592

## 筛选器设置

接受额外两行，777–779

提供相关说明，781–782

## 操作元素

在模板文件中，337–338

在主题方法中，335

Simpletest 框架与，528–530

## 使用 CSS 精简元素，766

## 主题化，329–340

查找表单 ID，332–333

`hook_theme()` 方法，333–334

标记生成，329–332

使用修改钩子进行修改，339–340

使用模板文件，336–339

使用主题方法，334–335

## 表单 API，591–592

自动文件包含，592

通过 `#ajax` 属性实现动态表单，591–592

## 论坛模块，93

## FOSDEM（自由及开源软件开发者欧洲会议），824

## FOSS（自由及开源软件），822

## 自由及开源软件开发者欧洲会议（FOSDEM），824

## 自由及开源软件（FOSS），822

## 朋友的朋友（FOAF）词汇，662

## 首页视图，57

`<front>` 文本，22

## FTP（文件传输协议）客户端

设置为使用文本编辑器，238

设置，235–238

通过服务器访问设置创建书签，235–236

输入登录凭据，237

选择安全协议，236–237

启动连接并上传文件，237

文本编辑器，237–238

## 函数签名，`hook_help()` 方法的，480

`function_exists($function_name)`，670

`function_name()` 方法，404

## 功能需求

开发与实施，207

IA 阶段，206

## 功能测试，使用 Simpletest 框架，517–532

`.test` 文件，522–526

优点与注意事项，518

API，530–531

描述，519–520

表单，528–530

延伸阅读，530–531

运行测试，520–522，526–527

设置测试，520–522

向 Drupal.org 提交补丁，531–532

TDD，519

何时使用，518

## 功能

通过模块扩展，15–16

禁用不需要的，15–16

OpenID 模块，15

修改现有功能，504

需要特定功能的页面或内容部分，259–260

## 函数

直接调用，438–439

确定所需函数，414–421

## 主题函数，301–304

调用，302

钩子建议与，308–310

覆盖，302–304



### G

`garfieldtech.com/blog/language-tradeoffs`，393

`Garland` 主题，271，277

`garland_width` 设置，277

`garland.info` 文件，277

格式塔心理学，716–717

`get_defined_vars()` 方法，311

`getfirebug.com`，753

`git add -A` 命令，255

`git add` 命令，42，776

`git bisect`，263

`git checkout mymodule.info`，45

`git checkout -- path/to/filename.php` 命令，45

`git clone --bare` 命令，254

`git commit -a -m` 命令，263

`git commit` 命令，43

`git diff` 命令，249

`.git` 目录，254

Git 主页，40

`git init` 命令，42

`git log` 命令，45

`git pull` 命令，139

`git reset` 命令，255

`git reset --hard HEAD` 命令，44

`git revert HEAD` 命令，45

`git status` 命令，42，45，255

`git status` 类型，139

Git 系统，263

Git 工具，31–38，45–46，858–861

- 将文件添加到仓库，859–861
- 数据库备份工具，45
- `git checkout mymodule.info` 命令，45
- `git log` 命令，45
- `git status` 命令，45
- 安装，39–41
- SSH 协议，858–859
- 使用，41–45
- 创建仓库，42–44
- 识别用户，41–42
- 在 Git 工具中回滚，44–45

Git 版本控制系统，254–255

`git.drupal.org`，389，792，862

`gitignore` 文件，894

`gitorious.org/drupal-open-curriculum/pages/Home`，872

全局重定向模块，107

全局设置页面，275–276

全局主题设置，275–277

- 自定义，277
- 徽标设置，276
- 主菜单和副菜单设置，277
- 名称和标语设置，276
- 快捷图标设置，277
- 在文章和评论中显示用户图片设置，277
- 评论中显示用户验证状态选项，277

全局变量，296–301

词汇表视图，57

粘合代码模块，来自的自定义升级方法，896–897

GNU 通用公共许可证，822

目标

- 设定，264
- 从...入手，838–839

GoMockingbird，206

网格选项，68

`GROUP BY`，908

群组内容类型，111–114

群组规则，358

群组相关角色，123

群组

- 创建，114
- 在 `drupal_add_js()` 方法中，357–359

群组登录页面，116

`Groups.Drupal.com/seattle`，872

`groups.drupal.org` 个人资料页面，763

`Groups.Drupal.org` 站点，197

`Groups.Drupal.org` 用户 ID，154

`groups.drupal.org/contributed-module-ideas`，91

`groups.drupal.org/drupalchix`，882

`groups.drupal.org/events`，875

`groups.drupal.org/html5`，940

`groups.drupal.org/peer-review`，479–480

GUI 工具，264

`mysql` 命令的 `-h` 参数，246



### H

`<h2>`标签，319

可入侵的 URL，允许用户页面的视图，801–802

攻击核心，277

服务器入侵，825–827

硬编码变量，289

模板文件中的硬编码变量，288

`<head>`标签，295，345

标头

概述，61

设置页面标头，引导阶段，690

“标头已发送”错误，303

头像图片

为作者构建头像，158–162

作者简介视图页面，162–164

图片样式，159–161

页面视图的菜单链接，161–162

概述，151

`helloworld.drush` 文件，618

`help` 命令，598，603

Drush 扩展的 `help` 钩子，630–631

帮助区域，285

帮助-显示帮助文本模块，92

理查德·海耶斯，622

隐藏区域，286–287

`<Hidden>` 标签，165

`hide()` 方法，319，327–329

高亮区域，323

多显示结果中高亮第一个结果，77–78

主页上下文，608

项目主页，130–131

`homepage.css` 样式表，347

钩子建议

概述，305

以及模板文件，305–308

以及主题方法，308–310

`hook_anything_whatsoever()` 方法，393

`hook_apachesolr_field_mappings_alter(&$mappings)` 方法，709

`hook_apachesolr_modify_query()` 方法，709

`hook_apachesolr_modify_query($query, $caller)` 方法，709

`hook_apachesolr_node_exclude($node, $namespace)` 方法，709

`hook_apachesolr_prepare_query($query, $caller)` 方法，709

`hook_apachesolr_search_result_alter($doc, $extra)` 方法，710

`hook_apachesolr_update_index()` 方法，709

`hook_apachesolr_update_index($document, $entity, $namespace)` 方法，708

`hook_apachesolr_update_index($document, $node, $namespace)` 方法，709

`hook_block()` 方法，260

`hook_block_view()` 方法，395

`hook_boot()` 方法，687–690

`hook_comment_presave()` 方法，503

`hook_comment_view()` 方法，393

`hook_commerce_currency_info_alter()` 方法，569

`hook_commerce_customer_profile_info()` 方法，577

`hook_commerce_line_item_info()` 方法，568，574

`hook_commerce_line_item_info_alter()` 方法，574

`hook_commerce_payment_info()` 方法，579

`hook_commerce_product_info()` 方法，590

`hook_commerce_tax_info()` 方法，574

`hook_contact_site_form()` 方法，334


`hook_cron()` 方法，685

`hook_css_alter()` 方法，343、345–348、916

`hook_custom_theme()` 方法，692

`hook_db_rewrite_sql()` 方法，63

`hook_default_formmsgs()` 方法，538

`hook_default_views()` 方法，538

`hook_entity_info()` 方法，460、550–551、554、556、571

`hook_entity_info_alter()` 方法，590–591、748、750

`hook_entity_load()` 方法，694

`hook_entity_prepare_view()` 方法，694

`hook_entity_view()` 方法，695

`hook_entity_view_alter()` 方法，695

`hook_exit()` 方法，687–689

`hook_field_attach_load()` 方法，694

`hook_field_formatter_*()` 方法，758

`hook_field_formatter_info()` 方法，757–758

`hook_field_formatter_view()` 方法，761–762

`hook_field_storage_pre_load()` 方法，694

`hook_filter_info()` 方法，774–776

`hook_form_alter()` 方法，190、339–340、392、409、440、505、542、545、923

`hook_form_FORM_ID_alter()` 方法，340、409

`hook_forms()` 方法，464

`hook_help()` 方法，394–396、416、422、440、463、480、482、539

`hook_init()` 方法，692

`hook_install()` 方法，258、451、508、560、808、814、899、903、907

`hook_install_tasks()` 方法，807–808

`hook_js_alter()` 方法，362–363、916

`hook_language_init()` 函数，689、691

`hook_language_negotiation_info()` 方法，691

`hook_language_negotiation_info_alter()` 方法，691

`hook_library()` 方法，359–362、545

`hook_library_alter()` 方法，363

`hook_load()` 方法，694

`hook_menu()` 方法——使用该方法创建页面，421–426

为管理页面选择路径，422

菜单项，422–426

`hook_menu` 实现，675、682


`hook_menu_alter()` 方法，362，424，427，681，705

`hook_menu_alter` 数组，679–682

`hook_menu_item_alter()` 方法，928–929

`hook_module_implements_alter(&$implementations, $hook)` 方法，509

`hook_node_info()` 方法，513

`hook_node_insert()` 方法，772

`hook_node_load()` 方法，694

`hook_node_update()` 方法，772

`hook_node_update_index($node)`，709

`hook_node_view()` 方法，513，772，795，798

`hook_node_view($node, $view_mode, $langcode)` 方法，709

`hook_node_view_alter()` 方法，695，804

`hook_nodeapi` 方法，496

`hook_page_alter()` 方法，325–326，437，511，697，929–932

`hook_page_build()` 方法，260，697，929–930

`hook_permission()` 方法，426，555

`hook_permissions()` 方法，432，555

`hook_preprocess_comment()` 方法，754

`hook_preprocess_HOOK()` 方法，754

`hook_preprocess_node()` 方法，748–749，754

`hook_preprocess_page()` 方法，362，754

`hook_process_HOOK()` 方法，754

`hook_rdf_mapping()` 方法，661

`hook_requirements()` 方法，452

`hook_schema()` 方法，258，451，453–454，551，553

`hook_search_execute()` 方法，704–705

`hook_search_info()` 方法，704–705

`hook_search_page()` 方法，705–706

`hook_stream_wrapper_alter()` 方法，691

`hook_stream_wrappers()` 方法，691

`hook_system_info_alter()` 方法，286–287

`hook_theme()` 方法，301–302，316，333–334，407，435–436，439，476，483

`hook_theme_registry_alter()` 方法，799

`hook_update_dependencies()` 方法，452

`hook_update_N()` 方法，258，451–452，454，560，814，897，899，903，906

`hook_url_inbound_alter()` 方法，692

`hook_views_default()` 方法，538

`hook_views_default_view()` 方法，513

`hook_watchdog()` 方法，689

钩入模块，537–538

钩子，304–310，393–397，503，783

添加数据，708–709

修改，使用钩子修改表单，339–340

修改查询和结果，709–710

调用所有实现，432–433

描述，304

执行顺序，508–509

预处理和处理，315–317

搜索模块

定义新的搜索，704

可选，704–706

更新，258–259

捕捉额外的升级步骤，896–904

`hookvnode_view()` 方法，695

主机服务器，通过 SSH，243

托管

会议，879–880

服务，234–238

FTP 客户端设置，235–238

根据性能和服务选择，234–235

站点，本地，230

主机名，247

`.htaccess` 文件，10，138–139，141，245，248，962

HTML（超文本标记语言）

属性，297–301

渲染后的，231–232

语义标记，656

HTML 列表选项，68

html 主题，927

`html_tag`，324

HTML5 和 Drupal，940

`htmlspecialchars()` 方法，472–475

`html.tpl.php` 文件，293，295

`html.tpl.php` 模板，288

HTTP（超文本传输协议）加速器，Varnish，638

HTTP 端口，952

`httpd.conf` 文件，955

`http://drupal.org` 项目页面，486

`http://purl.org/dc/terms/` 命名空间，653

Hudson 工具，260

混合方法，253

超文本标记语言。*参见* HTML

超文本预处理器。*参见* PHP

超文本传输协议（HTTP）加速器，Varnish，638



