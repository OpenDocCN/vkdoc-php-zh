# 排版后的 Markdown 文档

## Q

- `query()` 方法

## R

- `rand()` 函数
- `random_image.php` 和 `index.php`
- `range()` 函数
- `readfile()` 函数
- 读写文件
  - 追加模式
  - CSV 文件（*参见* 逗号分隔值 (CSV) 文件）
  - `fclose()` 函数
  - `flock()` 函数
  - `fopen()` 函数
  - `fseek()` 函数
  - `ftruncate()` 函数
  - 函数
  - 内部指针
  - 模式
  - 覆盖现有文件
  - 文本文件
- `array_slice()` 函数
- `echo` 命令
- `explode()` 函数
- `file()` 函数
- `file_get_contents()` 函数
- 文件系统
- `nl2br()` 函数
- 参照完整性
- `blog_insert_mysqli.php`
- 类型转换运算符
- `$catError`
- 条件语句
- `$_FILES`
- 外键
- `getFilenames()` 方法
- `getMessages()` 方法
- `$_POST` 数组
- `upload()` 方法
- 类别与图像
- `blog_insert_mysqli.php`
- 复选框
- 条件语句
- `<form>` 标签
- 多选 `<select>` 列表
- `toggle_fields.js`
- 交叉引用表
- 类别
- 复合主键
- 文件上传
- 外键约束（*参见* 外键约束）
- InnoDB 存储引擎
- `INSERT` 查询
- PDO 版本
- 主键
- 远程文件访问
- RSS 源
- `<script>` 标签/超链接
- SimpleXML（*参见* SimpleXML）
- `strip_tags()` 函数
- `return` 关键字
- 富文本格式 (RTF)
- `rowCount()` 方法
- RSS 源

## S

- `sayHi()` 函数
- `scandir()` 函数
- 安全套接层 (SSL) 连接
- 自处理表单
- 服务器端包含 (SSI)
- `session_name()`
- 会话
  - 创建
  - 销毁
  - 基于文件的认证
  - `fgetcsv()` 函数
  - `header()` 函数
  - 登录页面
  - 创建注销按钮
  - 限制访问
  - 头部已发送
  - `ini_set()` 函数
  - 多页表单
  - 密码安全
    - `check()` 方法
    - `CheckPassword` 类
    - `DOCTYPE` 声明
    - 加密
    - `$errors` 数组
    - `filesize()` 函数
    - `fopen()` 模式
    - `getErrors()` 方法
    - `password_hash()` 函数
    - `password_verify()` 函数
    - `preg_match()` 方法
    - `requireMixedCase()` 方法
    - `trim()` 方法
    - 用户注册系统
  - `PHPSESSID`
  - 重新生成会话 ID
  - 限制访问
  - `DOCTYPE` 声明
  - `ob_start()`
  - `session_destroy()`
  - `setcookie()`
  - 存储的变量
  - `time()` 函数
  - `unset()` 变量
- 会话
- `setTimezone()` 方法
- `SHOWMAX`
- SimpleXML
  - `foreach` 循环
  - `<item>` 元素
  - RSS 新闻源
    - BBC World News
    - `DateTime` 类的 `format()` 方法
    - `foreach` 循环
    - `LimitIterator` 类
    - `newsfeed.css`
    - `setTimezone()` 方法
  - `sort()`
  - Splat 运算符
- `SplFileInfo` 类的 `getRealPath()` 方法
- SQL 命令
  - `DELETE`
  - `INSERT`
  - `SELECT`
  - `UPDATE`
- SQL 注入
  - 定义
  - 插入
  - 条件语句
  - `</form>` 标签
  - `<option>` 标签
  - MySQLi 版本
  - PDO 版本
  - `rowCount()` 方法
  - 预处理语句
  - 策略
- SQL 查询
  - 不区分大小写
  - 数字
  - `SELECT` 关键字
  - `LIKE` 关键字
  - `ORDER BY` 子句
  - 选择特定列
  - `WHERE` 子句
  - 字符串
  - 空白
- 标准 PHP 库 (SPL)
  - 同时参见 *另请参阅*
  - `FilesystemIterator` 类
- `strcasecmp()` 函数
- `stripos()`
- `str_replace()` 函数
- `strtolower()` 函数
- `switch()` 函数

## T

- 文本提取
  - `<br/>` 标签方法
  - 条件语句
  - `getFirst()` 函数
  - HTML 记录存储
  - `implode()` 函数
  - `LEFT()` 方法
  - MySQL
    - `DATE_FORMAT()` 方法
  - 段落提取
  - PHP `substr()` 方法
  - `preg_split()` 函数
  - `<p>` 标签方法
  - `strrpos()` 和 `substr()` 方法
- 缩略图图像
  - `basename()` 函数
  - `<body>` 标签
  - `checkType()` 方法
  - 类
    - 另请参见（*另请参阅* 类）
  - `createThumbnail()` 方法
  - 目标文件夹
  - 尺寸计算
  - `imagecopyresampled()`
  - Mac OS X
  - MIME 类型
    - `checkType()` 方法
    - `DOCTYPE` 声明
    - `getimagesize()`
  - 图像选择
  - `$maxSize` 属性
  - `PhpSolutions` 文件夹
  - `test()` 方法
  - 服务器能力
  - 创建 Setter 方法
    - `abs()` 函数
    - 脱字符 (`^`)
    - `DIRECTORY_SEPARATOR` 常量
    - 图像选择
    - `is_numeric()` 函数
    - `preg_match()`
    - `setMaxSize()` 方法
    - `strpos()` 函数
    - `substr()` 函数
    - `test()` 方法
  - `Thumbnail.php` 编码
  - 使用 GD 函数
- 时间限制会话
- `<title>` 标签
- `trim()` 函数
- 双向加密
  - 优点与缺点
  - `AES_DECRYPT()` 函数
  - `AES_ENCRYPT()` 函数
  - 用法
  - 用户身份验证（使用）
    - MySQLi
    - PDO
  - `users_2way` 表
- 类型声明
- 类型转换
- 排版约定

## U

- `ucfirst()` 函数
- `ucwords()` 函数
- `UNIQUE` 索引
- `UPDATE` 查询
- URL 编码

## V

- 验证用户输入，*参见* 处理与验证用户输入
- `valid()` 方法
- `var_dump()` 函数

## W, X, Y, Z

- Web 页面，PHP
  - 优点
  - 维护
  - 图形化表示
- 万维网联盟 (W3C) 标记验证服务

```
// 注：原始内容末尾的多个空的反引号代码块已被清理，此处仅保留一个空代码块作为示意。实际应删除。
```
