# getFromMap() 方法

`getHeaderText()` 方法

`getInstance()` 方法

`getKey()` 方法

`getPlayLength()` 方法

`getPrice()` 方法

`getProducer()` 方法

`getProduct()` 函数

`getRow()` 方法

`getStatus()` 方法

`getTaxRate()` 方法

`getUser()` 方法

`getVenueMapper()` 方法

`getWealthFactor()` 方法

### Git 服务器

- 添加目录
- 克隆
- `command/FeedbackCommand.php` 文件
- 文档
- `execute()` 方法
- FeedbackCommand
- `git branch` 命令
- GitHub 标签
- `git push` 命令
- 安装
- megaquiz 项目
- 拉取请求
- `quizobjects/User.php` 文件对比
- 远程仓库
- 本地用户
- Shell 访问
- SSH 访问
- 删除目录
- 版本控制

## globalKey() 方法

### H

`handleLogin()` 方法

`handleRequest()` 方法

### I

### 标识映射

- `addToMap()` 方法
- 效果
- `exists()` 方法
- `getFromMap()` 方法
- `globalKey()` 方法
- 集成
- 对象信息
- `ObjectWatcher` 类
- `targetClass()` 方法
- 测试代码

### 继承

- 类切换（多态）
- 问题
- 书籍和 CD
- 第一种方法
- `getProducer()` 方法
- `getSummaryLine()` 方法
- `$numPages` 和 `$playLength` 属性
- `ShopProduct` 类
- `ShopProductWriter` 类
- public、private 和 protected 访问权限
- 访问器方法
- 元素
- `getPrice()` 方法
- `ShopProduct` 类
- `private $price` 属性
- 工作原理
- 构造函数
- `getPlayLength()` 和 `getNumberOfPages()` 方法
- `getSummaryLine()` 方法
- 被覆写的方法
- `$title` 属性

## init() 方法

`insert()` 方法

`instance()` 方法

`interpret()` 方法

`InterpreterContext` 类

`InterpreterContext` 对象

### 解释器模式

- `add()` 方法
- `BooleanAndExpression` 类
- 布尔逻辑
- `BooleanOrExpression` 类
- `BooleanOrExpression` 对象
- `CharacterParse` 类，代码清单
- 类图
- `CollectionParse` 类，代码清单
- `compile()` 方法
- 复杂性问题
- 组合解析器
- 组合模式
- `Context` 类，代码清单
- 缺点
- `discard()` 方法
- `dointerpret()` 方法
- `doScan()` 方法
- 领域特定语言
- `eatWhiteSpace()` 方法
- 扩展巴科斯范式
- `EqualsExpression` 类
- `EqualsExpression` 对象
- 等号运算符
- `evaluate()` 方法
- 表达式实现
- `expression()` 方法
- `getKey()` 方法
- `getState()` 方法
- 语法描述
- `handleMatch()` 方法
- 接口设计
- `interpret()` 方法
- `InterpreterContext` 类
- `InterpreterContext` 对象
- 调用内部机制
- `invokeHandler()` 方法
- `isSpaceChar()` 方法
- `isWordChar()` 方法
- `LiteralExpression` 对象
- `lookup()` 方法
- MarkLogic 语法
- MarkLogic 解释器
- `MarkParse` 类，代码清单
- Metsker, Steven
- 迷你语言
- `next()` 方法
- `nextToken()` 方法
- `OperatorExpression` 类
- `Parser` 类，代码清单
- 解析器类图示
- 解析语句
- `push()` 方法
- `Reader` 接口
- `replace()` 方法
- `scan()` 方法
- `Scanner` 类，代码清单
- `ScannerState` 类
- 安全问题
- `setHandler()` 方法
- `setState()` 方法
- `StringReader` 类
- 终结符解析器
- `term()` 方法
- 标记
- 解析器对象树
- `trigger()` 方法
- `VariableExpression` 对象
- `VariableExpression` 类
- `VariableHandler` 类
- `variable()` 方法

## is_array() 函数

`isNull()` 方法

### J, K

`joinExisting()` 方法

### L

### 后期静态绑定

`LibXmlError` 类

## LiteralExpression 类

`LiteralExpression` 对象

`LoginAnalytics` 类

`lookup()` 方法

### M

`mail()` 函数

Mailman

`Mapper::createObject()` 方法

`mark()` 方法

匹配器方法

`MatchMarker`

## Megaquiz 项目

`method()` 方法

`ModuleRunner::handleMethod()` 方法

`ModuleRunner` 的 `init()` 方法

`movementRange()` 方法

`myMethodName()` 函数

### N

`NastyBoss::addEmployee()` 方法

`newUpdate()` 方法

`Notifier::getNotifier()` 方法

`notifyAccess()` 方法

`notifyPasswordFailure()` 方法

### 空对象模式

- `isNull()` 方法
- `NullUnit` 类
- `TileForces` 构造函数
- `UnitAcquisition` 类

### O

### 对象生成

- 抽象工厂模式
- BloggsCal
- `BloggsCommsManager` 类
- `CommsManager` 类
- 具体创建者和产品
- 效果
- 工厂方法
- MegaCal
- 产品族

- `AppConfig` 类
- `AppointmentMaker`
- 装配器类
- `BloggsApptEncoder` 对象
- 委托对象实例化
- `Employee` 类
- 工厂方法模式
- 抽象类 `ApptEncoder`
- 抽象类 `CommsManager`
- 抽象创建者
- `ApptEncoder` 类
- `BloggsApptEncoder` 类
- `BloggsCal`
- `BloggsCommsManager` 类
- `CommsManager` 类
- 具体创建者
- 效果
- 泛化
- `getFooterText()` 方法
- MegaCal 类
- 产品类
- 术语

- `getInstance()` 方法
- 硬编码配置
- `init()` 方法
- `NastyBoss` 类
- 多态
- 协议类型



### 原型对象

### 原型模式

### 优势

### 文明风格网页游戏

### 类图

### `clone()` 方法

### `$navigability` 属性

### 并行继承层次结构

### `TerrainFactory` 对象

### `TerrainFactory` 类

### 单例模式

### 后果

### DSN 字符串

### 全局变量

### 实现

### `$instance` 属性

### `Preferences` 类

### `Preferences` 对象

### 私有构造函数

### 静态方法

### 设计良好的系统

### 静态方法

### 字符串与实例化

### `TerrainFactory` 类

### 面向对象设计

### 美学的考量

### 自动构建

### 代码重复

### 持续集成（CI）

### 解耦的定义

### 封装

### 全局变量

### 元系统

### 多个项目

### phpDocumentor

### 多态

### 公共接口

### 促进可复用性

### 测试框架的优势

### 紧耦合的原因

### 面向对象系统

### 对象与设计

### 抽象基类

### `addParam()` 方法

### 类

### 代码设计

### 内聚性

### 耦合

### 封装

### 文件扩展名

### `getInstance()` 方法

### 示例代码

### 接口

### 正交性

### 持久化方法

### 多态

### 抽象基类

### 条件语句

### 定义

### 返回类型

### 子类

### 过程式代码

### `readParams` 函数

### 职责

### 路标

### 代码重复

### 条件语句

### 全局变量

### 职责

### 静态方法

### UML

参见统一建模语言（UML）

### `writeParams()` 函数

### XML 模式

### `$objects` 数组

### 对象工具与技术

### 自动加载

### 默认行为

### 函数

### 机制

### `replaceUnderscores()` 函数

### `ShopProduct` 类

### `spl_autoload()` 函数

### `spl_register_function()`

### `spl_unregister_function()`

### 类与对象函数

### `BookProduct`/`CdProduct` 类

### `callable()`

### `class_exists()` 函数

### 完全限定字符串引用

### `get_class()` 函数

### `get_class_methods()`

### `get_declared_classes()` 函数

### `getProduct()` 函数

### 继承

### `method_exists()` 函数

### 方法调用

### 属性

### 文件系统

### 包含路径

### PEAR 方式

### PHP 包与命名空间

### 显式别名

### 反斜杠访问

### 类、函数与变量

### `Debug` 类

### 全局空间

### `helloWorld()`

### `__NAMESPACE__` 常量

### 命名冲突

### 组织/项目标识符

### URL 与文件路径

### 反射 API

### 应用程序

### 类

### 可克隆方法

### 检查方法

### `execute()` 方法

### `handleMethod()`

### 实现

### `init()` 方法循环

### 模块接口

### 模块对象的 setter 方法

### 多维数组

### `$params` 数组参数

### 快速但不优雅的方法

### `ReflectionClass`

### `ReflectionClass` 对象

### `ReflectionFunction` 类

### `ReflectionMethod`

### 静态 `export()` 方法

### `var_dump()` 函数

### 观察者模式

### 抽象超类

### `attach()` 方法

### 类图

### `detach()` 方法

### `getStatus()` 方法

### `handleLogin()` 方法

### 实现

### `Login` 类

### `LoginObserver` 类

### `notify()` 方法

### `Observer` 类接口

### `rand()` 函数

### SPL

### `SplObjectStorage`

### `SplObserver`

### `SplSubject`

### `Update()` 方法

### 用户访问

### `OperatorExpression`

### 正交性

### 重载

### P, Q

### 包

### 构建工具

### 客户端与开发者

### 云产品

### Composer

### 良好的 `mail()` 函数

### Packagist 网站

### PEAR

### Phing 应用程序

### 标准

### 第三方包

### 版本控制系统

### `ParamHandler` 类

### `parse()` 方法

### 模式语言

### `PatternSet`

### 属性

### `exc_test`

### `inc_code`

### `refid` 属性

### PEAR

### Pear 应用程序

### `performOperations()` 方法

### Phing

### Ant

### 定义

### 安装工具

### 包创建

### PHP 应用程序

### PHP 工具

### tar/gzip 压缩包

### 任务

### PHP

### 客户端代码

### Zend Engine 2

### PHP 5

### PHP 代码库

### Apache 模块 ModRewrite

### BinaryCloud

### 设计模式

### Java 语言

### 面向对象设计

### 对象

### 模式

### 实践

### 表示层

### 测试

### XP 原则

### Zend 2 引擎

### `php_codesniffer` 任务

### PHP 扩展与应用库（PEAR）

### PHP 框架互操作小组（PHP-Fig）

### PHP 对象

### 倡导与不可知论

### Andi Gutmans

### 拥抱变化

### 特性

### 继承

### PHP 7

### PHP/FI

### 悄然革命

### Zeev Suraski

### PHP 的核心面向对象特性

### 抽象类

### 匿名类

### 参数与类型

### `AddressManager` 类

### 类型提示

### `outputAddresses()` 方法

### 原始类型与检查函数

### 内置错误类

### 回调、匿名函数与闭包

### 类

### `__clone()`

### 常量属性

### 析构方法

### final 类与方法

### 错误处理

### `Conf` 类

### 错误标志

### 异常

### `simplexml:load_file()`

### 子类化异常

### 抛出异常

### try/catch 子句

### 继承

参见 PHP 的核心继承

### 拦截器方法

### `Address::$streetaddress` 属性

### `__call()` 方法



### composite property  
`get()`方法  
`getName()`方法  
`__isset()`方法  
`$name`属性  
重载  
`Person`类  
`__set()`方法  
`setName()`方法  
`__unset()`方法  
接口  
内部错误类  
后期静态绑定  
抽象类  
`create()`方法  
定义  
`DomainObject`类  
`DomainObject`类  
`getGroup()`方法  
`self`关键字  
`SpreadSheet`类  
静态工厂方法  
方法  
构造方法  
`getProducer()`方法  
方法体  
对象变量  
可见性关键字  
对象  
设置属性  
静态方法和属性  
`__toString()`方法  
trait  
抽象方法  
更改访问权限  
`calculateTax()`方法  
宿主类属性  
`insteadof`运算符  
接口  
覆盖方法  
`PriceUtilities`和`IdentityTrait`  
问题  
`ShopProduct`和`UtilityService`  
静态方法  
PHP 标准  
设计模式  
PSR-1  
类文件  
类命名  
开始和结束标签  
`read_conf()`方法  
副作用  
PSR-2  
调用方法和函数  
类声明  
流程控制关键字  
行和缩进  
多行声明  
命名空间声明  
PHP_CodeSniffer  
属性声明  
单行声明  
PSR-4  
附加/库存目录  
自动加载标准  
`index.php`  
json 文件  
`LibraryCatalogue`类  
`popp\library`命名空间  
`popp\library`类  
`popp\library\inventory\Book`类  
`popp\Services`类  
`require_once()`函数  
PSR  
推荐标准  
PHPUnit  
`PHPUnit_Framework_TestCase`方法  
PHPUnit 测试  
验收测试  
断言方法  
优势  
客户端类  
约束方法  
异常  
功能测试  
内存数据库  
模拟和桩  
`.phar`文件  
重构 Web 应用  
Selenium  
`AddSpace`页面  
`AddVenue`页面  
模板测试类  
浏览器窗口  
组件  
文档  
工厂方法  
php-webdriver  
`RemoteWebDriver`类  
`sendKeys()`方法  
Web 页面  
SUnit 框架  
手动测试  
单元测试  
`UserStore`类  
`ValidatorTest`类  
WOO 系统  
xUnit 家族  
Portland 模式仓库  

### 表示层  
应用控制器  
`AddSpace`命令  
`AddVenue`命令  
`AppController`类  
`CliRequest`  
命令/状态元素  
`Command`类  
`ComponentDescriptor`  
具体命令  
配置指令  
影响  
前端控制器  
`getDescriptor()`方法  
高级流程  
初始化流程  
模式  
`processView()`方法  
`resolveCommand()`方法  
`TemplateViewComponent`  
`TemplateViewDisplay`  
视图元素  
`ViewComponentCompiler`类  

### 前端控制器  
`ApplicationHelper`类  
`CliRequest`类  
`Command`类  
`CommandResolver`  
影响  
`Controller`类  
`execute()`方法  
`handleRequest()`方法  
`HttpRequest`类  
实现  
`init()`方法  
`Request`父类  
`run()`方法  
时序图  

### 页面控制器  
`AddVenueController`类  
基类  
子类  
影响  
`forward()`方法  
新场地  
模式  
`process()`方法  
关系管理  
视图元素  

### 模板视图  
影响  
`render()`方法  
安全问题  
`sponsorList()`方法  
`View Helper`类  
`process()`方法  
`processView()`方法  

### 原型模式  
R  
`rand()`函数  
`$raw`数组  
`recruit()`方法  
`RegexpMarker`  
`registerCallback()`  
`remove()`方法  
`removeUnit()`方法  
`render()`方法  
`replace()`方法  
`resolveCommand()`方法  
`run()`方法  
`runCommand()`方法  
`Runner.php`类  

### S  
`sale()`方法  
`sendKeys()`方法  
服务定位器模式  
`setDepth()`方法  
`setMaximumStart()`方法  
`setUp()`方法  
`setValue()`方法  
`ShopProduct`类  
单例模式  
`SpaceIdentityObject`  
`spl_autoload_register()`函数  
`sponsorList()`方法  
PHP 标准库(SPL)  
策略模式  
定义子类  
标记策略  
两种力量  
提取算法  
提高灵活性  
继承层次  
`mark()`方法  
标记对象  
`MarkLogic`语句  
`MarkParse`对象  
`Question`类  
`AVQuestion`  
编码  
`TextQuestion`  
减少子类化  
`TextQuestion`对象  
SUnit 框架  

### T  
`targetClass()`方法  
`tearDown()`方法  
模板方法  
`TestCase`类  
`testgetUser()`方法  
测试工具  

### U  
统一建模语言(UML)  
类图  
抽象类  
聚合  
关联  
属性  
说明  
组合  
继承和实现  
接口  
操作  
关系  
表示类  
可见性符号  
约束  
定义  
时序图  
统一建模语言(UML)  
`Update()`方法  
`UserStore::addUser()`方法  
`UserStore`类  

### V  
Vagrant  
命令  
挂载  
配置过程  
`chkconfig`  
主机名配置  
MySQL  
`mysqladmin`命令  
`poppch20.conf`文件  
`Vagrantfile`文档  
搜索接口  
`Vagrantfile`文档  
VirtualBox 安装  
虚拟化  
`validateUser()`方法  
`VariableExpression`类  
`VenueMapper::doCreateObject()`方法  

### 版本控制  
优势  
分支  
按日期回滚  
`visitArmy()`方法  

### 访问者模式  
`accept()`方法  
`addUnit()`方法  
`ArmyVisitor`对象  
类图  
`CompositeUnit`类  
劣势  
`getDepth()`方法  
`getReport()`方法  
`getTax()`方法  
`setDepth()`方法  
`TaxCollectionVisitor`对象  
`TextDumpArmyVisitor`对象  
Unit 类  
`visit()`方法  
`visitArmy()`方法  
访问者类  

### W  
`warnAmount()`方法  
wiki  
DokuWiki  
PhpWiki  
`workWithProducts()`方法  
`write()`方法  
`writeHTML()`方法  
`writeXML()`方法  

### X, Y  
xUnit  

### Z  
Zend 引擎
