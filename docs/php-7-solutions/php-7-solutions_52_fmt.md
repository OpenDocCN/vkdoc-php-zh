# 将信息拆解为小块

你可能已经注意到，图 12-3 右侧的表格为每位作者的**名**和**姓**分别设置了单独的列。这是关系型数据库的一条重要原则：*将复杂信息拆解为其组成部分，并分别存储每个部分。*

决定拆分到什么程度并不总是那么容易。除了名和姓之外，你可能还希望为称呼（先生、太太、女士、博士等）以及中间名或首字母设置单独的列。地址最好拆分为街道、城镇、县、州、邮政编码等字段。虽然将信息拆分为小块可能有些繁琐，但你随时可以使用 `SQL` 和/或 `PHP` 将它们重新组合起来。然而，一旦你拥有超过几十条记录，要尝试拆分存储在单个字段中的复杂信息，就会是一项艰巨的任务。