# 索引

`shopping_cart_move_product_to_cart`

`load_product.php`，164，286

`function`，272，277

`load_products_list.php`，157，160，184，195

`shopping_cart_remove_product` 函数，271，273，274

`load_reviews.php`，543

`shopping_cart_save_product_for_later`

`load_search_box.php`，182，183

`function`，272，276，277

命名约定，96

`shopping_cart_update` 函数，271，273

使用方法，96

`SHORT_PRODUCT_DESCRIPTION_LENGTH`

## Smarty 模板引擎

常量注释，43

定义产品列表常量，135，136

显示自定义购物车详情，288

`$smarty.config` 变量，43

`smarty_function_load_departments_list` 函数，96

`smarty_modifier_prepare_link` 函数，103

Smarty 模板：`admin`，200，211；`admin_cart`，300；`admin_categories`，200，202，227–229，244；`admin_departments`，200，216–222；`admin_login`，200，212，214；`admin_menu`，200，213，215；`admin_orders`，314，316，320–324，435；`admin_order_details`，314，325，327–334，436，463，489，500，501，502；`admin_product`，200，246–253；`admin_products`，200，232–234；`amazon_products_list`，564；`cart_details`，290，293，347–350；`cart_summary`，287，289；`categories_list`，149–152；`checkout_info`，414–421；`customer_address`，388，400–406；`customer_credit_card`，388，406–410；`customer_details`，388，394–400；`customer_logged`，388，391–394；`customer_login`，388，388–391；`department`，143–148；`departments_list`，95–103，566；`first_page_contents`，162–163；`header`，38，43；`index`，37，40，42–43，100，183，192，194，287；`order_done`，497；`order_error`，497；`product`，163–166，286，347–350；`products_list`，135，153–161，170，186，192，195，285；`reviews`，541–545；`search_box`，181–183；`search_results`，184–187；`shopping cart`，284

使用 Smarty 将布局与代码分离，21–23

## SimpleXML 扩展

与 DataCash 交换 XML 数据，511

`ExtractXml` 方法，373

`simplexml_load_string` 函数，563

## PayPal 单项目购买功能，197–198，304

转义特殊字符，197

工作原理，41

安装，32–34

选择原因，23

[www.it-ebooks.info](http://www.it-ebooks.info/)

648XIndex.qxd 11/22/06 2:46 PM Page 614

**614**

■ 索引

## 购物车管理页面，299

在其中使用 JavaScript，194

## 螺旋方法

项目管理，586–588

## SQL（结构化查询语言）

*另请参阅* PostgreSQL

SQL 命令，74

与数据库通信，74–81

与 PostgreSQL 通信，26

数据库引擎方言，23

简介，74–78

PostgreSQL 函数与类型，78–81

SQL 命令：`DELETE` 语句，77；执行，86；`INSERT` 语句，76–77；`SELECT` 语句，74–76；`UPDATE` 语句，77

SQL 注入攻击：数据库函数，78；预处理语句的防范，87

SQL 查询：预处理语句，86

## SSL（安全套接层）连接

验证数字签名，203

为 SSL 配置 HatShop，205

强制使用 SSL 连接，206，421–423

其重要性，571

在 Unix/Linux 上安装支持 SSL 的 Apache，577–578；在 Windows 上，571–573

获取 SSL 证书，205

网络安全组织，203

选择性强制使用，206

设置 Apache，203

使用安全连接，203

XAMPP 包，204

## 标准，命名与编码，27–28

语句：非预处理语句，87；预处理语句，86

静态方法，93

## 库存检查电子邮件，490

库存检查：设计订单流水线，454

## 停用词

`catalog_flag_stop_words` 函数，176

`FlagStopWords` 方法，179，181

搜索向量，172

编写数据库搜索代码，178

## 存储客户账户，354

HatShop 客户账户方案，354–356

## 字符串

使用引号，28

## Amazon 订阅 ID，549

`substring` 函数，122

## 来自 Amazon 的 Super Hats，555

## 供应商通知，473

`SUPPLIER_EMAIL` 值，463

## 对称加密，360

## 表示层，15

## 产品目录，61