# 自定义函数的位置

如果自定义函数与调用它的代码位于同一页面，那么函数声明的位置无关紧要，既可以放在使用之前，也可以放在使用之后。不过，最佳实践是将所有函数集中存放，位于页面顶部或底部，这样更易于查找和维护。

如果函数在多个页面中调用，最好将它们存储在外部文件中，并在每个页面中引入该文件。下一章将详细介绍使用`include`和`require`引入外部文件的方法。当函数位于外部文件中时，必须在调用函数之前引入该外部文件。