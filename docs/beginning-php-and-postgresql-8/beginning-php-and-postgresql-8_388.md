# 前缀化

链接到详细视图, 697–699

最左前缀, 752

列表页面编号, 704–706

`preg_grep` 函数, 201

`numberFields` 函数, 695

`preg_match` 函数, 201

`numQueries` 函数, 692, 694

`preg_match_all` 函数, 201

`numRows` 函数, 691

`preg_quote` 函数, 202

面向对象, 692, 693

`preg_replace` 函数, 203

`pageLinks` 函数, 704, 705

`preg_replace_callback` 函数, 203

分页输出, 701–704

`preg_split` 函数, 204

产品表, 689

`prepare` 方法, PDO, 564

查询函数, 691

### 预处理语句

检索行, 694

PDO, 562, 564–566

排序输出, 689, 699–701

`pg_execute` 函数, 686

表格输出, 689, 695–697

`pg_prepare` 函数, 685

## PostgreSQL 数据库集群

`pg_send_execute` 函数, 686

在 Linux 上安装 PostgreSQL, 584

`pg_send_query_params` 函数, 686

## PostgreSQL 库

PostgreSQL, 685–686

安装 PHP, 11

## 表示逻辑

`postgresql.conf` 文件

将业务逻辑与表示逻辑分离, 448

`krb_server_keyfile`, 651

Smarty 模板引擎, 450, 454–464

`listen_address`, 651

模板引擎与表示逻辑, 448

[www.it-ebooks.info](http://www.it-ebooks.info/)

**836**

■索 引

`prev` 数组函数, 114

NuSOAP, 创建方法代理, 495

`PRIMARY KEY` 属性

使用 NuSOAP 的 Proxy 类, 495

PostgreSQL 数据类型, 642

`proxy_host`/`proxy_login`/`proxy_password`/`proxy_port` 参数

主键索引, PostgreSQL, 750

`SoapClient` 构造函数, 503

主键值, 642

`psql`, 611–619

`print` 语句, PHP, 47

选项, 613–614

`print_r` 数组函数, 105

控制命令历史, 619

`printf` 语句, PHP, 49

列出 psql 命令, 613

私有声明

查看所有可用命令, 617

缓存会话页面, 431

## 常见提示符替换序列, 619

私有字段, 139

常用 psql 变量, 615

私有方法, 145

psql 任务, 613–619

权限, PostgreSQL, 575

连接新数据库, 614

`GRANT` 命令, 660

控制命令历史, 619

`REVOKE` 命令, 661

在不离开 psql 的情况下编辑文件, 614

## 过程语言

执行位于特定文件中的命令, 614

安装 PostgreSQL, 588

执行查询, 618

PL/pgSQL 函数, 730–736

登录/登出服务器, 613

PostgreSQL, 736–737

修改 psql 提示符, 618

示例 PL/PHP 函数, 737

将查询输出发送到外部文件, 614

产品表, 689

将配置信息存储在启动文件中, 616

创建产品表, 557

存储 psql 变量和选项, 615–616

PHP 的 PostgreSQL 扩展, 667

查看所有可用命令, 617

## 提示符

查看已设置变量列表, 615

常见提示符替换序列, 619

Tab 补全功能, 614

修改 psql 提示符, 618

`PSQL_EDITOR` 变量, 615

## 属性, 面向对象编程, 140–143

`PTR (指针记录)` 记录类型, DNS, 360

创建自定义 getter 和 setter, 142

公有声明

getter (`__get`) 方法, 142

缓存会话页面, 431

PHP 的局限性, 140

公有字段, 138

`ReflectionProperty` 类, 175

公有方法, 145

setter (`__set`) 方法, 140

## 代理

受保护字段, 139

为 Web 服务生成 C# 代理, 513

受保护方法, 146

NuSOAP 指定 HTTP 代理, 501

`:punct:` 字符类, 195

NuSOAP 代理类, 493

[www.it-ebooks.info](http://www.it-ebooks.info/)

■索 引

**837**

`putenv` 函数

■**R**

`safe_mode_protected_env_vars` 指令, 518

`RAISE` 命令

python 选项

PL/pgSQL 函数, 734

从源码安装 PostgreSQL, 583

随机值, 129

■**Q**

`random_page_cost` 设置, PostgreSQL, 598

q 选项, psql, 613

`range` 数组函数, 108

## 量词

rank 函数

Perl 风格, 198

使用全文索引, PostgreSQL, 758

正则表达式, 193, 194

读已提交事务隔离级别, 766

## 查询, PDO, 562–563

读未提交事务隔离级别, 766

## 查询, PostgreSQL, 671–673

可读文件

数据库类

检查文件是否可读, 241

*参见* PostgreSQL 数据库类



`readdir` 函数，第 251 页

`pg_query` 函数，第 672 页

`readfile` 函数，第 248 页

`pg_send_query` 函数，第 672 页

读取

`PGSQL_EMPTY_QUERY` 值，第 674 页

`ldap_read` 函数，第 405 页

使用 PHP 查询视图，第 716 页

读取目录内容，第 251–252 页

处理视图，第 707 页

读取文件，第 244–249 页

查询，SQLite，第 540–541 页

`readline-devel` 软件包

`sqlite_array_query` 函数，第 543 页

从源代码安装 PostgreSQL，第 583 页

`sqlite_query` 函数，第 540 页

`readlink` 函数，第 235 页

`sqlite_unbuffered_query` 函数，第 541 页

`REAL` 数据类型，PostgreSQL，第 638 页

`query` 函数

特殊值，第 638 页

PostgreSQL 数据库类，第 691 页

简易信息聚合（RSS），第 476–486 页

`query` 方法，PDO，第 563 页

介绍，第 473 页

查询优化

MagpieRSS 库，第 479–486 页

索引，PostgreSQL，第 749 页

Moreover Technologies 公司，第 476 页

问号参数

RSS 聚合器，第 476 页

预处理语句，PDO，第 564 页

RSS 源，第 476 页、第 477 页

`QUOTE` 子句，`COPY` 命令

RSS 语法，第 478 页

向/从表复制数据，第 782 页

SharpReader 界面，第 477 页

引号

`realpath` 函数，第 231 页

`magic_quotes_gpc` 指令，第 35 页

`recent` 属性，消息，第 380 页、第 383 页

`magic_quotes_runtime` 指令，第 35 页

最近查看的文档索引示例

`magic_quotes_sybase` 指令，第 35 页

会话处理，第 439 页、第 440 页

单引号和双引号的使用，第 34 页

`RECORD` 类型变量声明

PL/pgSQL 函数，第 731 页

[www.it-ebooks.info](http://www.it-ebooks.info/)

**838**

■ 索引

恢复，PostgreSQL，第 605–609 页

正则表达式，第 192–205 页

递归函数，第 63 页、第 97 页

正则表达式的替代方案

Red Hat 操作系统

函数，第 214–222 页

启动和停止 PostgreSQL 服务器，第 596 页

Perl 正则表达式语法，第 198–205 页

函数，第 200–205 页

引用赋值

元字符，第 199–200 页

变量声明，PHP，第 59 页

修饰符，第 199 页

`REFERENCES` 属性

PHP 正则表达式函数，POSIX，

PostgreSQL 数据类型，第 643 页

第 195–198 页

`references` 属性，消息，第 384 页

管道（`|`）运算符，第 193 页

引用完整性，表，第 643 页

POSIX 正则表达式语法，第 193–195 页

`referer_check` 指令，第 430 页

预定义字符范围，第 194 页

反射 API

拒绝认证方法

组成类，第 170 页

`pg_hba.conf` 文件，PostgreSQL，第 655 页

使用反射的其他任务，第 176 页

`relacl` 列

依赖于反射的 PEAR 包，第 176 页

`pg_class` 表，PostgreSQL，第 656 页

`ReflectionClass` 类，第 170 页

重载命令，`pg_ctl` 程序，第 594 页

`ReflectionMethod` 类，第 172 页

`REMOTE_ADDR`，`$_SERVER` 超全局变量，第 65 页

`ReflectionParameter` 类，第 174 页

`rename` 函数，第 253 页

`ReflectionProperty` 类，第 175 页

`RENAME` 关键字，第 627 页

反射，面向对象编程，第 169–176 页

修改表，第 632 页

描述，第 157 页

重命名条目

内省，第 170 页

`ldap_rename` 函数，第 417 页

`ReflectionClass` 类，第 170 页

可重复读事务隔离级别，第 766 页

`ReflectionMethod` 类，第 172 页

`replace` 函数，PostgreSQL，第 724 页

`ReflectionParameter` 类，第 174 页

`replace` 参数，`header` 函数

`ReflectionProperty` 类，第 175 页

认证，PHP，第 327 页

`region` 方法

`reply_to` 属性，消息，第 380 页

`Validate_US` 包，PEAR，第 227 页

`reply_toaddress` 属性，消息，第 380 页

`register` 命令，`pg_ctl` 程序，第 594 页

`report_memleaks` 参数，第 31 页

`register_argc_argv` 参数，第 34 页

报告敏感级别

`register_globals` 参数，第 33 页

`error_reporting` 指令，第 178 页

`register_long_arrays` 参数，第 34 页

`$_REQUEST` 超全局变量，第 67 页

`register_tick_function` 函数，PHP，第 78 页

`request` 属性

`registration.php` 文件

NuSOAP 调试，第 502 页

密码指定，第 338 页

请求验证

注册表

PostgreSQL 访问权限系统，第 652 页

警告：`PEAR_ENV.reg` 文件，第 263 页

`REQUEST_URI`，`$_SERVER` 超全局变量，第 65 页

正则表达式运算符，PostgreSQL，第 721 页

[www.it-ebooks.info](http://www.it-ebooks.info/)

■ 索引

**839**

`require` 语句

`retrieveBio` 函数，NuSOAP

函数库，第 100 页

向客户端返回数组，第 498 页

PHP，第 88 页

检索数据，PostgreSQL，第 678–681 页

Smarty 模板引擎，第 452 页

选择的行数和修改的行数，第 681 页

`require_once` 函数，PHP，第 89 页

检索 LDAP 数据，第 404 页

`require_once` 语句，面向对象编程，第 155 页

`reset` 数组函数，第 113 页

`resource` 数据类型，PHP，第 53 页

`return` 关键字/语句

资源处理，PHP 4，第 2 页

返回多个值，第 96 页

资源限制

从函数返回值，第 95 页

`return` 语句，PHP，第 78 页

`return_path` 属性，消息，第 380 页


好的，作为一名高级文档工程师和翻译员，我将遵循您提供的注意事项和示例，对给定的英文文本进行翻译。

译文如下：


PHP 配置指令，28

复用软件资源

Web 服务存在的原因，475

文件 I/O，242

`REVOKE` 命令

管理，PostgreSQL，596

保护 PostgreSQL，661

响应属性，NuSOAP 调试，502

`REVOKE DELETE` 命令

`restart` 命令

使视图可交互，715

`pg_ctl` 程序，594

`rewind` 函数，250

`RESTRICT` 关键字

重写特性，Apache，315

删除序列，635

`right_delimiter` 属性

删除模式，628

将 CSS 与 Smarty 结合使用，467

删除视图，708

`rmdir` 函数，252

`RESTRICT` 选项

角色

移除触发器，PostgreSQL，741

保护 PostgreSQL，660

受限模式

回滚

*参见* 安全模式

回滚事务，765

结果集，SQLite

事务示例，770

操作结果集指针，546–548

`ROLLBACK` 命令

解析，541–544

事务示例，770，771

检索详细信息，544–546

`rollback` 方法，PDO，571

`sqlite_current` 函数，546

`rollback` 方法，PHP，772

`sqlite_has_more` 函数，546

`rollbacktosavepoint` 方法，PHP，772

`sqlite_next` 函数，547

罗马数字

`sqlite_rewind` 函数，547

转换数字格式，261

`sqlite_seek` 函数，547

根目录

结果

`doc_root` 指令，519

`pg_free_result` 函数，678

`DocumentRoot` 指令，Apache，523

`pg_result_error` 函数，675

隐藏敏感数据，523

`pg_result_error_field` 函数，676

`rowCount` 方法，PDO，563

`pg_result_status` 函数，674

[www.it-ebooks.info](http://www.it-ebooks.info/)

**840**

■索 引

行

`safe_mode_gid` 参数，25，517

`pg_affected_rows` 函数，681

`safe_mode_include_dir` 参数，25，517

`pg_fetch_row` 函数，680

`safe_mode_protected_env_vars` 参数，

`pg_num_rows` 函数，681

26，518

PostgreSQL 数据库类检索，694

`save_handler` 指令，427

`sqlite_changes` 函数，546

`save_path` 指令，428

`sqlite_last_insert_rowid` 函数，541

保存点，事务，769

`sqlite_num_rows` 函数，546

可伸缩性

RPC

基于数据库的身份验证，331

XML-RPC 协议的实现，261

PHP 4 特性，2

RPM 包

PostgreSQL，574

下载 PostgreSQL，580

`scandir` 函数，252

从 RPM 包安装 PostgreSQL，582

`schema` 命令，SQLite，537

`rsort` 数组函数，120

模式搜索路径，628

`rtrim` 函数，223

模式

规则，PostgreSQL，708–711

创建，627

创建规则，709

删除，628

删除规则，711

信息模式，687–688

`DO ALSO` 形式的规则，716

重命名，628

插入规则，710

`schema` 搜索路径，628

`INSTEAD` 形式的规则，715

表模式，SQLite，548

使视图可交互，711–716

`schemaTargetNamespace` 方法，499

移除规则，709

作用域

规则类型，710–711

在...内修改配置指令，21

选择规则，710

嵌套函数，97

规则和触发器，747

按引用传递参数，93

更新规则，710

按值传递参数，92

运行时，PostgreSQL

`scope` 属性

管理运行时信息，600

`config_load` 函数，Smarty，466

■**S**

作用域，PHP 变量，60–63

安全模式

函数参数，61

安全配置 PHP，516–518

全局变量，61

启用后的影响，516

局部变量，60

PHP 配置指令，25

静态变量，62

`sql.safe_mode` 指令，520

`script` 参数

`safe_mode` 参数，25，516–517，520

`insert` 标签，Smarty，463

`safe_mode_allowed_env_vars` 参数，

`script` 标签

26，518

界定 PHP 代码，45

`safe_mode_exec_dir` 参数，25，518

[www.it-ebooks.info](http://www.it-ebooks.info/)

■索 引

**841**

脚本编写

Smarty 模板引擎，450

跨站脚本攻击，524

SQLite，535

脚本

变量函数，100

`doc_root` 指令，37

`SEEK_CUR`/`SEEK_END`/`SEEK_SET`

搜索，PostgreSQL

移动文件指针，249

全文搜索，763

`seen` 属性，消息，384

文本搜索，749

选择数据，PostgreSQL

搜索

选择的行数，681

`ldap_list` 函数，405

选择规则，710

`ldap_read` 函数，405

`self` 关键字

`ldap_search` 函数，404

静态类成员，153

PostgreSQL，759–764

`sender` 属性，消息，381

模式搜索路径，628

`senderaddress` 属性，消息，380

`section` 属性

发送消息

`config_load` 函数，Smarty，466

`imap_mail` 函数，387

`section` 函数，Smarty，459

`sendmail_from` 指令，40，368

`sectionelse` 函数，Smarty，461



## 索引

`sendmail_path`指令，40，368  
安全  
分隔符  
安全配置 PHP，516–520  
`arg_separator.input`指令，33  
更改文档扩展名，522  
`arg_separator.output`指令，32  
配置参数，518–520  
序列  
`expose_php`指令，521  
创建，633  
隐藏配置细节，520–522  
删除，635  
安全模式，516–518  
函数，634  
阻止`phpinfo`调用，522  
修改，633  
跨站脚本攻击，524  
`SERIAL`数据类型，PostgreSQL，639  
数据加密，528–532  
可序列化事务隔离级别，766  
文件删除，524  
`serialize`方法，NuSOAP，501  
隐藏敏感数据，522–523  
`serialize_handler`指令，430  
LDAP 模型，400  
`serialize_precision`指令，24  
NuSOAP 连接，502  
`$_SERVER`超全局变量，65  
PHP 安全编程，515–532  
服务器错误  
PostgreSQL，575，649–663  
`faultstring`属性，NuSOAP，500  
用户定义函数，728  
服务器签名  
`expose_php`指令，521  
清理用户数据，524–528  
`escapeshellarg`函数，526  
服务器  
`escapeshellcmd`函数，527  
`PDO_ATTR_SERVER_*`属性，560  
相关函数，526  
启动和停止数据库服务器，593–596  
`htmlentities`函数，527  
`ServerSignature`指令，Apache，520  
`strip_tags`函数，528  
`ServerTokens`指令，Apache，521  

`serialize_handler`，430  
服务配置  
`url_rewriter.tags`，432  
安装 PostgreSQL，586  
`use_cookies`，428  
服务名称参数  
`use_only_cookies`，428  
安装 PostgreSQL，586  
`use_trans_sid`，431  
服务参数  
会话数据编码/解码，435  
`pg_connect`函数，668  
函数  
服务  
`session_close`，441  
互联网服务，364–365  
`session_decode`，436  
保护 PostgreSQL，650  
`session_destroy`，433，441  
`$_SESSION`超全局变量，67  
`session_encode`，435  
会话处理，425–446  
`session_garbage_collect`，441  
*另请参阅* cookies  
`session_id`，434  
自动登录示例，437–439  
`session_open`，441  
配置指令，427–432  
`session_read`，441  
创建/删除会话变量，434  
`session_register`，434  
自定义基于 PostgreSQL 的会话处理器，442–445  
`session_set_save_handler`，441  
定义回调处理器，430  
`session_start`，433  
销毁会话，433  
`session_unregister`，434  
确定会话页面的缓存方式，431  
`session_unset`，433，434  
指令  
`session_write`，441  
`auto_start`，429，433  
管理会话中的对象，429  
`cache_expire`，431  
最近查看文档索引示例，439–440  
`cache_limiter`，431  
检索和设置`SID`，434  
`cookie_domain`，429  
会话处理支持，PHP 4，3  
`cookie_lifetime`，429，437  
启动会话，432  
`cookie_path`，429  
存储会话信息，427  
`entropy_file`，430  
用户定义会话处理器，441–445  
`entropy_length`，431  
使用会话验证登录信息，438  
`gc_divisor`，430  
`sessioninfo`表  
`gc_maxlifetime`，432  
自定义基于 PostgreSQL 的会话处理器，442  
`gc_probability`，430  
`set`命令，psql，615  
`hash_bits_per_character`，432  
`setAttribute`方法，PDO，561  
`hash_function`，431  
`setCancelText`方法  
`name`，429  
`Auth_HTTP`类，PEAR，336  
`referer_check`，430  
`setClass`方法  
`save_handler`，427  
创建 SOAP 服务器，510  
`save_path`，428  

`setcookie`函数  
`$_COOKIE`超全局变量，66  
`setDay`方法，291  
`setDMY`方法，290  
`setFetchMode`方法，PDO，570  
`setFirstDow`方法，300  
`setHTTPProxy`方法，NuSOAP，501  
`setJulian`方法，292  
`setLastDow`方法，301  
`setlocale`函数，279  
`setMonth`方法，292  
`setPersistence`方法  
创建 SOAP 服务器，510  
`setsavepoint`方法，772  
SimpleXML，486–491  
函数，486–488  
`simplexml_import_dom`，488  
`simplexml_load_file`，487  
`simplexml_load_string`，488  
方法，488–491  
排序  
在数组中添加值，130  
数组中值的排序，129  
`SID`  
存储 cookies，426  
生成过程，430  
`hash_bits_per_character`指令，432  
`hash_function`指令，431  
使用 URL 重写实现持久性，426  
检索和设置，434  
使用`SID`进行会话处理，425  
`session_id`函数，434  
`use_trans_sid`指令，431  
`setter`（`__set`）方法  
创建自定义 getter 和 setter，142  
属性，140  
`setToLastMonthDay`方法，300  
`setToWeekday`方法，298



`settype` 函数，56

`asXML`，489

`setval` 序列函数，634

`attributes`，488

`setYear` 方法，293

`children`，489

`shared_buffers` 设置，PostgreSQL，596

`xpath`，490

`SharpReader` 接口，477

`SimpleXML` 扩展，474

Shell 命令，252–253

简洁性，5

系统级程序执行，254–258

单引号
Shell 语法
字符串插值，76

注释，PHP，46

单列普通索引，PostgreSQL，751

`SHELL`，`$_ENV` 超全局变量，67

`shell_exec` 函数，258

一维数组，104

短标签 (`<? ... ?>`)
size 属性，消息，384
注意：与 XML 冲突，45

`size` 变量
界定 PHP 代码，44

`$_FILES` 数组，348

`short_open_tag` 参数，22，44

`size_limit` 参数，`ldap_search()`，404

`show` 属性
`sizeof` 数组函数，117

`section` 函数，Smarty，460

`SMALLINT` 数据类型，PostgreSQL，637

`show_source` 函数
Smarty 模板引擎，449–471
语法高亮，27

`$cache_lifetime` 属性，468

`shuffle` 数组函数，129

缓存，450，468–471
每模板多缓存，470

[www.it-ebooks.info](http://www.it-ebooks.info/)

**844** ■ 索引

注释，454
典型模板的语法，448

配置文件，465–466
模板编译，450

控制结构，457–462
使用，452–454

创建简单设计模板，452
结合 CSS 使用，467

特性，449
变量修饰符，454–457

函数：
`capitalize`，454
`$smarty.config` 变量，466

`config_load`，465
`SMARTY_DIR` 常量，451

`count_words`，455
`SMTP` 指令，40，367

`date_format`，455
`SMTP` 协议，实现，260

`default`，456
`smtp_port` 指令，40，368

`display`，453
`SOA`（授权起始记录）记录类型，361

`get_config_vars`，466
`SOAP`，491–512

`include_php`，464
装箱客户端/服务器，511

`is_cached`，469
C# SOAP 客户端，513

`php`，464
客户端/服务器交互，511–512

`section`，459
配置指令，507–508

`sectionelse`，461
创建 SOAP 客户端，503–506

`strip_tags`，456
创建 SOAP 服务器，506–511

`truncate`，456
定义，491

`insert` 标签，463
介绍，474

在模板中插入横幅，463
方法/函数：

安装，450–452
`addFunction`，509

实例化 `Smarty` 类，452
添加类方法，510

`literal` 标签，464
导出所有函数，509

对执行脚本可见，452
`getFunctions`，504

表现逻辑，450，454–464
`getLastRequest`，505

引用配置变量，466
`getLastResponse`，505

安全性，450
`getQuote`，509

语句：
`handle`，509
`else`，458
`setClass`，510

`fetch`，462
`setPersistence`，510

`foreach`，458
`NuSOAP`，492–502

`foreachelse`，459
PHP 5 的 SOAP 扩展，502–512

`if`，457
`SoapClient` 构造函数，503–504

`include`，462
`SoapServer` 构造函数，508

`require`，452
`soap.wsdl_cache_dir` 指令，508

典型 Smarty 模板的语法，449
`soap.wsdl_cache_enabled` 指令，508

[www.it-ebooks.info](http://www.it-ebooks.info/)

■ 索引 **845**

`soap.wsdl_cache_ttl` 指令，508

`sort` 数组函数，118

`soap_fault` 类
排序标志：
`NuSOAP` 错误处理，500，501
`array_multisort` 标志，121

`SOAP_PERSISTENCE_REQUEST` 模式，511
`sort_flags` 参数，118

`SOAP_PERSISTENCE_SESSION` 模式，511
`sort_mem` 设置，PostgreSQL，596

`soap_version` 参数
排序表格输出，PostgreSQL，699–701
`SoapClient` 构造函数，504
排序值
`SoapServer` 构造函数，508
`ldap_sort` 函数，411

`SoapClient` 构造函数，503–504
源码发行版，Apache，10

`actor` 参数，503
源码发行版，PHP，11

`compression` 参数，503
源码发行版，PostgreSQL，580，582–583

创建 `SoapClient` 对象，504
`:space:` 字符类，195

`exceptions` 参数，503
特殊字符：
`login` 参数，503
转换为 HTML，211

`options` 参数，503
在其前插入反斜杠分隔符，202

`password` 参数，503
拼写检查器：
`proxy_host` 参数，503
Google Web 服务，494

`proxy_login` 参数，503
`split` 函数：
`proxy_password` 参数，503
基于文件的认证，330

`proxy_port` 参数，503
正则表达式，197

`soap_version` 参数，504
`split_part` 函数，PostgreSQL，724

`trace` 参数，504
使视图可交互，713

`wsdl` 参数，503
`spliti` 函数，198

`SoapServer` 构造函数，508
`printf` 语句，PHP，50

`actor` 参数，508
SQL（结构化查询语言）



