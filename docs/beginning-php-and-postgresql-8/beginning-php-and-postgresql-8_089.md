# PHP 函数与指令

`phpinfo`函数, 522  
`pgsql.max_persistent`指令, 666  

## 配置命令

存储连接信息于  
在 Unix 上自定义 PHP 安装, 17  
单独文件, 669–670  
从源码安装 PostgreSQL, 582  

## 一致性

`configureWSDL`方法, 499  
事务的 ACID 测试, 765  

## psql 命令

`connect`命令, `psql`, 614  

## PEAR 包

`Console_Getopt`包, PEAR, 260  

## PostgreSQL 连接函数

`connect`函数, PostgreSQL, 690  
对其运行 `info` 命令, 265  

## 参数

`connect_timeout`参数  

#### 常量

常量, OOP, 143  
`pg_connect`函数, 668  
常量, PHP, 68  

## 连接认证

`CONSTRAINT`关键字, 641  
PostgreSQL 访问权限系统, 652  

## 约束

连接  
**另见** 链接  
检查约束, 640  
定义, 641  
域, 645, 646  
关闭, SQLite, 539  
外键, 643  
建立套接字连接, 365–367  
`PRIMARY KEY`属性, 642  
`imap_close`函数, 375  

## 构造函数

`imap_open`函数, 374  
声明语法, 148  
`ldap_connect`函数, 401  
默认异常构造函数, 185  
`ldap_start_tls`函数, 402  
调用父类构造函数, 150  
打开连接但不打开邮箱, 374  
调用不相关的构造函数, 150  
打开, SQLite, 538–539  
重载构造函数, 185  
`PDO_ATTR_CONNECTION_STATUS`属性, 560  
重载, 151  
PostgreSQL 数据库类, 693  
PHP 4, 148  
保护 PostgreSQL, 661  
构造函数, OOP, 148–151  
`sqlite_close`函数, 540  
继承与构造函数, 164–165  
`sqlite_open`函数, 538  

## 容器

连接, PostgreSQL  
容器不是邮箱, 376  
建立和关闭连接, 667–671  

## 上下文

持久或非持久连接, 669  
流包装器, 391  
`pg_close`函数, 671  

## 控制语句

`pg_connect`函数, 668  
`continue`语句, PHP, 86  
`pg_connection_busy`函数, 672  
**Contrib 模块**  
`pg_connection_status`函数, 673  
安装 PostgreSQL, 588  
`pg_hba.conf`文件, 654  

##### 控制结构

控制结构, 78–89  
`pg_pconnect`函数, 669  
条件语句, 79–81  
[www.it-ebooks.info](http://www.it-ebooks.info/)  
执行控制语句, 78–79  
■索引  
文件包含语句, 86–89  
**797**  
循环语句, 81–86  

## PL/pgSQL 函数

`HEADER`子句, 781  
PL/pgSQL 函数, 732–733  
`NULL`子句, 781  

## Smarty 模板引擎

`QUOTE`子句, 782  

## 转换数据

复制  
`pg_convert`函数, 683  
向/从表复制数据, 778–782  
`$_COOKIE`超全局变量, 66  
复制消息, 389  
`cookie_domain`指令, 429  

#### 复制表

`cookie_lifetime`指令, 429  
复制表, 630  
`cookie_path`指令, 429  
`pg_copy_from`函数, 684  

#### Cookie

`pg_copy_to`函数, 683  
Cookie  
`PGSQL_COPY_IN`值, 674  
允许/限制 URL 重写, 428  
`PGSQL_COPY_OUT`值, 674  
更改 Cookie 名称, 429  
`count`数组函数, 116  
跨站脚本, 525  
`count`函数, PostgreSQL, 725  
`name`指令, 429  
`count_chars`函数, 224  
检索会话名称, 427  
`count_words`函数, Smarty, 455  
会话处理, 426  

## CrackLib 扩展

`use_cookies`指令, 428  
**CrackLib 扩展**, PHP  
存储会话信息, 428  
避免易猜测密码, 340  
`use_only_cookies`指令, 428  
安装, 340  
协调世界时, 271  
最小密码要求, 340  
`COPY`命令, PostgreSQL, 777–783  
PECL 网站, 340  
`BINARY`关键字, 779  
使用, 340–341  
从 PHP 脚本调用, 782–783  
使用字典, 341  
`COPY FROM`命令, 778  
`cracklib_dict.pwd`字典, 341  
分隔字段, 779  
`CREATE DATABASE`命令, 626  
`COPY TO`命令, 778  
`CREATE DOMAIN`命令, 646  
向/从表复制数据, 778–782  
`CREATE FUNCTION`命令, 727  
更改默认分隔符, 780  
`CREATE GROUP`命令, 659  
从表复制数据, 778  
`CREATE INDEX`命令, 753  
从文本文件复制数据, 779  
`CREATE RULE`命令, 709  
从表复制数据到文本文件, 782  
`CREATE SCHEMA`命令, 627  
复制特定列, 780  
`CREATE SEQUENCE`命令, 633  
处理 NULL 值, 781  
`CREATE TABLE`语句, 629, 630  
导出表 OID, 780  
`CREATE TABLESPACE`命令, 601  
处理 CSV 文件, 781  
`CREATE TRIGGER`命令, 739  
`CSV`子句, 781  
`CREATE TYPE`命令, 644  
`DELIMITER`子句, 780  
`CREATE USER`命令, 658  
`ESCAPE`子句, 782  
`CREATE VIEW`命令, 707, 708  
`FORCE`子句, 782  
`create_crumbs`函数  
[www.it-ebooks.info](http://www.it-ebooks.info/)  

**798**  
■索引  
从数据库表数据创建面包屑, 320



