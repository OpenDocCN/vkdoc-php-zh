# pg_hba.conf 文件, PostgreSQL, 655

处理条目值, 405–406

`krsort` 数组函数, 123

`ldap_8859_to_t61` 函数, 420

`ksort` 数组函数, 122

`ldap_add` 函数, 416

### L

`ldap_bind` 函数, 402

L 选项, `psql`, 612

`ldap_close` 函数, 403

l 选项, `psql`, 612

`ldap_compare` 函数, 411

语言特性, 4–7

`ldap_connect` 函数, 401

语言选项

`ldap_count_entries` 函数, 407

PHP 配置指令, 22

`ldap_delete` 函数, 418

语言（外部）

`ldap_dn2ufn` 函数, 421

`HTTP_Upload` 类, PEAR, 357

`ldap_err2str` 函数, 422

安装 PostgreSQL, 585

`ldap_errno` 函数, 422

`lastval` 序列函数, 634

`ldap_error` 函数, 423

[www.it-ebooks.info](http://www.it-ebooks.info/)

## 索引

**817**

`ldap_explode_dn` 函数, 421

轻量级目录访问协议

`ldap_first_attribute` 函数, 407

*参见* LDAP

`ldap_first_entry` 函数, 412

LIKE 运算符, PostgreSQL, 721

`ldap_free_result` 函数, 415

LIMIT 子句

`ldap_get_attributes` 函数, 408

分页数据库类输出, 701, 702

`ldap_get_dn` 函数, 410

行

`ldap_get_entries` 函数, 414

`getLine` 方法, 异常类, 186

`ldap_get_option` 函数, 420

lines 属性, 消息, 382

`ldap_get_values` 函数, 406

链接文件类型, 232

`ldap_get_values_len` 函数, 406

`link` 函数, 232

`ldap_list` 函数, 405

link 标签

`ldap_mod_add` 函数, 416

将 CSS 与 Smarty 结合使用, 467

`ldap_mod_del` 函数, 418

`linkinfo` 函数, 233

`ldap_mod_replace` 函数, 417

链接

`ldap_modify` 函数, 417

*另请参阅* 连接

`ldap_next_attribute` 函数, 408

创建硬链接, 232

`ldap_next_entry` 函数, 413

创建符号链接, 235

`LDAP_OPT_XYZ` 选项, 419

链接失效, 321

`ldap_read` 函数, 405

`pageLinks` 函数, 704

`ldap_rename` 函数, 417

`pgsql.allow_persistent` 指令, 666

`ldap_search` 函数, 404

`pgsql.max_links` 指令, 667

`ldap_set_option` 函数, 420

检索符号链接信息, 233

`ldap_sort` 函数, 411

检索符号链接的目标, 235

`ldap_start_tls` 函数, 402

Linux

`ldap_t61_to_8859` 函数, 421

下载 PHP, 10

`ldap_unbind` 函数, 403

在 Linux 上安装 Apache/PHP, 11–13

`left_delimiter` 属性

在 Linux 上安装 PostgreSQL, 582–585

将 CSS 与 Smarty 结合使用, 467

安装后步骤, 583–585

最左前缀, 752

`list` 数组函数, 107

Lerdorf, Rasmus, 1

返回多个值, 96

小于 (`<`) 运算符, 74

`list` 函数, PHP

小于运算符, PostgreSQL, 720

`sqlite_fetch_array` 函数, 542

库, 5

`listen_address`, `postgresql.conf` 文件

函数库, 100

保护 PostgreSQL, 651

辅助函数, 153–155

`literal` 标签

许可

Smarty 模板引擎, 464

PostgreSQL, 575, 579

将 CSS 与 Smarty 结合使用, 467

SQLite, 535

局部变量

许可限制, 7

变量作用域, PHP, 60

[www.it-ebooks.info](http://www.it-ebooks.info/)

**818**

## 索引

区域设置选项

`ForceType` 指令, 315

安装 PostgreSQL, 587

用户友好的 URL, 314

本地化字符串, 279

`loop` 属性

本地化日期和时间

`section` 函数, Smarty, 460

显示, 279–282

循环语句, PHP, 81–86

Log 包, PEAR, 261

替代语法, 80

`log_duration` 设置, PostgreSQL, 599

`break` 语句, 85

`log_duration`, PostgreSQL, 599

`continue` 语句, 86

`log_errors` 参数, 30, 179

`do...while` 语句, 82

`log_errors_max_len` 参数, 31, 180

`for` 语句, 83

`log_min_duration_statement`,

`foreach` 语句, 84

PostgreSQL, 600

`while` 语句, 81

`LOG_XYZ` 错误日志选项, 182

松散类型, 5

日志记录

`lostpassword.php`

*另请参阅* 错误日志

重置用户密码, 344

`checkpoint_segments` 设置,

`:lower:` 字符类, 195

PostgreSQL, 599

`lower` 函数, PostgreSQL, 724

`log_duration` 设置, PostgreSQL, 599

`lstat` 函数, 233

`log_min_duration_statement` 设置,

`ltrim` 函数, 222

PostgreSQL, 600

PEAR 包, 261

### M

PHP 配置指令, 29

m4 宏处理器

实用查询分析工具, 600

在 Linux/Unix 上安装 PHP, 12

逻辑

`magic_quotes_gpc` 参数, 35

将业务逻辑与表示逻辑分离, 448

`magic_quotes_runtime` 参数, 35

逻辑运算符, 72

`magic_quotes_sybase` 参数, 35

MagpieRSS, 479–486

# PostgreSQL

## 性能与配置
- `maintenance_work_mem` 设置，PostgreSQL，597
- `max_fsm_pages` 设置，PostgreSQL，597
- `max_fsm_relations` 设置，PostgreSQL，597
- `max_prepared_transactions` 设置，PostgreSQL，597
- `shared_buffers` 设置，PostgreSQL，596
- `work_mem` 设置，PostgreSQL，596
- PostgreSQL，678

## 日志与登录
- `login` 参数
- `logins`
- 自动登录，会话处理，437
- 通过 `psql` 登录/退出服务器，613
- 用户登录管理，337–344
- 使用会话验证登录信息，438

## 聚合与缓存
- 聚合订阅（aggregating feeds），483–484
- 缓存订阅（caching feeds），485
- 禁用缓存，485
- 限制显示标题数量，484
- 解析订阅，479–481
- 渲染获取的订阅，481–482
- 特性，479
- 安装，479
- `SoapClient` 构造函数，503

## Apache 相关
- `lookback` 特性，Apache
- `AcceptPathInfo` 指令，316
- 配置，315–316
- `Files` 指令，315

### 邮件
- `mail` 函数，368
- `addl_headers` 参数，368
- `addl_params` 参数，368
- 配置指令，367–368
- 使用示例，369–372
- `force_extra_parameters` 指令，368
- 邮件传输代理（MTA），367, 368
- `sendmail_from` 指令，40, 368
- `sendmail_path` 指令，40, 368
- `SMTP` 指令，367
- `smtp_port` 指令，368
- Mail 包，PEAR，260
- 将 PHP 变量传递到 JavaScript 函数，311
- PHP 配置指令，40

## 密码与加密
- `md5` 认证方法，`pg_hba.conf` 文件，PostgreSQL，655
- `md5` 函数，基于文件的认证，330
- PHP 加密函数，529
- `MCrypt`，531
- `mcrypt_decrypt` 函数，532
- `mcrypt_encrypt` 函数，531
- `mhash` 函数，529, 530

## 邮件与邮箱
- 邮箱属性，消息，379
- 邮箱，容器非邮箱，376
- 创建邮箱，388
- 删除邮箱，388
- 清除邮箱，374, 375
- `imap_createmailbox` 函数，388
- `imap_deletemailbox` 函数，388
- `imap_getmailboxes` 函数，375
- `imap_num_msg` 函数，376
- `imap_open` 函数，374
- `imap_renamemailbox` 函数，389
- `imap_status` 函数，377
- 邮箱管理，388–389
- 在邮箱间移动消息，389
- 邮箱中的消息数量，376
- 以只读权限打开邮箱，374
- 重命名邮箱，389
- 检索关于邮箱的信息，375
- 检索邮箱状态信息，377
- 无子邮箱，376
- `message_id` 属性，消息，380, 383
- 消息组成，386
- 消息复制，389
- 消息清除，389
- 消息头属性，379
- `imap_expunge` 函数，389
- `imap_fetchbody` 函数，385
- `imap_fetchoverview` 函数，383
- `imap_fetchstructure` 函数，382
- `imap_headerinfo` 函数，379–382
- `imap_headers` 函数，378
- `imap_mail` 函数，387
- `imap_mail_compose` 函数，386
- `imap_mail_copy` 函数，389
- `imap_mail_move` 函数，389
- `imap_num_msg` 函数，376
- 发送带额外头的电子邮件，369
- 设置 `From` 字段，368
- 发送邮件附件，Mime Mail 5，371
- MIME 类型

## 内存与性能参数
- 释放内存，LDAP，415
- `ldap_free_result` 函数，415
- `maintenance_work_mem` 设置，PostgreSQL，597
- `max_execution_time` 参数，29, 346, 519
- `MAX_FILE_SIZE` 指令，350
- `max_input_time` 参数，29
- `memory_limit` 参数，29, 347, 519
- `report_memleaks` 指令，31

## 序列与数学运算符
- `MAXVALUE` 关键字，创建序列，633
- 数学运算符，PostgreSQL，721
- `max` 函数，PostgreSQL，725
- `max` 属性，`section` 函数，Smarty，460

## 反射与方法
- 方法参数，`ReflectionParameter` 类，174
- `method_exists` 辅助函数，155
- 方法，异常类，186
- `ReflectionMethod` 类，172
- 方法，OOP，143–147
- 抽象方法，146
- 检查方法是否可用于对象，155
- 声明方法，144
- final 方法，147
- 函数与方法比较，143
- `getMessage` 方法，异常类，186
- 获取类的方法，154
- 调用方法，144
- 方法作用域，144–147
- private 方法，145
- protected 方法，146
- public 方法，145
- static 作用域，152

## 数据库
- Microsoft SQL Server，558
- 从源码安装 PostgreSQL，582
- 掩码，`umask` 函数，241

## 其他
- Masinter, Larry，345

[www.it-ebooks.info](http://www.it-ebooks.info/)

■索引

**819**

## 索引

### A

`default_mimetype` 指令，35

`message` 管理，389

`min` 函数，PostgreSQL，725

移动，389

`MINVALUE` 关键字，633

邮箱中的消息数量，376

`mktime` 函数，PHP，277

检索消息正文，385

确定当前月份的天数，284

检索消息头信息，379

`mm`（共享内存）选项

将消息头检索到数组，378

存储会话信息，427

检索消息概览，383

`mode` 参数

检索消息结构，382

`setFetchMode` 方法，PDO，570

检索消息，378–386

`modes`

发送消息，387

设置文件 I/O 访问级别，243

`metacharacters`

`modifiers`

Perl 正则表达式语法，199–200

Perl 正则表达式语法，199

`METHOD` 字段，`pg_hba.conf` 文件，655

修改数据

方法重载，OOP，158

*参见* 更新数据

[www.it-ebooks.info](http://www.it-ebooks.info/)

■索引

**821**

### M

`module` 设置

`MyException` 类

PHP 配置指令，39

扩展异常类，187

`modulus`（`%`）运算符，71

MySQL

`monetary` 表示

PDO 支持的数据库，558

本地化格式，280

`safe_mode` 限制，517

Moreover Technologies

■**N**

Real Simple Syndication (RSS)，476

`name` 属性

`mortgage.php`，98

`foreach` 语句，Smarty，458

`section` 函数，Smarty，460

`move_uploaded_file` 函数，PHP，350

`name` 指令，429

`HTTP_Upload` 类，PEAR，357

`name` 参数

移动消息

`insert` 标签，Smarty，463

`imap_mail_move` 函数，389

`name` 变量

`msgno` 属性，消息，383

`$_FILES` 数组，348

MTA（邮件传输代理）

命名参数

`mail` 函数，368

预处理语句，PDO，564，565

设置 `mail` 函数的参数，367

命名空间，PHP 与，158

Muffett，Alec，340

命名约定

多维数组，104

目录与 PEAR 包，268


排序，121

接口，OOP，166

多列普通索引，

`POST`变量，304

PostgreSQL，752

PostgreSQL 表，630

多重继承，PHP 与，158

命名模型，LDAP，400

多选列表框，307

`NaN`特殊值，638

乘法（`*`）运算符，71

`NAPTR`（Naming Authority Pointer）记录

多处理模块

类型，DNS，360

在 Linux/Unix 上安装 Apache，12

`natcasesort`数组函数，120

国家气象局

*参见* MVCC

PostgreSQL 用户，577

`MustUnderstand`错误

`natsort`数组函数，119

`faultstring`属性，NuSOAP，500

Navicat，622–623

`mutators`，140

导航提示，网站，313–323

setter（`_set`）方法，142

面包屑导航，317–321

MVCC（多版本并发控制），574

自定义错误处理器，321–323

PostgreSQL，602

用户友好 URL，313–317

PostgreSQL 事务，766

导航路径

MX（邮件交换记录）记录，DNS，360

*参见* 面包屑导航

`getmxrr`函数，363–364

[www.it-ebooks.info](http://www.it-ebooks.info/)

**822**

■索引

### N

嵌套，5

`NOT`（`!`）逻辑运算符，73

嵌套函数，96

表示，178

`PL/pgSQL`函数，736

`NOT NULL`属性，PostgreSQL，642

`Net_SMTP`包，PEAR，260

`NOTHING`关键字

`Net_Socket`包，PEAR，260

删除规则，711

Netcraft，2

`notice`

网络，393–398

`pg_last_notice`函数，674

使用 NMap 创建端口扫描器，395

`pgsql.ignore_notice`指令，667

Ping 服务器，394–395

`pgsql.log_notice`指令，667

子网转换器，395–397

`now`函数，PostgreSQL，724

测试用户带宽，397–398

`NS`（Name Server Record）记录类型，

`NEW`构造

DNS，360

触发器函数，PostgreSQL，740，741，742，743，745

`NULL`属性，PostgreSQL，642

`new`关键字，OOP，136

`NULL`字符，二进制数据，550

`NEW`变量

`null`数据类型，PHP，54

触发器函数，PostgreSQL，745

`NULL`状态，`BOOLEAN`数据类型，640

换行符，242

`NULL`值

`newsgroups`属性，消息，380

比较运算符，PostgreSQL，720

`newsrc`配置文件

将数据复制到/从表，781

`imap_open`函数，374

索引最佳实践，759

`next`数组函数，114

逻辑运算符，PostgreSQL，720

`nextval`序列函数，634

`PDO_ATTR_ORACLE_NULLS`属性，560

`nl2br`函数

`NULLIF`函数，PostgreSQL，726

NuSOAP 调试，502

`numberFields`函数，PostgreSQL，695

数字

将数字格式转换为字符串，210

转换数字格式，261
