# 第 4 章：模块化编程

完成本章学习后，学生将能够：

- 创建一个无错误的简单面向对象（`OO`）模块化 PHP 程序

- 创建一个 PHP 类，并实例化该类（对象）

- 创建一个 `OO` PHP 封装程序，包括 `get` 和 `set` 方法

- 创建接受参数并返回信息的 PHP 方法（函数）

- 创建 PHP 公有和私有属性（变量）

- 将来自其他文件或库的现有 PHP 代码导入到程序中

- 使用三元（条件）运算符验证接收到的信息