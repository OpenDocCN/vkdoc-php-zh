# PHP 参考手册索引

`file uploads`，346–355

`PGSQL_DIAG_XYZ` 值，676，677

常规特性，4–7

`PGSQL_DML_NO_CONV` 值，682

用其生成表单，308–310

`PGSQL_DML_STRING` 值，682

历史沿革，1–4

`PGSQL_EMPTY_QUERY` 值，674

标识符，57–58

`PGSQL_ERRORS_DEFAULT` 值，678

安装问题，18

`PGSQL_ERRORS_TERSE` 值，678

安装指南

`PGSQL_ERRORS_VERBOSE` 值，678

定制 Unix 构建，17

`PGSQL_FATAL_ERROR` 值，675

定制 Windows 构建，17–18

`PGSQL_NONFATAL_ERROR` 值，675

在 Linux/Unix 上，11–13

`PGSQL_NUM` 值，678

在 Windows 上，13–16

`PGSQL_STATUS_LONG` 值，674

使用 PostgreSQL 库，11

`PGSQL_STATUS_STRING` 值，674

输出，47–50

`PGSQL_TUPLES_OK` 值，675

将 PHP 变量传入 JavaScript

`PGUSER` 变量，`psql`，615

函数，311–313

幻读，766

字符串插值，75–77

`phoneNumber` 方法

超全局变量，63

`Validate_US` 包，PEAR，227

测试安装，16–17

PHP（个人主页）

事务方法，772

自动选择表单数据，310–311

事务，771–775

PHP 缩写的变化，2

使用 CrackLib 扩展，340–341

代码重用，259

变量，58–67

注释，46–47

网页表单，303–313

安全配置 PHP，516–520

处理多值表单

组件，307–308

隐藏配置细节，521–522

PHP 4，2–3

常量，68

PHP 5，3–4

控制结构，78–89

SOAP 扩展，502–512

数据类型，50–57

GNOME XML 库，503

日期和时间函数，272–278

PHP 5.1

界定 PHP 代码，43–46

Date（日期和时间库），288–301

下载指南，10–11

PHP 身份验证，326–337

源代码分发，11

身份验证方法，328–337

Windows 安装程序界面，11

身份验证变量，327–328

Windows zip 包，11

PHP 基础异常类，185

下载 PHP 手册，19

PHP 配置指令

在 HTML 中嵌入 PHP 代码，43–46

创建 SOAP 服务器，507–508

错误报告级别，30

数据处理，32

转义序列，76

动态扩展，39

表达式，68–75

[www.it-ebooks.info](http://www.it-ebooks.info/)

**830**

■ 索 引

启用 PostgreSQL 扩展，665

`default_mimetype`，35

错误与异常处理，177–180

`default_socket_timeout`，39

错误处理与日志记录，29

`define_syslog_variables`，39

文件上传/资源指令，346–347

`disable_classes`，27

文件上传，37

`disable_functions`，26

`fopen` 封装器，38

`display_errors`，30，179

语言选项，22

`display_startup_errors`，30，179

邮件函数，40，367–368

`doc_root`，37

管理配置指令

`docref_ext`，32

`.htaccess` 文件，19–21

`docref_root`，31

`httpd.conf` 文件，21

`enable_dl`，37

`ini_set` 函数，21

`engine`，22

`php.ini` 文件，19–20

`entropy_file`，430

在指令作用域内修改，21

`entropy_length`，431

模块设置，39

`error_append_string`，32

路径与目录，36

`error_log`，32，180

PostgreSQL 扩展，666–667

`error_prepend_string`，32

资源限制，28

`error_reporting`，29，178

安全模式，25

`expose_php`，28

会话处理，427–432

`extension`，39

语法高亮，27

`extension_dir`，37

`syslog`，39

`file_uploads`，38，346

PHP 配置指令，列表

`force_extra_parameters`，368

`allow_call_time_pass_reference`，25

`from`，38

`allow_url_fopen`，38

`gc_divisor`，430

`always_populate_raw_post_data`，36

`gc_maxlifetime`，432

`arg_separator.input`，33

`gc_probability`，430

`arg_separator.output`，32

`hash_bits_per_character`，432

`asp_tags`，22

`hash_function`，431

`auto_append_file`，35

`highlight.bg`，28

`auto_detect_line_endings`，39

`highlight.comment`，27

`auto_prepend_file`，35

`highlight.default`，28

`auto_start`，429

`highlight.html`，28

`cache_expire`，431

`highlight.keyword`，28

`cache_limiter`，431

`highlight.string`，27

`cookie_domain`，429

`html_errors`，31

`cookie_lifetime`，429

`ignore_repeated_errors`，31，180

`cookie_path`，429

`ignore_repeated_source`，31，180

`default_charset`，36

`ignore_user_abort`，27

[www.it-ebooks.info](http://www.it-ebooks.info/)

■ 索 引

**831**

`implicit_flush`，24

`save_handler`，427

`include_path`，36

`save_path`，428

`java.class.path`，40

`sendmail_from`，40，368

`java.home`，41

`sendmail_path`，40，368

`java.library`，41

`serialize_handler`，430

`java.library.path`，41

`serialize_precision`，24

`log_errors`，30，179

`short_open_tag`，22

`log_errors_max_len`，31，180

`SMTP`，40，367

`magic_quotes_gpc`，35

`smtp_port`，40，368

`magic_quotes_runtime`，35

`track_errors`，31，180

`magic_quotes_sybase`，35

`unserialize_callback_func`，24

`max_execution_time`，29，346

`upload_max_filesize`，38，347

`max_input_time`，29

`upload_tmp_dir`，38，347

`memory_limit`，29，347

`url_rewriter.tags`，432

`name`，429

`use_cookies`，428

`open_basedir`，26

`use_only_cookies`，428

`output_buffering`，23

`use_trans_sid`，431

`output_handler`，23

`user_agent`，38

`pgsql` (with-pgsql)，665

`user_dir`，37

`pgsql.allow_persistent`，666

`variables_order`，33

`pgsql.auto_reset_persistent`，666

`y2k_compliance`，23

`pgsql.ignore_notice`, 667

`zend.ze1_compatibility_mode`, 22

`pgsql.log_notice`, 667

`zlib.output_compression`, 24

`pgsql.max_links`, 667

`zlib.output_handler`, 24

`pgsql.max_persistent`, 666

PHP Data Objects（PHP 数据对象）

`post_max_size`, 34, 347

见 PDO

`precision`, 23

PHP Extension Community Library (PECL)（PHP 扩展社区库），

`referer_check`, 430

`register_argc_argv`, 34

php 函数, 91–101

`register_globals`, 33

数组函数, 105–131

`register_long_arrays`, 34

日期和时间函数, PHP, 272–278

`report_memleaks`, 31

辅助函数, 153–155

`safe_mode`, 25

正则表达式函数, 195–205

`safe_mode_allowed_env_vars`, 26

Smarty 模板引擎, 464

`safe_mode_exec_dir`, 25

字符串操作函数, 205–226

`safe_mode_gid`, 25

PHP 反射 API

`safe_mode_include_dir`, 25

见 反射 API

`safe_mode_protected_env_vars`, 26

PHP 脚本

引用 POST 数据, 304

[www.it-ebooks.info](http://www.it-ebooks.info/)

**832**

■索引

php.ini 文件

管道（|）运算符

注释, 20

正则表达式, 193

配置模板, 19

PL（过程语言）, 736

在 Windows 上自定义 PHP 安装

PL/pgSQL 函数

17

ALIAS 类型, 731

在 Linux/Unix 上安装 PHP, 12

参数, 731

在 Windows 上安装 PHP, 15

控制结构, 732–733

管理配置指令, 19–20

错误处理, 733–735

PDO 构造函数, 559

通知错误, 734

设置参数, 20

捕获错误, 733

php.ini-dist 文件

示例函数, 735

在 Linux/Unix 上安装 PHP, 12

EXCEPTION 子句, 733

在 Windows 上安装 PHP, 15

FOR 循环, 733

php.ini-recommended 文件

IF 块, 732

在 Linux/Unix 上安装 PHP, 13

安装, 730

在 Windows 上安装 PHP, 15

INTO 指定, 732

php.ini 配置模板, 19

嵌套, 736

php_admin_flag 关键字

RAISE 命令, 734

管理配置指令, 21

语法, 731–736

php_admin_value 关键字

用户自定义函数, PostgreSQL,

管理配置指令, 21

730–736

PHP_AUTH_PW

变量赋值, 732

认证变量, PHP, 327

变量声明, 731

硬编码认证, 329

WHILE 循环, 732

PHP_AUTH_USER

PL/PHP 函数, 737

认证变量, PHP, 327

平台支持, PostgreSQL, 574

硬编码认证, 329

指针

php_flag 关键字, 21

移动文件指针, 249, 250

PHP_INI_XYZ 作用域, 21

获取文件指针位置, 250

php_value 关键字, 21

多态性, 135

phpinfo 函数

POP3 协议

隐藏配置细节, 522

IMAP 协议, 372

测试 PHP 安装, 16, 17

打开到 POP3 邮箱的连接, 374

phpPgAdmin, 621–622

端口号选项

导出界面, 784

安装 PostgreSQL, 587

导入界面, 784

端口参数

使用 phpPgAdmin 导入和导出数据,

`pg_connect` 函数, 668

783–785

端口设置, `postgresql.conf` 文件

PHPUnit 包, PEAR, 260

保护 PostgreSQL 安全, 651

ping 服务器, 394–395

[www.it-ebooks.info](http://www.it-ebooks.info/)

■索引

**833**

端口

基于 PostgreSQL 的自定义会话处理器,

另见 套接字连接

442–445

c-client 库混淆, 373

数据完整性, 574

使用 fsockopen() 创建端口扫描器, 366

数据库类

使用 NMap 创建端口扫描器, 395

见 PostgreSQL 数据库类

互联网服务的默认端口, 364

删除数据, 685

建立端口 80 连接, 365

显示数据, 678–681

设置连接到服务器的端口, 368

下载 PostgreSQL, 579–581

position 函数, PostgreSQL, 724

文档, 581

POSIX 正则表达式函数, 195–198

Unix 版本, 580

POSIX 正则表达式语法, 193–195

Windows 版本, 580–581

`$_POST` 超全局变量, 65

不退出 psql 编辑文件, 614

POST 方法, 304

启用 PostgreSQL 扩展, 665