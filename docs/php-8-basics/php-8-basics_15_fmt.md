# 索引

## A

- 抽象类
- 实际单引号
- API 特定文档
- 应用程序编程接口（API）
- `app/Models/User.php`
- 任意 Cookie
- `array_change_key_case()`
- `array_chunk()`
- `array_column()`
- `array_combine()`
- `array_count_values()`
- `array_diff()`
- `array_diff_assoc()`
- `array_diff_key()`
- `array_diff_uassoc()`
- `array_diff_ukey()`
- `array_fill()`
- `array_fill_keys()`
- `array_filter()`
- `ARRAY_FILTER_USE_BOTH`
- `ARRAY_FILTER_USE_KEY`
- `array_flip()`
- 数组函数
- `array()`
- `array_change_key_case()`
- `array_chunk()`
- `array_column()`
- `array_combine()`
- `array_count_values()`
- `array_diff()`
- `array_diff_assoc()`
- `array_diff_key()`
- `array_diff_uassoc()`
- `array_diff_ukey()`
- `array_fill()`
- `array_fill_keys()`
- `array_filter()`
- `array_flip()`
- `array_intersect()`
- `array_intersect_assoc()`
- `array_intersect_key()`
- `array_intersect_uassoc()`
- `array_intersect_ukey()`
- `array_is_list()`
- `array_key()`
- `array_key_exists()`
- `array_key_first()`
- `array_key_last()`
- `array_keys()`
- `array_map()`
- `array_merge()`
- `array_merge_recursive()`
- `array_multisort()`
- `array_pad()`
- `array_pop()`
- `array_product()`
- `array_push()`
- `array_rand()`
- `array_reduce()`
- `array_replace()`
- `array_replace_recursive()`
- `array_reverse()`
- `array_search()`
- `array_shift()`
- `array_slice()`
- `array_splice()`
- `array_sum()`
- `array_udiff()`
- `array_udiff_assoc()`
- `array_udiff_uassoc()`
- `array_uintersect()`
- `array_uintersect_assoc()`

## PHP 数组函数与相关概念

### 数组函数列表

- `array_uintersect_uassoc()`
- `array_unique()`
- `array_unshift()`
- `array_values()`
- `array_walk()`
- `array_walk_recursive()`
- `arsort(): bool`
- `assort(): bool`
- `compact()`
- `count()`
- `current()`
- `each()`
- `end()`
- `extract()`
- `krsort(): bool`
- `ksort(): bool`
- `list(): array`
- `natcasesort()`
- `natsort()`
- `next(): mixed`
- `overview`
- `prev(): mixed`
- `range(): array`
- `reset(): mixed`
- `rsort(): bool`
- `shuffle()`
- `shuffle(): bool`
- `sort()`
- `uasort()`
- `uksort()`
- `uses`
- `usort()`
- `array_intersect()`
- `array_intersect_assoc()`
- `array_intersect_key()`
- `array_intersect_uassoc()`
- `array_intersect_ukey()`
- `array_is_list()`
- `array_key()`
- `array_key_exists()`
- `array_key_first()`
- `array_key_last()`
- `array_keys()`
- `array_map()`
- `array_merge()`
- `array_merge_recursive()`
- `array_multisort()`
- `array_pad()`
- `array_pop()`
- `array_product()`
- `array_push()`
- `array_rand()`
- `array_reduce()`
- `array_replace()`
- `array_replace_recursive()`
- `array_reverse()`
- `array_search()`
- `array_shift()`
- `array_slice()`
- `array1_sort_flags`
- `array_splice()`
- `array_sum()`
- `array_udiff()`
- `array_udiff_assoc()`
- `array_udiff_uassoc()`
- `array_uintersect()`
- `array_uintersect_assoc()`
- `array_uintersect_uassoc()`
- `array_unique()`
- `array_unshift()`
- `array_values()`
- `array_walk()`
- `array_walk_recursive()`

### 其他重要概念

#### A

- `Artisan`
- 关联数组
- 认证/授权
- 授权流程
- 自动加载

#### B

- `beginning-php8-and-mysql`
- `Betchy McCleaver`
- 博客与教程
- `bookToAdd`
- `bookToUpdate`
- 布尔类型
- `Bundle`

#### C

- 回调函数
- `Catch`
- 被捕获的异常
- `CHAR`
- 字符类
- `checkMyNum()` 函数
- 类（构造器、定义、析构器、继承、命名空间、对象、父子关系、限制、可见性）
- `ClassNotFoundException`
- `Symfony cli 与 bin 命令`
- `ClosedGeneratorException`
- `column_key`
- 命令行接口 (CLI)
- 命令提示符
- 社区开源开发者
- `compact()`
- 复杂度降低
- `Compose` 命令、`容器`、`Docker 应用`、`Docker Desktop`、功能
- `Composer` 库
- `composer require--dev symfony/maker-bundle`
- `composer require laravelcollective/html`
- `composer require symfony/form`
- `composer require symfony/orm-pack`
- `config/database.php` 文件
- 常量
- `__construct()` 函数
- `construct()` 方法
- 构造器
- `contentPost.php`
- 控制器路由（创建控制器）
- `curl` 命令（`GET 和 POST 请求`、`GET 或 POST 方法`、`元数据存储`、`Postman 请求`、`注册命令输出`、`注册视图表单`）
- `Cookie`（创建、删除、检查元素、修改、用途、检索、用户识别）
- `count()`

- `创建、读取、更新和删除 (CRUD)`（`API`、`数据库连接`、`GET 方法`、`deleteBook`、`showAddBook`、`showBooks`、`showEditBook`、`GET 变量`、`全局变量`、`页眉和导航创建`、`if 语句`、`交互`、`POST 方法`、`bookToAdd`、`bookToUpdate`、`网站`）

- `current()`

#### D

- `数据库 (DB)`（配置、创建、库、迁移、迁移目录、相关值、服务器）

- `数据保护`

- `数据结构`

- `DB_DATABASE`

- `"db.mysite.com"`

- `解耦模式`

- `deleteBook` 函数

- `delete_cookie.php`

- `DELETE 方法`

- `依赖注入`

- `降序`

- `销毁 UserClass`

- `析构器`

- `Docker 应用文件夹`

- `Docker Desktop`

- `Docker Desktop Installer.exe`

- `Docker Desktop for Windows`

- `Docker 安装网页`

- `Docker 安装`（在 Linux、Mac OS、Windows 中）

- `Docker 化环境`

- `Doctrine EntityManager`

- `doctrine_migration_versions` 表

- `Doctrine 包安装`

- `文档`

- `DOMException`

- `双引号字符串`

- `DumpTruckClass`

#### E

- `each()`

- `echo` 命令

- `E_DEPRECATED` 错误

- `Eloquent`

- `启用 Hyper-V Windows 功能`

- `封装`

- `加密密码`

- `end()`

- `.env` 环境文件

- `ErrorException`

- `error_reporting( E_ALL )`

- `错误`（`所有错误`、调试、`include_once`、`ini_set()`、变量）

- `错误类型`

- `错误与异常`

- `E_WARNING 级别错误`

- `execute` 方法

- `extract()`

- `提取标志`

- `提取变量`

#### F

- `FILTER_VALIDATE_EMAIL`

- `filter_var()`

- `finally` 块

- `first_cookie.php`

- `浮点类型`

- `名为 form-action.php`

- `$form`

- `FormBuilder` 方法

- `Form::close`

- `Form::open`

- `框架`（优点、组件、`Composer`、设计模式、缺点、层：认证/授权、数据库库、依赖注入、MVC 层、包管理、会话管理、测试框架、MVC 模式、PSR、选择：活跃发布/开发、应用/业务用例兼容性、博客/教程、社区支持、约定 vs. 定制、开发者技能集、文档、IDE 支持、学习曲线、许可证、测试覆盖率、测试）

## G

### `__get()` 方法

### `GET` 方法

### `PHP CRUD GET` 方法

### `getMenuName()` 方法

### Getter 方法

### `git status`

## H

### Hash 辅助函数

### `header.php`

### `htmlspecialchars PHP` 函数

### 超文本传输协议 (HTTP)

## I, J

### `inc/` 目录

### `include_once` 函数

### 索引数组

### `index_key`

### 独立对象

#### 继承

### Instagram 插件

### 整数

#### 接口

### `IntlException`

### `IOException`

### `__isset()` 方法

### `is_string()` 函数

## K

### 键

### `key_compare_func`

### `krsort()`

### `ksort()`

## L

### Laravel 数据库表创建

### Laravel PHP 框架

#### 常见任务

#### 控制器路由

#### 数据库迁移

#### 命令

#### 创建与表选项

#### 默认模板

#### 目录

### 迁移表结构

#### 修改

#### 创建新迁移文件

#### 输出

### phpMyAdmin

#### 撤销更改

#### 时间戳值

#### 用户注册功能

#### 用户表结构

#### 数据库设置与配置

#### 安装

#### MVC 设计模式

### 注册视图表单

#### 存储用户数据

#### Web 应用程序框架

### Laravel 项目目录结构

### Laravel 表结构

### Laravel 网页

### 学习曲线

### 长度

### `list()`

### `localhost:8000/chapter2/vartest.php`

## M

### `main2.php`

### `main3.php`

### `main.php` 文件

### `ManagerRegistry`

### 操作

### `MessageClass::EXIT_MESSAGE`

### 元字符

### 微框架

### 创建迁移

### `migrations` 表

### 迁移表结构

### 模型-视图-控制器 (MVC)

### 修饰符

### `modify_cookie.php`

### 多维数组

### MVC 层

### MyISAM 存储引擎

### `mysql` 适配器

### `mysql` 连接

#### MySQL

### MySQL 8.0

### MySQLi 优势

#### MySQLi 方法

### MySQL PDO 参数绑定

## N

### 命名空间

### `natcasesort()`

### `natsort()`

### 创建新数据库

#### 基本设计

#### 目录

#### Docker

#### MySQL

#### 命令

#### 容器

#### `mysql-db` 属性

#### `show tables` 命令

#### 结构

#### 表

### `newtest.php`

### `next()`

### Nginx

### Nowdoc 语法

##### NULL

### 数字

## O

### 面向对象编程 (OOP)

#### 降低复杂度

#### 数据保护

#### 封装

#### 自由与创造力

#### 继承

#### 多态

#### 对象

#### 赋值

#### 类更改的影响

#### 复合数据类型

#### 数据操作

##### 定义

#### 功能

#### 函数

#### 实例化

#### OOP 概念

#### 示例

#### 用户自定义类

#### 调用 `var_dump`

### 可选偏移量参数

## P

### 包管理

### PATCH 方法

### PHP

##### 数组

#### 框架

#### 库

#### MySQL 预处理语句

#### 正则表达式，字符序列

#### 保留字

#### 变量

### PHP 8

### `php bin/console doctrine:database:create`

### `php bin/console doctrine:migrations:migrate`

### `php bin/console make:controller UserController`

### `php bin/console make:entity`

### `php bin/console make:migration`

### PHP 与 MySQL 通信

#### Docker 开发环境

#### `mysql:8.0`

#### MySQLi 方法

#### PDO 版本

### PHP 连接数据库

#### `addUser.php`

#### `artisan`

#### ASC 和 DESC 选项

#### 条件

#### 数据库变量

#### 数据组织

#### `db.php` 文件

#### `execute` 方法

#### 逻辑处理

#### MySQL

#### `mysqli` 构造函数

#### MySQLi

#### ORDER 查询

#### 数据填充

#### SELECT 查询

#### `users` 表

#### `DB_HOST`

#### `mysqli` 对象

#### `query` 对象

### PHP CRUD POST 方法

### PHP 数据对象 (PDO)

#### 优势

#### 方法

### PHP 数据类型

#### 分类

#### 复合类型

##### 数组

#### 对象

#### 数据存储

#### 标量类型

##### 布尔型

##### 定义

##### 浮点型

##### 整型

##### 短语

##### 字符串

#### 特殊类型

##### NULL

### 资源

### PHP 异常

#### catch 块

#### `checkMyNum()`

#### Finally 块

#### 机制

#### 运行时错误

#### 带函数的特殊类

#### `throw`

#### `throwable`

#### try 块

### PHP 表单数据处理

### PHP 表单验证

#### 电子邮件

#### 空字段

#### 错误消息

#### 文件

#### HTML 网页表单

#### 字母和空格

#### 正则表达式函数

#### 特殊字符

#### 值

### PHP 框架互操作性组 (PHP-FIG)

### PHP 基础知识

#### 错误

#### 对象

#### 变量

#### 动词

### PHP GET 表单

#### `basicForm.php`

#### `functions.php`

#### 值设置

#### 变量设置

#### name 和 email 变量

### PHP 全局变量 `$_POST`

### phpMyAdmin

#### 界面

#### 模型

#### 工具

### PHP POST 表单

#### 代码结果

#### 开发者思维

#### 电子邮件服务器

#### `filter_var` 函数

#### `functions.php`

#### HTML 元素

#### HTML 表单

#### 方法设置

#### POST 变量

#### 特殊字符

#### 用户输入

### PHP 编程语言

#### 命令行

#### 数据组织与传递

#### 事实上的编程语言

#### 开发环境

#### 重要性

#### 脚本集合

#### 服务器端脚本

### `PHP. $_REQUEST`

### PHP 标准推荐 (PSR)

#### 自动加载

#### 编码风格

#### 目标/目的

#### HTTP

#### 接口

#### 规范

#### 标准列表

### PHP 字符串函数

#### `is_string()`

#### `strlen()`

#### `strpos()`

#### `str_replace()`

#### `strstr()`

#### `strtolower()`

#### `strtoupper()`

#### `substr()`

#### `trim()`

### 规划新数据库

#### CHAR 数据类型

#### 选择 CHAR 而非 VARCHAR

### 数据库引擎选择

#### 连接条件

#### MySQL EXISTS 函数

- 不在索引列上使用函数

- 无省时省力的日期设计

- ORDER BY 子句

- 合适的数据类型

- SELECT 查询

#### 多态

### POST

- 功能

- 方法

- 请求

- 请求提交的数据

- 路由

### Postman

- `Postman` 请求

### `preg_match()`

### `preg_match_all()`

### `preg_replace()`

### `preserve_keys`

### `prev()`

### 私有类

### 受保护类

### 公有类

### PUT 方法

## Q

- `量词`

- `查询复杂度`

## R

- `range()`

### `/register`

- `/register POST` 路由

- `register.blade.php`

- `RegisteredUser`

- `/register POST` 路由

### 注册视图表单

- 控制器代码

- 表单构建器实例

- `FormBuilder` 方法

- 包安装

- `POST` 提交请求

- `User` 实体

- 查看文件

- `XSS` 防护

### 正则表达式

- 算术运算符

- 区分大小写

- 注意事项

- 函数

- 分组

- 元字符

- 修饰符

- 量词

- 方括号

- 语法

- 用途

### `remove_session.php`

### `替换` 数组

### `请求` 面板

### `reset()`

### 资源

- `resources/views/` 目录

- 基于 `REST API` 的框架

- `REST API` 谓词

- `routes/web.php` 文件

### 行级锁定

### `rsort()`

## S

- `数据净化`

- `search_value` 参数

- `安全性`

- `seedDB` 文件

- `seedDB.php`

- `数据填充`

- `连续整数键`

- `会话管理`

- `会话`

  - 一致性命名

  - 数据库连接

  - 数据移除

  - 声明

  - 描述

  - 输入与净化

  - 管理

  - `remove_session.php`

  - `seedDB.php`

  - `session_start()` 函数

  - `session_test.php`

  - 存储

  - 测试数据

  - 变量

- `session_start()` 函数

- `会话` *vs.* Cookie

- `session_test.php`

- `setcookie()` 函数

- `setlocale()`

- `setRegistrationNumber`

- `设置器`

- `showAddBook` 函数

- `showBooks` 函数

- `showEditBook` 函数

- `shuffle()`

- `sizeof()`

- `Skittles`

- `slogan` 方法

- `sort()`

- `排序类型标志`

- `源代码管理系统`

- `SQLException`

- `SQL 注入`

- `方括号`

- `src/Entity/User.php`

- `Stack Overflow` 平台

- `基于标准的方法`

- `start_session()` 函数

- `静态数据`

- `字符串`

  - 复杂语法

  - 正确语法

  - 定义

  - 双引号

  - heredoc 语法

  - 标识符

  - 引号内

  - 字面量

  - nowdoc 语法

  - 变量与转义序列

  - 变量复杂性

- `strlen()` 函数

- `strpos()` 函数

- `str_replace()` 函数

- `strstr()` 函数

- `strtolower()` 函数

- `strtoupper()` 函数

- `结构元素`

- `结构化查询语言 (SQL)`

- `substr()` 函数

- `超全局变量`

- `Symfony` 应用程序

  - 创建

  - 数据库设置与配置

  - 项目创建

- `symfony check:security`

- `Symfony CLI`

- `Symfony` 开发工具栏

- `Symfony` 目录结构

- `Symfony` 框架

  - 自动化工具

  - 社区支持

  - 定义

  - 特性

    - 依赖注入

    - 易于使用

    - 可扩展

    - 高性能

  - 维护 `PHP` Web 应用程序

  - 模块化组件

  - `PHP` 灵活框架

  - 稳定且可持续的模型

  - 全栈框架

  - `Profiler` 工具

  - 命令行工具

  - 组件

  - 描述

  - 开发工具栏

  - 文档

  - 请求面板

  - `SensioLabs` 创建者

  - 工具箱

  - 网站与应用程序开发

- `Symfony` 安装

  - `CLI`

  - 基于操作系统

  - 结果

  - 前提条件

  - 系统要求

  - 验证

- `Symfony` 项目创建

  - 应用特定包

  - 命令

  - 仪表板网页

  - 开发工具栏

  - 已安装的包

  - 包漏洞

  - `Symfony` 服务器

  - `symfony server:start`

  - `Symfony` Web 服务器

## T

- `表锁定` *vs.* 行级锁定

- `任务自动化`

- `tempalte3.php` 文件

- `测试框架`

- `$this` 变量

- `抛出`

- `可抛出的` 异常与错误

- `特性`

- `trim()` 函数

- `尝试`

- `二维数组`

## U

- `uasort()`

- `UI` 组件

- `uksort()`

- `未捕获的异常错误`

- `更新` 方法

- `UserClass()`

- `用户数据` 存储

- `用户实体` 创建

- `user/index.xhtml.twig`

- `UserPasswordHasherInterface`

- `usort()`

## V

- `验证`

- `value_compare_func`

- `VARCHAR`

- `Var_dump()` 函数

- `变量`

  - 数据存储

  - 声明命令

  - 名称

  - 规则

- `vartest.php`

- `谓词`

## W

- `Web 表单验证`

- `WordPress` 配置文件

## X

- `XSS` 防护

## Y, Z

- `YEAML` 文件

- `又一种标记语言 (YAML)`