# 搜索模块，699–711

## 管理选项，700–701

## Apache Solr 项目

- 配置，707–708
- 自定义，708–710
- 与服务器集成，710–711
- API，704–706
- 搜索结果和分面块，703
- 搜索结果，703
- `search.api.php` 文件，704
- 搜索，Apache Solr 项目与，710–711

## 安全外壳（SSH）协议，858–859

## 安全性，125–136，863

- 流程，128–136
- 选择模块和主题，129–132
- 代码，132–135
- 处理问题，135–136
- 选择协议，236–237
- 站点安全性，125–128
- 分配权限，127
- 避免使用 PHP 过滤器模块，128
- 保持文本格式严谨且安全，127
- 为管理员保留第一个用户，126
- 强密码，125–126

## 安全漏洞，131

## `security@drupal.org`，135

## `--security-only` 标志，605

## `--select` 标志，603

## `-select` 选项，490

## `--select` 选项，624

## 选择查询

- 使用 `variable_get()` 方法进行计数和分组，450
- 通过查询获取数据，443–445

## 可选择的库，374

## 使用 Drush 选择站点，597–600

## 选择设置，121

## 语义标记，651–665

- 数据内容，654
- 去中心化数据空间，654
- HTML，656
- 信息识别，651–654
- 在全球范围内链接数据，655
- RDF 映射结构，661–665
- 使用的选项，661–662
- 词汇表，662–665
- RDFa，656–658
- 网络，659–660

## 语义互联在线社区（SIOC）词汇表，662

## 服务器，被黑客入侵，825–827

## 服务器

- 访问设置，使用创建书签，235–236
- 创建数据库并导入，245–246
- 将 Apache Solr 项目与服务器集成，710–711
- 管理 Solr 索引中的数据，710
- 搜索与分析，710–711
- 将站点迁移至服务器，245

## 服务器端任务与工具，239–241



规避一键安装程序，240–241

数据库设置

- 使用 `phpMyAdmin` 工具，239–240
- 使用向导和手动工具，240

会话处理，初始化，690

会话子系统，648–649

设置错误报告的状态，876

设置回调函数，776

设置表单，464–467

设置属性，280

`settings.php` 文件，126，141–142，246–247，597，616，648，689，921

`setUp( )` 方法，528

`setup.exe`，230

七号主题，272，275，345

快捷图标设置，277

快捷列表，自定义，225

快捷模块，92，277

`show( )` 方法，327–329，782

`SHOW TABLES`，899

`--show-invoke` 选项，632

`--show-invoke output` 选项，632

`--show-passwords` 选项，609，615

侧边栏第一个区域，289，300

`sidebar_first` 区域，529–530，929

相似性法则，717

简单知识组织系统（SKOS）词汇表，662

`simplehtmldom.sourceforge.net`，772

Simpletest 框架，功能测试，517–532

- `.test` 文件，522–526
- 优势与注意事项，518
- API，530–531
- 描述，519–520
- 表单，528–530
- 延伸阅读，530–531
- 运行测试，520–522，526–527
- 设置测试，520–522
- 向 Drupal.org 提交补丁，531–532
- TDD，519
- 何时使用，518

`--simulate` 选项，612

模拟，无障碍访问，945

SIOC（语义互连在线社区）词汇表，662

SIPP（收入与项目参与调查），942

网站架构与视觉设计，939

站点构建器，735

站点上下文，608

网站上线检查清单，内部文档，226

Drush 的站点列表，609–610

站点配方，内部文档，226

使用 Drush 进行站点选择，597–600

站点模板，803–805



开发配置文件，804–805

示例发行版，Drune，805

全功能服务，804

`sitealias` API，620

`site-alias` 命令，616

站点。*参见* 网站

`sites` 目录，140

`sites` 文件夹，94，140，597，637

`sites/all/libraries` 文件夹，97

`sites/all/modules` 目录，491，508，523

`sites/all/modules/contrib` 目录，94，386，502

`sites/all/modules/ctools` 目录，51

`sites/all/modules/custom` 目录，94，384，386–387，502，906

`sites/all/modules/mymodule/mymodule.info` 文件，526

`sites/all/modules/mymodule/mymodule.module` 目录，523

`sites/all/modules/mymodule/mymodule.test` 目录，519，524

`sites/all/modules/views` 目录，51

`sites/all/themes` 目录，278

`sites/all/themes/gamma/gamma.info`，356

`sites/all/themes/gamma/js/gamma.js`，356

`sites/all/themes/mytheme` 目录，292

`sites/default`，540，647–648

`sites/default/default.settings.php` 文件，11，973

`sites/default/files` 目录，11，490

`sites/default/files/coder_upgrade/new` 目录，491

`sites/default/files/coder_upgrade/old` 目录，490

`sites/default/modules/` 目录，386–387

`sites/default/modules` 文件，493

`sites/default/settings.php` 目录，240，637，973

`sites-enabled` 目录，248

`sites/example.com/modules/custom/` 目录，387

特定站点模块，使用 API，792–794

`sites/sitename/themes` 目录，278

`sitesvall/modules/custom`，386

`site-upgrade` 命令，893

`site-upgrade-modules`，904

草图绘制

概述，724–725

规则模块用户界面，725

跳过导航，941

`skip-tables` 选项，615

SKOS（简单知识组织系统）词汇表，662

石板色，14

滑块库，375

标语设置，276

`small_square`，160

智能裁剪模块，99

史密斯·格雷格，219

社交网络，相关模块，104–106

评论通知，104–105

有机群组，105

Profile2，106

评分，105

角色限制，106

用户积分，105

投票 API 依赖，105



`sogame.cat/dummylipsum`，174

`Solr project`（Solr 项目），`Apache`（阿帕奇）。*参见* `Apache Solr project`（阿帕奇 Solr 项目）

`sort()`方法，432

`Sort criteria`（排序条件），61，66–67

`sortable`库，376

`sortable tables`（可排序表格），在表格中显示数据，456–460

排序，与逻辑运算符结合使用，65–66

`source .bash_profile`文件，37

`source`命令，602

`--source-dump`选项，614

空格

- 控制语句与括号之间，405
- 二元运算符和连接符两侧，406
- 缩进两个空格，405
- 参数之间，405

`Specific context`（特定上下文），608

赞助，881

`SQL`（结构化查询语言）中的`NULL`值，642–643

`SQL`转储，263

`sql-conf`命令，615

`sql-sync`命令

- 使用`Drush`站点上下文控制选项，616–617
- 在远程系统上无需安装`Drush`即可使用，615–616

`square_root()`方法，517

`SSH`（安全外壳协议），858–859

设置`SSH`密钥对，611–612

`st()`方法，808

`staging server`（暂存服务器），222

`staging websites`（暂存网站），251–261

- 方法，252
- 捕获所有代码变更，256–260
- 代码变更，254–255
- 内容变更，253–254
- 开发工作流，260–261
- 工作流，252–253

`standards`（标准），编码，862

`Stark`主题，272–273

`starter themes`（入门主题），350

`static queries`（静态查询）

- 使用`variable_get()`方法进行计数和分组，450
- 连接两个表，使用`fetch`获取数据，445–447

`stats.php`，648

`Status`字段，`Suggestion`内容类型，187–190

`Status`选项，筛选下拉菜单，486

`storage`（存储）

- 贡献模块，94
- 数据，548–549

`store configuration`（商店配置），`Drupal Commerce`项目，566–585

- `Cart`模块，583
- 结账系统，581–583
- `Commerce`模块，569
- `Customer`模块，577
- 动态定价，570
- 启用支付方式，579–581
- `line items`（订单项），573–575
- 订单系统，577–578
- `Payment`模块，578
- `Price`模块，569–570
- `Product display`节点类型，575–577
- `Product`模块，571–573



产品参考模块，575

主要组件摘要，584–585

压力测试设计，730–733

空屏幕状态，731

错误屏幕状态，733

满屏状态，732

正常屏幕状态，731

字符串运算符，400

`strlen($element['#value'])`，781

Strongarm 模块，258

结构化查询语言 (SQL)，中的 NULL 值，642–643

结构/功能，111

`structure-tables` 选项，615

样式设置，163

`style-rtl.css` 文件，343

样式，覆盖，74

样式表

为 Internet Explorer 添加条件，345

通过 `.info` 文件添加，343–344

对于管理页面，有条件地包含，788–790

使用 `drupal_add_css()` 方法有条件地加载，344

使用 `hook_css_alter()` 方法控制，345–348

`stylesheets` 属性，280

`stylesheets[all] = file.css`，355

`stylesheets[TYPE][]` 指令，440

使用 CSS 文件进行样式化，440–442

Submit Again 模块，495

提交按钮，更改其标签，505–506

`submit` 方法，259

子站点，使用 Acquia Dev Desktop 应用创建，988–989

`substr()` 方法，398

`substr_count()` 方法，781

子主题，基础主题和，348–351

自定义，351

继承，349

流行，350

作为开发者的成功，841–843

预算时间，842

创建流程，841–842

使用发行版，843

使用经验丰富的开发者，843

使用安装配置文件，843

成功日志级别，625

`sudo a2ensite dgd7` 命令，248

`sudo apt-get install git` 命令，39

建议与探索属性，而非确认属性，722

建议内容类型概述，16–19

状态字段，限制访问，187–190

建议

`hook`

和模板文件，305–308

和主题方法，308–310

页面级别，306–308

`suggestions` 路径，768

`suggestions/installing-drubuntu`，802

峰会，Drupal，879

超级用户账户，设置，983

收入和项目参与调查 (SIPP)，942

可持续性

贡献者的，848–849

在主题化中，351–354

利用默认 CSS 类，353–354

模块 vs. 主题，354

有目的地覆盖模板文件，353

从良好的基础主题开始，351–352

可持续的选择，842

`switch` 语句，403，763

SwitchTheme，275

语法致命错误，472

Syslog 模块，94

系统名称，查找

在代码中，430–431

在数据库中，428–430

`system_admin_config_page()` 方法，465

`system_block_view()` 方法，395–397

`system_element_info()` 方法，324，331

`system_help` 区块，395，397

`system_init()` 方法，692

`system_main`，926

`system_modules()` 方法，384

`system_rebuild_theme_data()` 方法，285，415，421

`system_requirements()` 方法，919

`system_schema()` 方法，453

`system_settings_form()` 方法，465，467

`system_themes_page()` 方法，415

`system.admin.css` 文件，342

`system.base.css` 文件，342，354

`system.maintenance.css` 文件，342

`system.menus.css` 文件，342

`system.messages.css` 文件，342

`system.module` 文件，414–415

`system.theme.css` 文件，342



### T

`t()`方法，412–414，424，470–471，474，479，529，808

## 目录

使用 Book 模块制作目录，168–178

- 添加到主菜单，178
- `Chapter`内容类型，172–175
- 为组织和编写章节设置权限，170–171
- 使用`Menu Block`模块以更好显示，176–177

### `Table` 选项

68

### 表格

453–454

- 管理表格，使用暴露的过滤器克隆和制作，80–81
- 格式化数据以表格形式显示，433–435
- 可排序表格，显示数据，456–460

### 标签

- 使用本地任务菜单项定义，425
- 用于独特显示，78–79

### `tabs` 库

377–378

### 标签

- `<?php` 开始标签，404
- 分支与标签，在`Drupal.org`上，861
- 关闭 PHP 标签，404
- 与替换标记表单，776–782
- 定义设置回调，776
- 过滤器设置，777–782
- 接受多个元素的表单，776–779
- 在保存前使用`validate`方法操作值，779–780

### `--tar` 选项

817

### `--target-dump` 选项

614

### `--target-structure-tables='users,user_roles'` 选项

620

### 任务管理系统

213

### 服务器端任务

239–241

- 规避一键安装程序，240–241
- 数据库设置，239–240

### `Taxonomy` 模块

24–26，92

### 分类系统

223

### `Taxonomy term` 视图

57

### 分类术语

223

### `taxonomy_help()` 方法

396

### `Tax/Tax UI`

568

### `t('Content summary')`

413

### TDD（测试驱动开发）

519

### 修改摘要显示

166–167

### 技术技能

397–408

- `<?php` 开始标签，404
- 自动化遵守，406
- 检查权限，407
- 清除缓存，407
- 关闭 PHP 标签，404
- 控制语句新行起始，405
- 控制结构，402–403
    - `if`、`elseif`和`else`语句，402–403
    - 循环，403
    - `switch`和`case`语句，403
- `Drupal`编码标准，404–406
- 运算符，399–402
    - 算术运算，400



#### 索引

- `赋值`，399–400
- `比较`，400–401
- `逻辑`，401–402
- `字符串`，400
- `三元`，401
- `PHP 语言`，398
- `在内部方法前加下划线`，404–405
- `显示错误`，408
- `空格`
  - 控制语句与括号之间，405
  - 二元运算符与连接符两侧，406
  - 缩进两个空格，405
  - 参数之间，405
- `术语`，398–399
- `模板文件`，293–301
  - `通用核心模板`，295
  - `全局变量`，296–301
  - `钩子建议` 与，305–308
  - `覆盖`，295–296
  - `有目的地覆盖`，353
  - `主题`与模板中硬编码变量的对比，288
  - `使用模板对表单进行主题化`，336–339
    - `操作表单元素`，337–338
    - `预处理方法`，338–339
- `template_preprocess()` 方法，297，314
- `template_preprocess_block()` 方法，297，305
- `template_preprocess_book_navigation()` 方法，799
- `template_preprocess_field()` 方法，754
- `template_preprocess_html()` 方法，344–345
- `template_preprocess_node()` 方法，297，305，314–316，476
- `template_preprocess_user_picture()` 方法，320
- `template_preprocess_username()` 方法，277
- `template_process()` 方法，315
- `template.php` 文件，318，333，356，359，476
- `模板`
  - 为包裹器添加类，318
  - `书籍模块`，重用模板以显示非书籍导航，798–801
  - `通用核心`，295
  - `核心`，模板中的渲染数组，326
  - `自定义主题化`，751–754
  - `templates 子目录`，751
- `术语`，在代码中，398–399
- `三元运算符`，401
- `测试环境`，代码，539–541
  - 复制，540–541
  - 在 Drupal.org 的沙盒中共享，541
- `.test` 文件，522–526
- `测试计划`，大纲，735
- `测试驱动开发（TDD）`，519
- `test.info` 文件，481
- `测试`
  - 自动化，用于无障碍性，944–945
  - 选择测试用户，735–736
  - 探索性可用性测试，736–737
  - 环境，737
  - 方法，736



- `scenario`，736
- `tasks`，736–737
- `Rules` 模块，使用原因，735
- 设置模块、记录和日志记录，738
- `Testing` 模块，94
- `test.module`，481
- `test.php` 文件，445，787

### `text`
- 添加允许显示图像的格式，184–187
- 双向支持，343
- **自定义过滤器**
  - 为输出添加自定义标记，795
  - 使用指定类名的容器表单元素，790–791
  - 编辑文本格式，794
  - 示例，773
  - 过滤内容，783–788
  - 钩子（hooks），783
  - 模块，771–773、776–788、791–794
  - 管理页面的样式表，788–790
  - 标签和替换标记的形式，776–782
- 编辑格式，794
- **编辑器**
  - 配置其偏好设置，238
  - 设置要使用的 FTP 客户端，238
  - 进行设置，237–238
- 保持格式紧凑且安全，127
- `text_long` 格式，559

### `Theme`，定义，213
- `theme()` 方法，使用其制作主题表单，334–335
  - 在其中操作表单元素，335
  - 使用 `drupal_render_children()` 方法，334
- `Theme Developer` 模块，312–313
- `Theme Developer` 窗口，313
- 主题引擎，854
- `Theme Screenshot`，349
- `Theme Settings`，349
- `theme_block()` 方法，923
- `theme_contact_site_form()` 方法，334
- `theme_container()` 方法，324
- `THEME_container()` 方法，412
- `theme_container()` 方法，790
- `THEME_container__xray()` 方法，412
- `THEME_container__xray__form()` 方法，412
- `theme_form()` 方法，332
- `theme_form_element()` 方法，766
- `theme_get_suggestions()` 方法，306
- `'theme_hook_suggestions'` 数组，748
- `theme_html_tag()` 方法，324
- `theme_image_resize_filter_form()` 方法，782
- `theme_item_list()` 方法，324
- `theme_links()` 方法，277、308
- `THEME_links()` 方法，309
- `THEME_links__comment()` 方法，309
- `THEME_links__contextual()` 方法，309
- `THEME_links__contextual__node()` 方法，309
- `THEME_links__node()` 方法，309
- `theme_links__system_main_menu()` 方法，309
- `theme_links__system_secondary_menu()` 方法，309
- `theme_menu_link()` 方法，278
- `theme_more_link()` 方法，303
- `theme_my_theme_hook()` 方法，302
- `theme_name_menu_link()` 方法，278
- `theme_name.info` 文件，278



`theme_node()` 方法，305

`theme_node__suggestion()` 方法，753

`THEME_preprocess_page()` 方法，310

`THEME_preprocess_page__front()` 方法，308

`theme_table()` 方法，336, 437, 457

`theme_that()` 方法，302

`theme_this()` 方法，302

`theme_user_admin_permissions()` 方法，433

`theme_username()` 方法，277

`theme_xray_permission_names()` 方法，435

`theme_xray_show_callback`，475

`theme_xray_show_page_callback()` 方法，439

`themeable` 代码，使用 `@ingroup themeable` 方法记录，433–435

主题，435–437

无障碍，944

选择，129–132

项目主页，130–131

安全代码审查，132

启用和设置默认值，274–275

实现，317

初始化，引导阶段，691–692

区域与，288

可渲染数组，436–437

资源，436

主题化，269–354

管理，274–278

启用和设置默认主题，274–275

全局主题设置，275–277

安装新主题，277–278

基础主题和子主题，348–351

基础主题，350–351

继承，349

核心主题目录，269–274

Bartik 主题，270

Garland 主题，271

Seven 主题，272

Stark 主题，272–273

主题引擎，273–274

定义元数据，`.info` 文件，278–281

表单，329–340

查找表单 ID，332–333

`hook_theme()` 方法，333–334

标记生成，329–332

使用 alter 钩子修改，339–340

使用模板文件，336–339

使用主题方法，334–335

函数，301–304

调用，302

覆盖，302–304

钩子建议

与模板文件，305–308

与主题方法，308–310

钩子，304–310

管理 CSS 文件，341–348

添加、移除和替换，343–348



聚合与压缩，341

模式与命名约定，342–343

预处理与处理方法，313–320

在用户照片下方添加更换图片链接，320

向模板包装器添加类，318

查找 `$variables` 数组的内容，317

钩子，315–317

对节点进行修改，319–320

区域，282–292

- 默认，284–286
- 与在模板文件中硬编码变量的对比，288
- 隐藏，286–287
- 布局策略，289–292
- 模块特定，287
- 与主题，288

`render()`、`hide()` 和 `show()` 方法，327–329

可持续性与最佳实践，351–354

- 利用默认 CSS 类，353–354
- 模块与主题，354
- 有目的地覆盖模板文件，353
- 从良好的基础主题开始，351–352

模板文件，293–301

- 通用核心模板，295
- 全局变量，296–301
- 覆盖，295–296

模板，自定义，751–754

主题层，在其中查找可用变量，311–313

使用渲染 API，321–326

- 元素，322
- 渲染数组，321–326

三列布局，285

倾斜图像，184

时间预算，842

Timely 属性，724

`timer_start()` 方法，909

时间戳项，621

迁移过程的时间安排，910

`'title arguments' => array(1)`，678

标题代码，530

标题字段，150，225

标题项，359，671

视图的标题，60–71

令牌依赖，96

工具栏模块，93

`toolbar.js` 文件，363

工具

- 了解，535–536
- 服务器端，239–241
    - 规避一键安装程序，240–241
    - 数据库设置，239–240

总包含成本，918

总自身成本，918

`.tpl.php` 后缀，336

追踪器模块，94

追踪器视图，57

追踪里程碑，212

追踪系统，212–213

触发器模块，94

设置截取长度，166–167

截断格式，167

`t('Save blocks')`，529

双重嵌套数组，246

使用字段格式化程序链接 Twitter 账户，757–766

- 使用默认缓存表缓存数据，765–766
- 获取用户名，763–764

将组件整合在一起，513

类型属性，808

类型项，621

类型，倾向性，707–708

典型慢查询摘要，920

`-u` 参数，`mysqldump` 命令，244



### U

Ubuntu 操作系统，971–975

- 使用 Drubuntu 脚本针对开发进行定制，972
- 在 Ubuntu 上安装 Drupal，973–975
  - 自动安装程序，974–975
  - 数据库，974
- 在 Microsoft Windows 或 Apple Mac OS X 上运行，971
- `ubuntu.com/download`，971

用户界面

- 为实体提供管理界面，555–559
- 在用户界面中编辑数据，548–549
- 保持与 API 分离，536–537
- Rules 模块草图，725

- 解压存档，238–239
- “施工中”图标，212
- 以下划线作为内部方法的前缀，404–405
- “无格式”选项，68
- 统一资源定位符。*参见* URL
- 单元测试，517
- 通用唯一标识符 (UUIDs)，260
- `--unlock=all` 选项，606
- `--unlock=module_name` 选项，606

- 更新钩子
  - 在更新钩子中捕获额外的升级步骤，896–904
  - 自动化字段（Fields）升级，902–903
  - 模块，896–902
  - 重新运行升级，903–904
  - 概述，258–259
- “更新管理器”模块，93
- 更新路径，710
- `update_advanced` 模块，606
- `updatedb` 命令，605
- `update.php` 文件，126，141，252，255，259，605，887–888，896，903

- 更新
  - 使用 Drush 应用更新，603–606
  - 概述，813
  - 执行更新，137–147
    - 贡献模块，145–147
      - 自动安装程序，146
      - 使用 Drush 工具，146–147
    - 使用差异（diff）方法，143–144
    - 使用 Drush 工具，142
    - 更新的重要性，137–138
    - 手动更新，139–142
    - 更新前的准备，138–139
- 升级模块，897–901
- `UPGRADE.txt` 文件说明，140–142

- 升级
  - 自动升级，为 Features 模块添加升级支持，906–907
  - 针对粘合代码（glue code）的自定义升级方法，896–897
  - 从 Drupal 6 升级到 Drupal 7，887–911
    - 评估当前状况，889–891
    - 在更新钩子中捕获额外的升级步骤，896–904
    - 数据迁移，907–911



特性模块，905–907

重复运行升级操作，891–896

自动化字段升级，902–903

模块，485–488

编码人员升级模块，489–493

向 Drupal.org 贡献补丁文件，498–499

自定义编码，488

查找模型代码，497–498

识别问题，493–497

API 变更列表，489

提交问题，486–488

重新运行，903–904

使用 FTP 客户端上传文件，237

`--uri` 选项，600–601，608

URL - 检出/#，583

## URL（统一资源定位符）

- 人类可读的，19–20，190–191
- 受密码保护的，222
- 请求。*参见*页面请求，引导阶段
- 允许为用户页面提供可破解 URL 的视图，801–802

## 可用性测试

- 探索性测试，736–737
- 测试环境，737
- 测试方法，736
- 测试场景，736
- 测试任务，736–737
- 参与者
  - 不要求提供解决方案，739
  - 让参与者放松，740
  - 提问技巧，739
  - 在提供帮助前先等待，740
- 设置模块、录制和记录，738

使用 `@alias` 命令，603

使用 AJAX，62

`USE databasename`，898

使用分组，62

使用分页器，61

使用从服务器，63

用户访问，根据其有条件地执行操作，468–469

用户账户，将作者的个人资料与其关联，155–156

用户体验，713–746

### 将原则付诸实践

- 分析结果，740
- 构建 Alpha 版并让用户验证，733–734
- 挑战，721–722
- 文案撰写，743–744
- 设计流程，720–721
- 来自问题队列的反馈，741
- 更宏观的概念，722–729
- 优化，734–738
- 原则，745–746
- 项目描述，742–743
- 发布模块，741–743
- 报告问题，740–741
- 可用性测试，739–740
- 线框图，729–733
- 色彩和谐，719
- 为...而设计，940

### 人类 API

- 法则
  - 接近法则，717–719
  - 相似法则，717
- 记忆，713–714
- 模块化，713
- 感知，715–717

用户组，738

用户组，844–845

用户界面，从中隐藏元素，507–508

用户界面。*参见*UI

Drupal 网站的用户登录框，224

用户对象，530

用户页面，允许为其设置可破解 URL 的视图，801–802

文章和评论设置中的用户图片，277

创建用户故事，216–217

评论选项中的用户验证状态，277

`user_access()` 方法，426，468–469，554–555

`user_admin_settings()` 方法，465

`user_menu()` 方法，464

`user_picture` 主题钩子，293

`user_roles` 表，617

`user/7/shortcuts` 用户的“快捷方式”选项卡，13

`user/86/edit`，15

用户名，获取，763–764

用户感知的性能，913

`user-picture.tpl.php` 文件，293，320

用户积分模块，105

用户，27–28

- 选择哪些用户进行测试，735–736
- 照片，在其下方添加更改图片链接，320
- 用户的图片，183–184
- 为管理员预留首个用户，126
- 让用户验证 Alpha 版，733–734

`users` 表，617

`usr/share/drush/commands` 文件夹，609

自定义工具方法，470–471

UUID（通用唯一标识符），260



### V

`validate method`（验证方法），在保存前处理数值，779–780

验证过滤器设置，780–781

数值，在保存前使用`validate method`（验证方法）进行处理，779–780

`var_dump()` 方法，311

`var_export()` 方法，796

变量系统，初始化，689–690

`variable_get()` 方法，450，465，467–469，785

`variable_set()` 方法，465，467

**变量**
- 在主题层查找可用变量，311–313
- 全局变量，296–301
- 模板文件中的变量，主题 vs. 硬编码，288

Varnish HTTP 加速器，638

`/var/www` 目录，245

`/var/www/dgd7` 目录，246

`--verbose` 标志，625

详细消息，522

`--verbose` 选项，625

版本项，359

版本选项，筛选器下拉菜单，486

`version`（版本）属性，280

版本特定依赖项，390

`vertical-tabs.css` 文件，346

`very_slow_find()` 方法，636

`vi /etc/apache2/sites-available/dgd7` 命令，247

视频播客，MustardSeed Media，226

**视图模式**
- 概述，747–751
- 以不同方式展示相同内容的使用方法，166

作者传记的查看页面，162–164

**视图**
- 允许用户页面使用可自定义的 URL，801–802
- 可用视图，55–56
- 默认视图，56
- 列表的元素，55–56
- 更改所列内容，54
- 使用上下文过滤器，82
- 使用视图模块创建视图，68–76
  - 向视图添加功能，74–76
  - 添加菜单，73
  - 管理信息，71
  - 高级设置框，73
  - 显示方式，71–72
  - 动态编辑，73–74
  - 格式设置，72
  - 创建视图的目标，68
  - 结果数量，72
  - 预览工作成果，73
  - 设置基础配置，69–71
  - 系统化方法，68–69
  - 视图标题，71
  - 查看视图，74
- 解构视图，57–68
  - 配置详情，59–63
  - 显示类型，58–59
- 将视图导出为代码，85–86
- 使用公开过滤器，81
- 扩展视图，76–81
  - 克隆视图并通过公开过滤器创建管理表格，80–81



显示，77–79

处理零结果，76

与其他模块协作，84

与 Organic Groups 模块一起使用，115–117

Views 模块，49–95

管理页面，53–56

操作链接，54

Advanced Help 模块，53

可用的 Views，55–56

更改已列出的 Views，54

高级实现，82–84

上下文过滤器，82–83

关系配置，83–84

Chaos Tools 依赖，95

使用它创建 Views，68–76

为其添加功能，74–76

添加菜单，73

管理信息，71

高级设置框，73

显示，71–72

动态编辑，73–74

格式设置，72

目标，68

结果数量，72

预览工作，73

设置基础知识，69–71

系统化方法，68–69

标题，71

查看，74

解构 Views，57–68

配置详情，59–63

显示类型，58–59

过滤器标准组，65–66

过滤器部分，64–65

格式设置，67–68

覆盖，63

排序标准，66–67

描述，49–53

使用示例，50

权限，50–53

导出为代码，85–86

扩展 Views，76–81

克隆并使用暴露的过滤器创建管理表，80–81

显示，77–79

处理零结果，76

与其他模块协作，84

文件`view.tpl.php`，351

虚拟主机配置文件（Apache），248

virtualbox.org，971

视觉设计，933–940

建议，939–940

明智地选择字体，939

为整体用户体验进行设计，940

记住设计的目的，939

理解站点架构，939

与 Drupal 一起使用

使用 Drupal 的优势，933

页面结构，934–938

从内容出发进行设计，938–939

差异，934

以及 HTML5，940

Visual Studio 开发环境，948

词汇表（RDF），662–665

Voting API 依赖，105

VS.Php 插件，957–962

VS.Php 成功启动界面，959



### W

- `WAI`（网页可访问性倡议），942
- `WAI-ARIA`（可访问的富互联网应用程序），945
- `WAMP`（Windows/Apache/MySQL/PHP）技术栈，948–953
- `WampServer` 控制面板，951
- `WampServer` 默认欢迎界面，951
- 警告日志级别，625
- `watchdog()` 方法，765
- watchdog 子系统，648–649
- `watchdog-show` 命令，623
- 瀑布流样式，207–208
- `WCAG`（网页内容可访问性指南）2.0，942，945
- 网页可访问性倡议 (WAI)，942
- 网页内容可访问性指南 (WCAG) 2.0，942，945
- 网站根目录，978–979
- Web 服务器， Apache，247–248
- 网站，243–261
  - 无障碍访问，943–946
  - 自动化测试，944–945
  - 对比度和颜色，944
  - 专家反馈，946
  - 维护，945–946
  - 模块，943
  - 定期审查页面，946
  - 模拟，945
  - 主题，944
  - `WAI-ARIA`，945
- `Acquia Dev Desktop` 应用
  - 用于创建子站点，988–989
  - 用于导入站点，990
- 向……添加产品，572–573
- 添加引用章节的`Resource`内容类型，179–181
  - 允许用户将通用文件附加到内容，180
  - 使用`Node reference`连接内容类型，181
  - 管理`Resource`内容类型的显示，181
  - 复用章节图片字段，179–180
- 添加允许图片的文本格式，184–187
  - 在文章中插入图片，186–187
- 作者
  - 将章节链接到作者，178
  - 列出作者，157–164
  - 通过个人资料页面展示作者信息，149–157
- 使用`Pathauto`模块自动生成人类可读的 URL，190–191
- 备份，249–251
- `Chapter`数字字段显示，754–757
- 社区。*参见* 社区网站，使用`Organic Groups`模块创建
- 内容类型的情境化“新增”链接，767–771
  - 寻找更好的方法，770–771
  - 模型，767–770
- 自定义文本过滤器，771–795
  - 为输出添加自定义标记，795
  - 带有指定类的容器表单元素，790–791
  - 自定义钩子，783
  - 编辑文本格式，794
  - 示例，773
  - 过滤内容，783–788



