# 索引

## 符号

`==` 和 `!=` 运算符

`$A="Alpha"` 命令

`$A++` 命令

`$color=colors()` 命令

`$gen=gen()` 命令

`$gen->rewind()` 命令

`$num=func_num_args()` 命令

`$number` 变量

`$res=power($z,$num)` 命令

`$rnd=randoms(5)` 命令

`$setNumber()` 方法

## A

抽象方法

匿名函数

数组

- 赋值
- 比较
- 拼接
- 定义
- 元素
- 示例
- 函数
- 循环
- 删除元素
- 元素
- `endforeach` 关键字
- `foreach` 语句
- 多维
- `array_pop($C)` 命令

赋值运算符

关联数组

## B

位运算

## C

`calc()` 函数

`__call()` 方法

`catch` 关键字

子/派生类

类与对象

- 构造函数
- 复制对象
- 创建
- 析构函数
- 构造函数中的字段
- 方法
- OOP 原则
- 私有字段
- 特殊任务
- 静态字段

类成员

常量

控制语句

- `do-while`
- `goto` 指令
- 条件 `if`
- `for` 循环
- `switch` 选择
- `while` 循环

`count()` 函数

`count_type()` 函数

`create()` 方法

`current()` 方法

## D

数据类型

- 函数
- 整数类型

运算

- 算术
- 赋值
- 位运算
- 比较
- 逻辑
- 字符串
- 三元

`defined()` 函数

`define()` 函数

`display()` 函数

`do-while` 语句

`drawLine()` 方法

## E

`echo` 关键字

错误处理

- 多个 `catch` 块
- 类
- 自定义异常
- 异常类
- 处理错误
- 原则
- 重新抛出异常
- 抛出异常
- `try-catch` 结构/`finally` 块

`error_reporting()` 函数

`eval()` 函数

## F

`factorial()` 函数

`fclose()` 函数

`feof()` 函数

`fgets()` 函数

`file_exists()` 函数

文件

- 访问模式
- `fopen()`
- 函数
- 在程序中包含文件
- `myfile.txt`
- 读取
- 写入

`fopen()` 函数

`foreach` 关键字

`foreach` 循环语句

`for` 关键字

`for` 循环语句

`fread()` 函数

`func_get_args()` 函数

`function_exists()` 函数

函数

- 匿名函数
- 参数
- 任意数量
- 默认值
- 传递机制
- 结果类型
- 命令
- 创建
- 定义
- `eval()` 函数
- 命名参数
- 递归
- 结果
- 返回值

`fwrite($h,"[$k] ")` 命令

## G

生成器函数

- 参数
- 数组
- 调用函数
- 定义
- 示例
- 循环语句
- 对象
- 传递值
- 结果

`generator_to_array()` 函数

`__get()` 方法

`getArea()` 方法

`get_class()` 函数

`getMessage()` 方法

`getName()` 方法

`getReturn()` 方法

`getsugar()` 函数

`gettype()` 函数

全局变量

`goto` 语句

## H

`hello()` 命令

`hello()` 函数

## I, J

`if` 关键字

`if` 语句

继承

- 构造函数
- 创建子类
- 字段值
- 多级
- 重写方法
- 私有类成员
- 受保护类成员
- 虚方法

解释器模式

- 命令
- 创建新项目
- `hello.php` 文件
- `index.php` 文件
- NetBeans
- NetBeans 环境窗口
- 程序执行
- 运行时环境
- Visual Studio Code 环境

`is_callable()` 函数

迭代器

- 类
- 接口
- 接口方法
- 对象

## K

`key()` 方法

## L

`list()` 函数

局部变量

逻辑运算符

## M

`match` 语句

- `default` 关键字
- 程序输出
- 程序使用
- 选择语句
- 语法

`method_exists()` 函数

多维数组

多行字符串

## N

NetBeans 环境

`next()` 方法

`numbers()` 函数

`nums()` 函数

`nums()` 生成器函数

## O

面向对象编程 (OOP)

OOP 机制

- 抽象类
- 匿名类
- 继承/接口，实现
- 接口继承
- 接口
- 命名
- 对象类型控制
- 特质

## P, Q

父类

PHP

- 按钮
- 属性
- 点击
- 示例
- 表单
- `index.html` 文件
- 程序
- 提交按钮
- 窗口
- `$_POST` 数组

HTML 文档，脚本

- 浏览器窗口
- 代码
- 斐波那契数列
- 本地服务器
- 端口
- 文本

列表与复选框

- 浏览器窗口
- 表单设置
- `index.html` 文件
- `<input>` 元素
- 程序代码
- 编程

请求参数

- 浏览器窗口
- 条件语句
- 示例
- 欢迎页

滑块/单选按钮

PHP 程序

- 代码
- NetBeans 开发环境
- 模式
- 解释器
- 服务器
- `POST` 方法

`power()` 函数

`print(" ".$b)` 命令

`print($result)` 命令

`print()` 函数

`print_r()` 函数

## R

`randoms()` 生成器函数

`readable()` 函数

引用

- 创建
- 示例
- 程序执行
- 变量

`resize()` 方法

`rewind()` 方法

`Run File` 命令

## S

`send()` 方法

服务器模式

- `hi.php` 文件
- HTML 文档
- 本地主机

`__set()` 方法

`set()` 方法

`setAll()` 方法

`set_exception_handler()` 函数

`setNumber()` 方法

`setSymbol()` 方法

`settype()` 函数

`show()` 函数

`show()` 方法

`sizeof()` 函数

静态变量

`strlen()` 函数

`str_replace()` 函数

`substr()` 函数

`sum()` 函数

`swap()` 函数

`swap($A,$B)` 命令

`switch` 关键字

## T, U

三元运算符

`__toString()` 方法

`trim()` 函数

## V

`valid()` 方法

变量

- 命令
- 注释
- 创建
- `$` 符号

虚拟性

## W, X, Y, Z

`while` 循环语句

Windows 操作系统